# Dynamic Distillation Requirements

## 1. Document Control
- Title: Dynamic Distillation Requirements Specification
- Version: 1.51
- Date: 2026-08-20
- Basis: Current implementation and tests in this repository.

## 2. Scope
This specification captures the as-built behavior of the current codebase plus explicit acceptance requirements identified by model-development evidence. Requirements that are not yet satisfied are called out in the notes and current-state document.

- `UR-*`: user-visible requirements.
- `FR-*`: functional/system requirements.
- Traceability links each requirement to implementation and test artifacts.

## 3. User Requirements
- `UR-001` Users shall be able to run dynamic distillation simulations from an Excel case through the Python CLI runner.
- `UR-002` Users shall be able to configure horizon and integration controls (`n_steps`, `dt`, logging cadence).
- `UR-003` Users shall be able to choose thermo execution mode (`stub`, `relative-volatility`, `clapeyron`, `dwsim`, DWSIM package aliases, `table`, `table-pool`) and thermo refresh strategy.
- `UR-004` Users shall be able to override key operating variables (reflux, boilup, condenser/reboiler duties, condenser pressure drop).
- `UR-005` Users shall be able to enable/disable control loops for level, top pressure, distillate composition, and bottoms composition.
- `UR-006` Users shall be able to enable top-drum PSV relief behavior and configure setpoint/gain/max vent.
- `UR-007` Users shall receive startup validation feedback split into blocking errors and non-blocking warnings.
- `UR-008` Users shall receive structured profile/summary CSV outputs when logging is enabled.
- `UR-009` Users shall be protected against accidental reruns of identical CLI commands unless explicitly overridden.
- `UR-010` Users shall have automatic run provenance tracking and experiment-ledger regeneration.
- `UR-011` Users shall be able to select runtime behavior mode (`parity`, `calibration`, `hydraulic`, `legacy`) from the CLI.
- `UR-012` Users shall be able to import and export richer Excel restart state, including boundary, energy, and controller memory sheets when present.
- `UR-013` Users shall be able to run source-topology validation cases that intentionally exclude standard boundary and vapor dynamic states when the validation source uses algebraic equivalents.
- `UR-014` Users shall be able to distinguish external steady-state seed data from accepted dynamic initial conditions, and shall have documented criteria for making a seeded column dynamic-ready.
- `UR-015` Users shall be able to distinguish a numerically quiet operational checkpoint from a rigorously physical dynamic solution whose tray flows, phase split, energy, pressure, and vapor inventory are mutually consistent.
- `UR-016` Users shall be able to distinguish local thermodynamic closure, global pressure/flow closure, and terminal-equipment closure, including an explicit result when a state is locally valid but outside the global algebraic constraint manifold.
- `UR-017` Users shall be able to distinguish mathematical feasibility of a conservatively redistributed state from an acceptable initializer, including the amount of component, energy, pressure, and temperature movement from the source checkpoint.

## 4. Functional Requirements

### 4.1 Case Loading and Validation
- `FR-001` The system shall load `.xlsx` case files and reject unsupported file types.
- `FR-002` The loader shall parse `Specifications`, `Initial Conditions`, and (best-effort) `Streams`.
- `FR-002a` The loader shall support optional restart-state sheets (`Boundary State`, `Energy State`, `Controller State`) when present.
- `FR-003` The loader shall canonicalize Excel component names to DWSIM-compatible component IDs.
- `FR-004` The system shall build a validated `ColumnSpec` and enforce stage/component consistency.
- `FR-005` The runner shall perform preflight validation and stop before integration on blocking errors.

### 4.2 State Initialization and Startup Conditioning

**Why Initialization Requires Explicit Attention**

The current v1 runtime implements an explicit-vapor, rigorous-energy
**sequential hybrid**, not a completed Index-1 DAE. Pressure, vapor holdup,
vapor flow, liquid flow, energy, and equilibrium targets are evaluated through
several ordered paths, some with previous-step feedback, profile blending, or
limiters. DD-060 through DD-075 show that these paths do not yet share one
consistent physical ownership structure. A future rigorous equilibrium-stage
implementation is expected to be a stiff DAE, but that is a target
architecture rather than an accurate description of the current runtime.

From the published literature on DAE initialization (Pantelides 1988; Barton, Biegler), a steady-state profile from an external tool (ChemSep, Aspen Plus, DWSIM) satisfies that source model's static equations. It does **not** automatically satisfy this model's topology, dynamic hydraulic equations, vapor-flow resistance, weir geometry, terminal inventories, pressure/volume constraints, or energy basis. When an incompatible state is launched directly, the first RHS evaluation can produce large derivatives or fail to lie on the algebraic constraint manifold.

Two separate initialization problems shall be recognized:
1. **DAE-consistent initialization**: hold specified independent variables and conserved differential states as required, then solve the algebraic equations so the state lies on the model's constraint manifold. Differential derivatives need not be zero.
2. **Steady-state initialization**: after algebraic consistency is established, solve the differential balances so all required inventory and energy derivatives approach zero while satisfying the same algebraic equations.

An initializer shall not report steady-state success merely because the algebraic block closes, and it shall not launch a dynamic smoke test from a state that fails algebraic consistency.

Initialization is not a trivial "switch to dynamics" operation; it is a distinct mathematical problem requiring structured root-finding, conservation audits, and acceptance gating. See `docs/dynamic_column_initialization_strategy.md` for mathematical foundation and workflow.

Requirements in this section define how external seeds are loaded, how initialization passes condition the column toward dynamic self-consistency, and what acceptance gates must pass before integration begins.


- `FR-006` The runner shall construct deterministic state layout and initial state vector from `ColumnSpec`.
- `FR-007` The runner shall initialize tray vapor holdup to match startup pressure profile assumptions.
- `FR-007a` In legacy explicit-phase restart mode, when `--use-excel-vapor-holdup` is enabled, the runner shall preserve explicit tray vapor holdup from the Excel `Initial Conditions` sheet through startup pressure initialization and thermo conditioning.
- `FR-007b` The runner shall support disabling dynamic tray vapor states for validation sources whose vapor composition is algebraic and whose equations do not include vapor holdup ODEs.
- `FR-007c` In rigorous conserved-state mode, imported or checkpoint liquid/vapor phase inventories shall be initial guesses only. The algebraic closure shall determine phase allocation, temperature, pressure, equilibrium compositions, and interstage flows while preserving the selected total component inventories and total internal energies.
- `FR-008` The runner shall support optional startup thermo-consistent conditioning iterations.
- `FR-009` The runner shall perform top-drum startup steadying pass for top-holdup residual reduction when top states are active.
- `FR-009a` When explicit runtime restart state is present, the runner shall skip vapor reseeding and startup conditioning/steadying steps that would overwrite the restart state.
- `FR-009b` External steady-state profiles, including ChemSep-derived profiles, shall be treated as initialization seeds unless a model-consistent residual audit or initializer demonstrates that the state is steady under this model's active topology, thermo, feed treatment, holdup states, and RHS equations.
- `FR-009c` Accepted dynamic initialization shall require block-level derivative/conservation checks in addition to any aggregate steady-state detector flag.
- `FR-009d` Initializer acceptance shall be conditional on a model-physics closure gate. An initializer shall not be expected to create a rigorous zero-residual state when the active runtime equations assign competing owners to pressure, phase totals, energy, or interstage flow.
- `FR-009e` Runtime steady-state acceptance shall include a whole-column inventory-rate criterion in addition to local state-rate criteria. A run with material `F-D-B` drift shall not pass solely because the drift is distributed across many tray states.
- `FR-009f` Initializer output shall distinguish at least: local thermodynamic closure failure; local thermodynamic closure success with global hydraulic failure; terminal-equipment mapping failure; algebraically consistent but dynamically non-steady; and fully accepted steady initialization.
- `FR-009g` A dynamic smoke test shall not be used to rescue or accept a candidate that fails the applicable local, global hydraulic, or terminal algebraic-closure prerequisite.

### 4.3 Thermodynamics Services
- `FR-010` The system shall support thermo providers for TP flash, phase enthalpy, and optional Z-factor diagnostics.
- `FR-010a` Rigorous equilibrium acceptance shall use either a direct phase-fugacity residual or a documented backend-certified equivalent. TP-flash phase-fraction consistency shall not be labeled as a fugacity residual when phase fugacity coefficients are unavailable.
- `FR-011` The system shall support tabular thermo from JSON surrogate tables.
- `FR-012` The system shall support process-pool tabular thermo mode for parallel batched stage flashes.
- `FR-013` The RHS shall use provider batch flash when available and fall back to scalar flash otherwise.
- `FR-014` The system shall support thermo refresh throttling by step cadence and optional per-stage `dT/dP/dx` thresholds.
- `FR-014a` The runner shall support optional live-PR override for the equilibrium-relaxation flash path while leaving the main thermo mode unchanged.
- `FR-014b` The system shall document optional external thermo dependencies and installation/setup steps for DWSIM/pythonnet, Python `thermo`, and Clapeyron/pyclapeyron backends.
- `FR-014c` The runner shall allow Clapeyron PR to use DWSIM PR component constants and binary interaction parameters when requested.
- `FR-014d` The runner shall provide a dependency-free `relative-volatility` thermo mode with constant-alpha VLE and simple enthalpy/Cp properties for validation cases with energy states.
- `FR-014e` The repository shall provide a Tier 1 source-topology validation workflow for Skogestad Column A using public source data and relative-volatility thermo, including steady-profile comparison, +1% feed-rate dynamic response comparison, and comparative plots.
- `FR-014f` When a run explicitly requests an external thermo backend, preflight or runtime shall fail clearly if that backend is unavailable. The runner shall not silently continue a nominal DWSIM or Clapeyron run using stale cached thermo packets after systematic backend failure.

### 4.4 RHS, Physics, and Closures
- `FR-015` The RHS shall compute tray/top/bottom component holdup derivatives with feed and boundary source terms.
- `FR-016` The model shall support condenser duty modes `total-condense` and `specified`.
- `FR-017` The model shall support pressure models `spec` and `hydraulic`, including optional condenser pressure drop.
- `FR-018` The model shall support vapor-flow models `profile`, `energy`, and `conductance` with reboiler-neighbor vapor guardrails.
- `FR-019` The model shall support optional equilibrium relaxation and optional energy-holdup states.
- `FR-019a` A production equilibrium-stage mode that changes liquid and vapor phase totals shall conserve component inventory and total tray energy during the phase update. A fixed-temperature/pressure phase target without latent-energy closure is diagnostic only.
- `FR-019b` A rigorously accepted hydraulic run shall have one thermodynamically consistent pressure owner. Hydraulic pressure, explicit vapor holdup, vapor volume, temperature, composition, and compressibility shall agree within an explicit closure tolerance.
- `FR-019b1` Explicit tray vapor holdup shall be a first-class dynamic state in any rigorous dynamic model. The model shall maintain separate vapor inventory and vapor-composition states when the runtime topology includes vapor residence, and it shall not treat vapor holdup as a diagnostic-only or algebraically reconstructed quantity unless the architecture explicitly declares a negligible-vapor-holdup reduced model. Any reduced model that neglects vapor holdup shall be labeled as a reduced-order feasibility layer and shall not be presented as a production dynamic model.
- `FR-019b2` A vapor-holdup implementation shall define one owner each for vapor component inventory, vapor composition, vapor volume, pressure, phase transfer, vapor internal energy, and terminal vapor boundaries. It shall register vapor state coordinates and balances, provider-owned vapor properties, volume/EOS closure, pressure coupling, implicit-step kinematics, consistent initialization, and vapor-specific conservation/refinement gates. The required code-level change plan is documented in `docs/core_v3_vapor_holdup_implementation_plan_20260819.md`.
- `FR-019c` In a rigorously accepted hydraulic run, internal liquid outflow shall be determined by active hydraulics from current state and geometry. Imported liquid-flow profiles may initialize or validate the model but shall not retain blended runtime ownership.
- `FR-019d` Vapor traffic and pressure closure shall be solved consistently with phase inventory and energy. Nominal-profile caps may be used as diagnosed startup safeguards but shall not determine the accepted operating profile.
- `FR-019e` The rigorous equilibrium-stage formulation shall use total component inventory and total internal energy as differential tray states. Temperature, pressure, phase split, phase compositions, and interstage liquid/vapor traffic shall be algebraic variables or outputs of the coupled closure.
- `FR-019f` The algebraic closure shall report local UV/component/energy/volume residuals separately from global pressure-drop and vapor-flow residuals. Passing local UV closure shall not imply passing global hydraulic closure.
- `FR-019g` Total condenser, reflux drum, partial reboiler, and bottoms sump representations shall have explicit conserved-state and volume mappings appropriate to their topology. A rigorous full-column closure shall not omit material or energy stored in terminal vapor or virtual terminal stages.
- `FR-019h` A rigorously accepted algebraic solution shall converge to materially the same state from pressure and flow guesses perturbed by at least `+/-10%`, unless a documented case-specific robustness range is stricter. Multiple materially different solutions or traversal-order dependence shall fail acceptance.
- `FR-019i` If frozen per-stage conserved totals and energies imply no physical global pressure/flow solution, the solver shall report the state as outside the algebraic constraint manifold. A subsequent steady-state solve may redistribute tray conserved states only while enforcing whole-column component and energy conservation and the specified operating degrees of freedom.
- `FR-019j` A conservative redistribution solver shall report per-node and aggregate `Delta N`, `Delta U`, pressure, and temperature movement from the reference checkpoint. Feasibility under conservation and ordering constraints shall not by itself constitute initializer acceptance; the accepted objective shall minimize scaled conserved-state movement and shall still pass global hydraulic and terminal closure.
- `FR-019k` A least-movement redistribution claim shall include materially different initial guesses, report convergence and objective value for every start, and require a reproducible accepted basin before hydraulic continuation. The gate shall also report terminal versus interior shares of absolute material and energy movement and shall reject a candidate whose apparent feasibility depends on large terminal reallocation or pressure discontinuity.
- `FR-019l` Before conserved-state redistribution is interpreted physically, the implementation shall audit `U=H-PV`, pressure-volume unit conversion, fixed-volume reconstruction, phase aggregation, eliminated-placeholder invariance, and objective scaling on all terminal owners plus representative interior controls. A node-local normalization that makes equal physical energy movement materially cheaper in terminal equipment shall be documented and corrected or explicitly justified before acceptance.
- `FR-019m` Conserved internal energy used by an initializer or steady-state solve shall be constructed on one canonical live-property basis. Any replacement of serialized phase enthalpy shall be reported separately from optimizer movement. A checkpoint-repair attempt shall predeclare its retry limit and acceptance gates; failure of the bounded corrected retry shall retire checkpoint projection and require a direct operating-specification steady-state formulation.
- `FR-019n` Before numerical solution, a direct steady-state formulation shall publish deterministic unknown and residual registries, deliberate eliminations, counts by block, closure ownership, a sparse dependency pattern, and a structural-rank audit. The registry shall be square, have no empty residual rows or unused unknown columns, and have no unexplained structural nullity. Separate terminal inventories may be combined into one conserved control volume only when the eliminated transfer is internal to that volume and all material, energy, phase, and level ownership remains explicit.
- `FR-019o` Before a direct nonlinear steady-state solve, every registered residual shall be evaluated with the selected live property backend at the primary guess and a deterministic physically bounded perturbation. The audit shall reject invalid reduced compositions without clipping or projection; prove component and energy telescoping independently; publish physical variable and residual scales; compute the scaled numerical Jacobian at two finite-difference step sizes; require full rank, no numerically unused unknown column, and stable rank at both guesses; and verify the registered sparsity pattern against an uncolored reference or equivalent independent check. A passing numerical gate authorizes bounded continuation only and is not initializer acceptance.
- `FR-019p` A staged direct steady-state continuation shall keep every active stage square, publish its released unknown and residual blocks, preserve the exact physical residual at the final homotopy endpoint, use smooth physical-domain coordinates, save accepted states, and stop on property failure, conservation loss, rank loss, excessive conditioning, coordinate saturation, or residual-gate failure. Full rank alone shall not establish endpoint feasibility. If a stage that fixes unreleased conserved component or energy states cannot reach physical closure under its predefined step and retry limits, the implementation shall revise the release ordering so conserved and phase states move consistently; it shall not lower tolerances, project the accepted state, or begin open-ended anchor tuning.
- `FR-019q` Every proposed continuation endpoint shall have full structural rank in its physical residual subgraph before a live solve. Anchor rows may establish a nonsingular lambda-zero homotopy but shall not conceal physical endpoint nullity. If the predefined final release-order redesign remains structurally singular, the system shall retire manual staged continuation; it shall not promote another equation family into a new first-stage variant. Subsequent work shall use a materially different architecture such as full-system pseudo-transient continuation, an analytic/automatic-derivative nonlinear or DAE solver, a reduced validation model, or an explicit physical-feasibility study.
- `FR-019r` Before full-system pseudo-transient development is authorized for a direct conserved formulation, one deterministic five-volume case shall retain the same live thermo, conserved component/energy, volume, equilibrium, Francis-hydraulic, vapor-pressure-drop, feed/product, terminal-equipment, and operating-specification equations. It shall publish structural and two-step numerical rank audits, use two predefined physical seeds, and attempt fixed trust-region and full-system pseudo-transient methods without clipping, profile forcing, equation-block removal, or post-result tuning. Authorization requires a common positive, pressure-ordered, conservative root with scaled physical residual `<1e-7`, full final rank, acceptable conditioning, no property fallback or coordinate saturation, and materially seed-independent results. Failure shall retire that formulation from production-initializer development; it shall not authorize a tray-count ladder, topology variation, tolerance change, solver sweep, or full-system pseudo-transient campaign.
- `FR-019s` A replacement rigorous architecture shall select one modeling family before implementation. The selected v2 family is equilibrium-stage DAE; a rate-based model with explicit interfacial mass- and heat-transfer laws is out of scope. Before code is written, v2 shall publish its physical control volumes, every crossing stream, differential and algebraic variable lists, one-owner table, complete governing equations, operating degrees of freedom, structural expectations, exclusions, phased validation gates, and stop rules.
- `FR-019t` The first v2 physical layer shall use prescribed ordered pressure, negligible tray vapor holdup, total component inventory and total internal energy differential states, algebraic temperature/compositions/equilibrium, Francis hydraulics as the sole tray liquid-flow owner, prescribed rectifying and stripping vapor rates, an algebraic total condenser, a liquid reflux drum, and one combined partial-reboiler/sump volume. The reduced steady specification shall own terminal liquid amounts and solve terminal product flows rather than leave terminal inventories free. It shall not contain equilibrium-relaxation transfer, profile-blended liquid flow, profile-capped or previous-step-owned vapor flow, controller-assisted acceptance, accepted projection, property fallback, or case-specific interior tray equations.
- `FR-019u` V2 shall pass gates in physical-complexity order: structural and source-equation assembly; one-volume energy/property closure; one five-inventory-volume prescribed-pressure column with prescribed section vapor rates and Francis-only tray liquid-flow ownership; one energy-determined vapor-traffic layer; unchanged production tray count; one simultaneous pressure-drop layer; and only then an optional explicit-vapor-inventory layer. A failed gate shall stop progression. It shall not trigger a tray-count ladder, tolerance change, solver sweep, or addition of a second physical owner.
- `FR-019v` Before Gate B, the v2 property-free source assembly shall reproduce an independent source implementation for nominal-profile drift, the accepted `+1%` feed step, and one deterministic bounded state perturbation. Acceptance requires normalized full-trajectory parity `<1e-9`, second-integrator or tighter-tolerance agreement `<1e-7`, differential and solver-integrated total/component conservation `<1e-10`, exact disturbance segmentation, live terminal-composition product withdrawal, positive holdup, valid compositions, and no clipping, projection, controller, property call, energy equation, mini8 governing equation, or historical-trajectory substitution.
- `FR-019w` Before Gate C, one role-selected interior inventory volume shall reconstruct `NL` and `x` directly from positive conserved component inventories and solve prescribed-pressure temperature plus the independent equilibrium-vapor coordinates from canonical live DWSIM internal energy and phase fugacity coefficients. The canonical state and predefined inventory, energy, composition, and combined perturbations shall recover a common physical root from three predefined guesses; the scaled algebraic Jacobian shall retain full rank at `h` and `h/2` with condition `<1e8`; live-density geometry shall yield positive liquid volume and height below the physical tray limit; and BDF/Radau nominal, composition, enthalpy, and combined dynamic cases shall pass algebraic residual `<1e-8`, component conservation `<1e-9`, energy conservation `<1e-8`, and normalized integrator agreement `<1e-6`. No serialized enthalpy, fixed interior vessel-volume equality, vapor holdup, clipping, projection, phase relaxation, property fallback, or legacy governing equation is permitted.
- `FR-019x` Before a Gate C nonlinear solve, the five-volume live residual shall eliminate `NL/x` reconstruction coordinates only by exact substitution from positive conserved component inventories, retain the reflux drum as liquid-only, and publish the resulting direct equation count together with its mapping to the unreduced structural ledger. The canonical state plus predefined inventory, energy, composition-transfer, and combined perturbations shall use fixed residual scales and live DWSIM phase enthalpy, density, and relative-fugacity properties. Every state shall have exact component and energy telescoping, full structural and two-step numerical rank, no zero row or column, no unregistered numerical coupling, colored/uncolored Jacobian agreement, condition `<1e8`, positive states and flows, and no clipping, projection, property fallback, serialized enthalpy, geometry adjustment, pressure unknown, or vapor-rate unknown. Passing this audit authorizes one bounded steady solve only; it does not establish that a physical root exists.
- `FR-019y` The Gate C steady-root decision shall use one precommitted bounded trust-region campaign with canonical, deterministic bounded-perturbation, and independently constructed smooth-profile starts. All starts shall use identical equations, scales, bounds, Jacobian rules, and tolerances and shall reach the same physical root with scaled residual infinity norm `<1e-8`, rank `38/38`, condition `<1e8`, conservation at DD-081 precision, positive finite states, physical liquid heights, and no active coordinate bound, clipping, projection, property fallback, regularization, or geometry adjustment. Any failed start, common residual floor, active bound, materially different root, or invalid property state shall fail Gate C and prohibit a solver, continuation, bound, geometry, or operating-specification variant.
- `FR-019z` After DD-082, prescribed section vapor traffic is retired for the five-volume steady feasibility model. Any successor shall begin with a structural-only energy-owned vapor-flow contract: prescribed ordered pressure; four independent internal vapor-link unknowns; simultaneous component and energy balances; all `C` component fugacity equalities at each equilibrium outlet, or a mathematically identical `C-1` relative-fugacity plus one saturation formulation; Francis-only tray liquid flow; fixed feed, reflux, and declared condenser/reboiler duties; specified terminal liquid amounts with `D/B` solved; and no profile, previous-step flow, cap, relaxation, controller, clipping, projection, or property fallback. The three-component registry shall be `37 x 37`, structurally full rank, and exactly conservative before any live property audit. Structural passage authorizes neither a nonlinear solve nor dynamic integration; a dynamic conserved-`N/U` extension requires a separate mass-matrix/index derivation.
- `FR-019aa` Before an energy-owned vapor-flow root solve, one frozen live-property numerical audit shall evaluate the unchanged `37 x 37` residual at a canonical role-mapped seed and one deterministic bounded perturbation, using one canonical residual-scale vector and uncolored central-difference Jacobians at `h=1e-5` and `h/2=5e-6`. Both states and steps shall retain rank `37/37`, condition `<1e8`, no zero row or column, no coupling outside the structural graph, component telescoping `<1e-12`, energy telescoping `<1e-10`, positive physical states and flows, liquid heights below tray spacing, and no clipping, projection, property fallback, profile forcing, controller, limiter, nonlinear solve, or integration. The residual norm shall be reported but shall not be a numerical-readiness criterion. Passing authorizes only drafting and precommitting one bounded steady-root campaign.
- `FR-019ab` The energy-owned vapor-flow steady-root decision shall use exactly one precommitted three-start `scipy.optimize.least_squares(method="trf")` campaign on the unchanged DD-084 `37 x 37` residual. It shall use the canonical DD-084 seed, exact deterministic DD-084 perturbation, and one independently constructed smooth physical seed; fixed physical-space bounds converted once to transformed coordinates; the DD-084 uncolored central-difference Jacobian at `h=1e-5`; unit coordinate scaling; and fixed `1e-12` solver tolerances with at most `500` residual evaluations. Every start shall reach the same physical root with scaled residual infinity norm `<1e-8`, pairwise normalized physical difference `<1e-7`, rank `37/37` at `h` and `h/2`, condition `<1e8`, component/energy telescoping below `1e-12`/`1e-10`, positive normalized compositions and flows, ordered temperatures, liquid heights below tray spacing, no property fallback or safeguard, and no coordinate within `1e-6` of a bound. Any failed start, distinct root, active bound, rank/conditioning/property/physical failure, or post-result contract change shall retire this five-volume energy-owned steady architecture without solver tuning, continuation, wider bounds, or operating-parameter variation. Passing authorizes only a structural dynamic-DAE mass-matrix/index/consistent-initialization contract, not integration.
- `FR-019ac` A total-condenser boundary shall not infer stable liquid solely from a phase-specific liquid enthalpy call. A prescribed-duty candidate shall compute `h_out=hV_in+Q_C/V_top` and independently verify the outlet phase at drum pressure and overall composition; any `beta>0` requires partial-condenser topology and a vapor outlet. The selected reduced steady successor shall instead solve `Q_C`, retain complete material transfer to the liquid drum, and enforce saturated-liquid outlet feasibility with `C-1` incipient-vapor coordinates and all `C` bubble-point fugacity equalities. Starting from the DD-083 ledger, this adds `C` unknowns and `C` equations, giving `10*C+10` unknowns/residuals (`40 x 40` for three components). Before any live root solve, the registry shall be square and full rank, contain exactly one `Q_C` owner and no fixed-duty parameter, preserve exact component/energy telescoping, and contain no profile, cap, relaxation, controller, clipping, projection, or fallback. Structural passage authorizes only one separately frozen live residual/Jacobian audit; it does not authorize solving or integration.
- `FR-019ad` Before a solved-duty saturated-liquid condenser root solve, one precommitted live numerical audit shall retain the first 37 DD-084 coordinates, add `C-1` bubble-vapor additive-log-ratio coordinates, and represent negative `Q_C` with a signed affine coordinate whose scale is the maximum consistent feed/reboiler/condenser energy-rate magnitude. The canonical seed shall solve only the local `C x C` bubble-fugacity block at declared drum pressure/composition and compute `Q_C` from the drum energy balance; the full residual shall not be evaluated until the two complete audit vectors are committed. The canonical and one deterministic bounded perturbation shall retain full `40/40` rank, condition `<1e8`, local bubble rank `3/3`, no zero or off-registry coupling, component/energy telescoping below `1e-12`/`1e-10`, negative duty, positive physical states, finite live properties, physical liquid heights, and no safeguard or fallback at both `h=1e-5` and `h/2`. Because DWSIM imposed-phase fugacity and TP-flash endpoints have a documented numerical consistency floor, the direct bubble residual is primary and shall close below `1e-10`; the independent TP check shall use precommitted near-boundary tolerances and shall reject stable vapor. Passing authorizes only drafting and precommitting one bounded `40 x 40` root campaign.
- `FR-019ae` The solved-duty saturated-liquid root decision shall use exactly one precommitted three-start bounded trust-region campaign on the unchanged DD-087 `40 x 40` residual. Starts shall be the exact DD-087 canonical and deterministic vectors plus one independently interpolated phase-stable seed with its own local bubble solve and energy-reconstructed duty. The campaign shall use fixed DD-087 residual scales, signed-affine duty bounds corresponding to `-3.0` through `-0.1` times `abs(Q_C,ref)`, the declared physical bounds, uncolored central differences, and one solver/settings set. Acceptance requires all starts to terminate at the same physical root below `1e-7` difference, scaled residual below `1e-8`, full and local-bubble rank at both endpoint steps, condition `<1e8`, no active bound or safeguard, exact conservation, negative duty, drum temperature below its supplying stage, strict direct bubble closure, and every precommitted TP consistency-floor criterion. Any failed subgate retires the five-volume solved-duty saturated-liquid architecture without solver/tolerance/bound/duty/topology variation or dynamics, even if a common algebraic root is otherwise obtained.
- `FR-019af` A prospective DWSIM PR phase-consistency contract shall distinguish overall composition `z` from TP-flash phase compositions `x_flash/y_flash`. Flash-derived `K=y_flash/x_flash` shall be checked against `x_flash`, not substituted into `K*z` when the flash reports nonzero phase fraction. Direct imposed-phase fugacity equilibrium shall be the primary bubble/dew authority; TP flash shall independently check phase classification, Rachford-Rice/lever-rule closure, and internally returned phase compositions. Any cross-interface composition tolerance shall be established prospectively through a standalone, fresh-process, order-varied provider study and parameter-aligned independent calculation. Such evidence shall not retroactively alter a frozen architecture decision.
- `FR-019ag` The prospective PR provider-authority table shall assign each thermodynamic quantity exactly one declared authority, expected composition basis, permitted uses, forbidden uses, tolerance where applicable, and explicit fallback policy. Direct DWSIM imposed-phase fugacity shall own production equilibrium with residual infinity norm `<1e-10`. Parameter-aligned independent PR shall be validation-only with `|delta T_bubble|<1e-3 F` and `max|delta y|<1e-6`. DWSIM TP flash shall own phase classification, phase fraction, returned phase compositions, `K_flash=y_flash/x_flash`, and lever-rule closure; `K*x_flash` and lever-rule errors shall be `<1e-12`, and a direct-bubble phase-region check may use `beta<=1e-3` while rejecting stable vapor. Mixed-basis `K_flash*z` equilibrium gating, unvalidated direct-y/flash-y equality, and interface fallback are prohibited. Passing authorizes only a separately versioned successor-architecture decision, not a solve or dynamics.
- `FR-019ah` A successor provider-governed equilibrium architecture shall have a distinct namespace and version identity and shall embed provider authority in its equation registry. For a three-component five-volume ledger it shall register exactly `40` unknowns and `40` residuals, have structural rank `40`, nullity zero, no empty row or unknown column, exact component and energy telescoping, solved condenser duty, four energy-owned internal vapor links, and three Francis-owned liquid flows. Governing stage and condenser equilibrium rows shall use direct imposed-phase fugacity only; TP flash shall remain diagnostic, independent PR shall remain validation-only, `K_flash*z` shall be prohibited, and interface fallback or imported historical acceptance shall fail the structural gate. A structural pass authorizes only one precommitted live residual/provider/conservation/Jacobian audit, not a root solve or dynamics.
- `FR-019ai` Before a Core V3 root solve, one frozen live numerical audit shall evaluate exactly two complete precommitted `40`-coordinate states with live DWSIM PR and a separately implemented Core V3 residual. Governing equilibrium shall use direct imposed-phase fugacity, energy shall use declared phase enthalpy, and Francis hydraulics shall use declared liquid density. TP flash shall be diagnostic-only after residual/Jacobian evaluation, and parameter-aligned independent PR shall be validation-only. At uncolored central-difference steps `h=1e-5` and `h/2=5e-6`, both states shall retain rank `40/40`, local bubble rank `3/3`, condition `<1e8`, no zero or off-registry coupling, component/energy telescoping below `1e-12`/`1e-10`, direct bubble residual `<1e-10`, negative condenser duty, positive physical states and flows, and no clipping, projection, fallback, mixed-basis `K_flash*z` gate, or cross-interface vapor-composition equality gate. Diagnostic TP flash shall reject stable vapor, satisfy `beta<=1e-3`, and close `K*x_flash` identity and the lever rule below `1e-12`; independent PR shall agree within `1e-3 F` and `1e-6` vapor composition. Passing authorizes only drafting and precommitting one bounded three-start Core V3 steady-root campaign.
- `FR-019aj` The Core V3 steady-root decision shall use exactly one precommitted three-start `scipy.optimize.least_squares(method="trf")` campaign on the unchanged DD-092 `40 x 40` residual. It shall use the exact DD-092 canonical and deterministic vectors plus one fully distinct smooth five-volume seed with its own drum composition, direct-fugacity bubble reconstruction, and energy-reconstructed negative condenser duty. Physical bounds shall be converted once to transformed coordinates; solver tolerances shall be `1e-12`, `max_nfev=500`, `x_scale=1`, solve Jacobian step `1e-5`, and endpoint steps `1e-5` and `5e-6`. Every start shall reach the same physical root below `1e-7`, with scaled residual below `1e-8`, governing fugacity below `1e-10`, full/local ranks `40/40` and `3/3`, condition `<1e8`, component/energy conservation below `1e-12`/`1e-10`, negative duty, drum temperature below the supplying stage, positive finite physical states, liquid heights below tray spacing, no coordinate within `1e-6` of a bound, and all DD-090 provider-authority checks. Any failed start or gate retires the campaign without solver, tolerance, bound, duty, pressure, provider, topology, or dynamic variation. Passing authorizes only a structural dynamic-DAE contract, not integration.
- `FR-019ak` A reporting-only successor to a frozen campaign may be authorized only by an explicit governance exception when the prior execution produced no serialized endpoint or scientific decision and failed solely after solver/audit completion in report assembly. The successor shall preserve exact starts, equations, coordinates, bounds, scales, solver settings, tolerances, provider rules, and acceptance gates under a published mathematical-contract checksum. Permitted changes are limited to the identified report-assembly defect, result-schema versioning, and direct plus end-to-end regression tests for that path. It shall be committed before one execution. Any subsequent failure retires the successor without another repair or rerun; a pass authorizes only structural dynamic-DAE specification work.
- `FR-019al` The first Core V3 dynamic contract shall retain prescribed pressure, negligible resident vapor holdup, full stage saturation, energy-owned vapor links and condenser duty, Francis-only liquid flow, and the DD-094 reduced feasibility root. Because fixed-pressure full fugacity determines temperature from component inventory, independent internal energy shall not be added as a second state coordinate in this layer. The contract shall use `N[j,k]` as the `J*C` independent coordinates and evaluate `dU[j]/dt` through a provider-consistent chain rule in each energy balance. With DD-094 `D/B` fixed and terminal-amount constraints removed, the three-component implicit solve shall contain `15` inventory derivatives, `23` algebraic variables, and `38` equations; it shall have structural rank `38`, no zero or off-registry dependency, no controller/profile/cap/relaxation/fallback, and exact global component and energy forms. Structural passage authorizes only a precommitted live leading-Jacobian, provider-chain-rule, conservation, and consistent-derivative audit. The DD-094 root is reduced-model feasibility evidence only; production acceptance requires separate terminal temperature, composition, and rate gates.
- `FR-019am` Before Core V3 dynamic integration, one precommitted live numerical audit shall evaluate the exact DD-094 inventory and algebraic root at zero derivative using the DD-095 `38 x 38` implicit ledger. Saturated-liquid stored internal energy shall be derived from direct-fugacity bubble reconstruction, declared DWSIM liquid enthalpy, and declared DWSIM liquid density; its physical-inventory central-difference gradient at relative steps `1e-5` and `5e-6` shall be finite, agree within `1e-3`, and retain bubble residual below `1e-10`. Uncolored leading Jacobians with respect to 15 scaled inventory derivatives and 23 algebraic coordinates at steps `1e-5` and `5e-6` shall each retain rank `38/38`, condition `<1e8`, no zero or off-registry coupling above `1e-7`, and singular-spectrum change below `0.25`. The zero-derivative scaled residual shall be `<1e-8`, component/energy telescoping shall be below `1e-12`/`1e-10`, and provider ownership shall pass without fallback. The audit may execute once from its committed contract. Passing authorizes only a separately frozen numerical implicit-solver contract; failure stops this fixed-pressure saturated-liquid dynamic path without numerical tuning. Integration remains unauthorized.
- `FR-019an` The first Core V3 implicit-step decision shall execute once from a precommitted contract at the exact DD-094 root with unchanged open-loop inputs. It shall use backward Euler on 15 component inventories and solve them simultaneously with the 23 DD-095 algebraic coordinates. Endpoint inventories shall use a positive exponential rate map without clipping or caps; physical rates shall be recomputed from exact endpoint differences. Energy rows shall use exact provider-derived saturated-liquid storage differences, with all direct-fugacity bubble, declared liquid-enthalpy, and declared liquid-density calls recorded as governing. One unbounded `least_squares(method="trf")` solver shall use a central Jacobian step of `1e-5`, `ftol=xtol=gtol=1e-12`, `max_nfev=40`, and `x_scale=1`. One zero-rate algebraic recovery plus independent `1.0 s` and `0.5 s` steps from the same root shall pass the frozen residual, rank, condition, stationary-motion, refinement, physicality, discrete-conservation, and provider gates in `docs/dd_097_core_v3_implicit_step_contract_20260725.md`. Passing authorizes only a separately frozen short open-loop trajectory contract; failure stops the implicit-step path without solver or step tuning.
- `FR-019ao` The first Core V3 nonzero open-loop trajectory decision shall execute once from a precommitted DD-098 contract. It shall run an unchanged-input `2.0 s` root hold at `dt=1.0 s` and two independent `2.0 s` feed-step trajectories at `dt=1.0 s` and `0.5 s`, all starting from the exact DD-094 root. The sole forcing shall multiply every feed component rate and total feed enthalpy by `1.001`, preserving feed composition and specific enthalpy while retaining fixed pressure, duties, reflux, product rates, geometry, and DD-097 solver settings. Every step shall pass the frozen solve, rank `38/38`, condition, residual, storage-bubble, physicality, discrete-conservation, and provider gates without retry or substepping. The root hold shall remain stationary. Each perturbed trajectory shall show strictly increasing total inventory and match the analytically expected global accumulation within `1e-6` relative. The refined endpoints shall satisfy the inventory, algebraic, temperature, and accumulation limits in `docs/dd_098_core_v3_short_open_loop_contract_20260725.md`. Passing authorizes only one separately frozen longer open-loop contract; failure stops the short-trajectory path without tuning.
- `FR-019ap` Before extending the DD-098 trajectory, one precommitted performance-correction audit shall remove nested saturated-state reconstruction from the backward-Euler residual and calculate liquid internal-energy storage from the governing trial-state enthalpy and density packet. Governing density shall be evaluated for all five volumes. An optional colored central Jacobian shall use the registered direct solve dependencies plus the backward-Euler chain from trial rates through endpoint inventories to state-dependent rows. The frozen three-component pattern shall have 38 columns, 17 conflict-free colors, and require 34 rather than 76 central-difference residual evaluations. Stationary and `+0.1%` feed cases shall each execute once with uncolored and colored Jacobians from the same DD-094 root. Both methods shall pass residual `<1e-8`, rank `38/38`, condition `<1e8`, equilibrium `<1e-10`, physicality, discrete conservation, and provider ownership without nested bubble calls. Their endpoints and Jacobians shall satisfy the DD-099 equivalence limits. Colored calls shall be lower in both cases, below `10,000` per solve, and more than `5x` lower than DD-098's mean calls per endpoint. Passing authorizes only one separately frozen modest longer open-loop contract.
- `FR-019aq` The first post-performance-correction trajectory shall execute once from a precommitted DD-100 contract using governing trial-state storage and the validated 17-color Jacobian. It shall run a `5 s` unchanged-input hold at `dt=1 s` plus independent `10 s`, `+0.1%` feed-throughput trajectories at `dt=1 s` and `0.5 s`, for exactly 35 endpoints. Every endpoint shall pass residual `<1e-8`, rank `38/38`, condition `<1e8`, equilibrium `<1e-10`, physicality, discrete conservation, and provider ownership without retry, substepping, fallback, or nested bubble reconstruction. The root shall remain stationary. Both disturbed runs shall accumulate inventory monotonically and match the exact external balance within `1e-6` relative; refined endpoints shall satisfy the DD-100 inventory, algebraic, temperature, and accumulation limits. The campaign shall average fewer than `6,000` provider calls per endpoint and finish within `180 s`. Passing authorizes only the next separately frozen dynamic-scope decision, not production integration, pressure dynamics, vapor holdup, or controllers.
- `FR-019ar` The first Core V3 pressure scope increase shall add simultaneous algebraic pressure closure without adding pressure differential states or a second vapor-flow owner. Reflux-drum pressure shall remain the sole fixed pressure anchor; the four lower-volume pressures shall become algebraic unknowns; and each of the four existing energy-owned vapor links shall receive one uncapped dry-tray-plus-liquid-head pressure-drop equation. The pressure relation shall use current inventory/composition, geometry, molecular weight, declared liquid density, and direct declared vapor compressibility factor. It shall contain no conductance flow equation, profile forcing, previous-step pressure, cap, relaxation, controller, fallback, or explicit vapor inventory. For three components, the structural ledger shall be `42 x 42`, rank `42`, nullity zero, with no empty row/column or unregistered dependency and inherited exact component/energy conservation. A structural pass authorizes only one separately frozen live residual and Jacobian audit at the accepted root and a bounded pressure perturbation.
- `FR-019as` The first live Core V3 algebraic-pressure audit shall execute once from a precommitted DD-102 contract. It shall evaluate the exact accepted root pressure profile and one fixed ordered lower-pressure perturbation, use direct declared DWSIM vapor compressibility and liquid density without fallback, and form two full central-difference `42 x 42` Jacobians per state at fixed steps `1e-5` and `5e-6`. Every matrix shall retain rank `42`, condition below `1e8`, stable singular values, and exact registered coupling; both states shall retain positive ordered pressures, positive hydraulic terms, and roundoff component/energy conservation. The accepted-root base residual shall remain below `1e-8`; pressure residual magnitude is diagnostic rather than a tuned acceptance target. The campaign shall use fewer than `10,000` audited provider calls, finish within `180 s`, and attempt no nonlinear solve or integration. A pass authorizes only one separately frozen pressure-layer steady-root contract. That contract shall explicitly affirm or replace the reduced bottom-link geometry ownership before execution.
- `FR-019at` The first Core V3 pressure-layer steady-root decision shall execute once from a precommitted contract with the combined reboiler/sump return modeled as a dry-resistance-only terminal link and the three physical tray links retaining dry plus liquid-head closure. It shall fix all 15 conserved inventory rates at zero and solve the 27 algebraic coordinates against all 42 equations from exactly two predefined starts using one bounded trust-region solver and one colored central-difference Jacobian. Acceptance requires both starts to reach one interior positive pressure-ordered root with scaled residual `<1e-8`, common-root difference `<1e-7`, algebraic column rank `27`, condition `<1e8`, stable endpoint spectra, pressure closure, exact conservation, and declared provider ownership within the frozen efficiency limits. A common nonzero floor retires algebraic-only pressure repair without another seed, solver, tolerance, scaling, geometry, or liquid-head variant. Subsequent work may define only a property-free pressure-enabled implicit-DAE structural contract in which conserved inventories and rates participate simultaneously; numerical stepping and integration remain unauthorized until that contract passes.
- `FR-019au` The first pressure-enabled Core V3 implicit-DAE contract shall retain the 15 component inventories as differential coordinates, restore their 15 rates as simultaneous implicit unknowns, and combine them with 27 algebraic coordinates in the existing 42-equation pressure ledger. Reflux-drum pressure shall remain the sole fixed anchor; four lower pressures shall remain algebraic; no pressure derivative or resident vapor inventory shall be introduced. The combined reboiler/sump return shall be dry-resistance-only, while the three physical tray links retain dry resistance plus liquid head. Backward-Euler structural dependencies shall include every rate-to-endpoint-inventory path and provider-derived endpoint energy storage at current pressure. For three components, the ledger shall be square with structural rank `42`, nullity zero, no empty or unregistered row/column, conflict-free deterministic coloring, exact inherited component/energy conservation, fixed open-loop DD-094 product rates, and no controller, profile, cap, relaxation, fallback, property call, nonlinear solve, mass-matrix evaluation, or integration. Passing authorizes only one separately frozen live pressure-enabled leading-Jacobian and consistent-rate audit at the DD-094 inventory state using the DD-103 pressure seed.
- `FR-019av` Because moving algebraic pressure changes stored internal energy, the first live pressure-enabled numerical decision shall use exact backward-Euler endpoint storage `U_next-U_previous`; it shall not reuse the fixed-pressure storage gradient as a continuous pressure-enabled mass matrix. Exactly two independent first steps, `1.0 s` and `0.5 s`, shall start from the same DD-094 state and use the DD-103 endpoint only as an algebraic guess. Both shall use one frozen 20-color trust-region solver and endpoint Jacobians at `1e-5` and `5e-6`, without retry or substepping. Acceptance requires residual `<1e-8`, rank `42/42`, condition `<1e8`, stable spectra, positive ordered pressure, exact conservation, provider compliance, and the frozen inventory/algebraic/pressure refinement limits. A refinement failure shall stop pressure-enabled stepping and prohibit timestep sweeps, tolerance changes, substeps, or a trajectory. Any successor consistent initializer must permit conserved-state movement under explicit whole-column component/energy conservation and terminal-inventory ownership without reviving checkpoint repair or manual staged continuation.
- `FR-019aw` The first pressure-consistent Core V3 initializer shall be specified without a timestep as one equality-constrained minimum-rate/minimum-movement problem over 15 positive component inventories, 15 continuous inventory rates, and 27 pressure-enabled algebraic variables. All 42 DD-104 DAE rows shall remain exact constraints. Three whole-column component totals and one live provider-derived whole-column stored-energy total shall equal DD-094, and reflux-drum plus combined-reboiler/sump total molar inventories shall equal their DD-094 values while terminal compositions remain free. For three components, the resulting 48 equality rows shall have structural rank 48 over 57 primal variables, leaving nine selection degrees of freedom; the declared diagonal normalized objective and equality ledger shall form a full-rank `105 x 105` structural KKT candidate. The selection objective may minimize scaled inventory rate, conserved-state redistribution, and algebraic movement but shall not relax any DAE, conservation, pressure, hydraulic, phase, or terminal-ownership equation. Structural passage authorizes only one separately frozen live numerical initializer contract with precommitted objective weights, solver, derivatives, physical/movement/rate/provider/efficiency gates, and a no-retry hard stop. It does not authorize an initializer execution, timestep, trajectory, controller, or integration.
- `FR-019ax` Before freezing the DD-106 live numerical contract, a property-free readiness audit shall verify that every potentially nonzero inventory rate has an exact pressure-aware continuous energy-storage derivative. Exact backward-Euler storage differences shall not be used without a declared timestep, and the DD-096 fixed-pressure reduced storage gradient shall not be reused after lower pressure becomes algebraic. If the initializer has no independent `U[j]`/`dU[j]/dt` ownership, no pressure-aware reduced derivative, and does not constrain all inventory rates to zero, live initializer execution shall stop before solver implementation, property calls, or objective-weight selection. Such a stop authorizes only one property-free conserved-`N/U` plus algebraic-pressure DAE ownership audit with five constitutive storage equations; it does not authorize checkpoint repair, hidden timestepping, numerical tuning, or dynamics.
- `FR-019ay` The first conserved-energy algebraic-pressure successor shall assign independent `U[j]` states, `dU[j]/dt` rates, and provider-owned liquid-storage constitutive equations only to volumes whose pressure is algebraic. The fixed-pressure reflux drum shall retain its exact saturation-manifold reduced storage derivative and shall not receive an independent `U` state. For three components the contract shall contain 15 component inventories, four lower internal energies, 19 corresponding rates, 27 algebraic variables, and 46 equations. It shall have structural rank `46`, nullity zero, no empty or unregistered row/column, four live-pressure storage closures, four pressure-drop rows, no pressure rate or vapor inventory, exact component/energy ownership, one energy-owned vapor-flow owner, and no controller, profile, cap, relaxation, previous-step dependency, property call, nonlinear solve, or integration. An all-five-volume `U` assembly that duplicates fixed-pressure top saturation shall be rejected rather than repaired with an anchor. A structural pass authorizes only one separately frozen live leading-Jacobian and constraint-manifold numerical contract, not an initializer execution or dynamics.
- `FR-019az` Before any conserved-`N/U` pressure initializer or timestep, one precommitted live numerical audit shall evaluate the unchanged DD-108 `46 x 46` leading residual at exactly two frozen states: the DD-094 conserved state with its original algebraic pressure profile and the same conserved state with the DD-103 pressure endpoint guess. The audit shall use direct DWSIM PR ownership, four live lower-volume internal-energy closures, the exact fixed-pressure top storage gradient, fixed residual/storage/rate scales, graph-colored central differences at `h=1e-5` and `h/2=5e-6`, and one full central-difference canonical cross-check. Every leading matrix shall retain rank `46/46`, the four lower storage rows shall have rank `4/4`, condition shall remain `<1e8`, singular-spectrum change shall remain below `0.25`, colored/full matrix difference shall remain below `1e-8`, and no zero or off-registry coupling may occur above `1e-7`. Canonical lower storage closure shall be below `1e-10` scaled; component/energy telescoping shall remain below `1e-12`/`1e-10`; pressures and storage shall remain physical; all property calls shall obey the DD-090 provider contract without fallback. The audit may execute once only after its exact states, hashes, gates, `25000`-call ceiling, and `180 s` wall-clock ceiling are committed. It shall perform no nonlinear solve, initializer, root repair, or timestep. Passing authorizes only a separately frozen conserved-state consistent-initialization contract; failure stops that path without tuning.
- `FR-019ba` A static reporting-gate successor to DD-109 may be authorized only by an explicit governance exception because DD-109 completed and serialized all numerical evidence and failed solely when a tray-height gate was applied to non-Francis terminal volumes. The successor shall checksum the immutable DD-109 contract and result, preserve every numerical value and every gate except `finite_physical_state` and `positive_pressure_and_geometry_terms`, and replace only those two checks with ownership-aware tests. It shall require finite positive heights for the three Francis hydraulic volumes, intentional `NaN` height sentinels for the reflux drum and combined reboiler/sump, positive liquid-head terms only on links that own liquid head, and zero liquid-head drop on the dry-only terminal link. Any unrelated failed source gate, changed source hash, invalid applicable height, missing terminal sentinel, changed inherited gate, or property/residual/Jacobian/solve/initializer/timestep attempt shall fail the successor. The static adjudication may execute once only after commit. Passing may authorize drafting one separately frozen conserved-state consistent-initialization contract; it does not authorize an initializer execution or dynamics.
- `FR-019bb` The conserved-`N/U` pressure-consistent initializer shall be an equality-constrained selection problem over 15 positive component inventories, four independent lower internal energies, 15 component rates, four lower energy rates, and 27 algebraic variables. It shall retain all 46 DD-108 DAE rows exactly, including four live pressure-dependent lower storage closures, and add three whole-column component totals, one whole-column stored-energy total, and two terminal total-inventory constraints. Whole-column energy shall equal provider-derived top storage on the fixed-pressure saturation manifold plus the four independent lower `U` states; no independent top `U/dU`, fixed-pressure lower gradient, or timestep-dependent storage definition is permitted. For three components the equality ledger shall have 52 rows of structural rank 52 over 65 primals, leaving 13 selection degrees of freedom; a positive diagonal normalized selection objective shall cover every primal and produce a full-rank `117 x 117` structural KKT candidate. Terminal constraints shall preserve total holdup without locking component splits. The structural contract shall be generic in component count and contain no property evaluation, nonlinear solve, controller, continuation, clipping, repair, or integration. Passing authorizes only one separately frozen live constrained-initializer contract, not execution.
- `FR-019bc` The first live conserved-`N/U` pressure initializer shall execute exactly one precommitted two-start `scipy.optimize.minimize(method="SLSQP")` equality-constrained campaign. The 65 numerical coordinates shall use log ratios for positive component inventories, bounded affine lower-energy coordinates scaled by DD-096 storage magnitude, and unchanged DD-109 rate plus DD-103 algebraic coordinates. Exact targets shall be the DD-094 whole-column component totals, DD-096 whole-column stored energy, and DD-094 reflux-drum/sump total inventories. The objective shall use fixed normalized weights `1/10/1` for conserved-state movement, conserved rates, and algebraic movement. Starts shall be the DD-094 storage/profile state and the DD-103 pressure endpoint with DD-109 live lower storage. Both shall be strictly interior and use one SLSQP configuration with `ftol=1e-10`, `maxiter=80`, analytic objective gradient, and a 21-color central-difference constraint Jacobian at `1e-5`. Acceptance requires both starts to succeed, all 52 constraints `<1e-8`, common normalized endpoint difference `<1e-6`, constraint rank `52/52`, condition `<1e8`, two-step spectrum change `<0.25`, colored/full difference `<1e-8`, no zero/off-registry coupling, KKT stationarity `<1e-5`, no active bound within `1e-6`, ordered positive pressure, physical inventories/flows, component/energy conservation below `1e-12`/`1e-10`, accepted provider provenance, fewer than `150000` calls, and wall time below `300 s`. Any failure retires this campaign without retry, alternate solver, weight/bound/tolerance change, continuation, timestep, or dynamics. Passing authorizes only a separately frozen zero-time initializer audit.
- `FR-019bd` A static physical-equivalence adjudication of DD-112 may occur only under the user's explicit governance authorization and shall not reclassify or rerun DD-112. It shall checksum the immutable DD-112 contract and result, require `common_solution` to be the only failed source gate, preserve every other gate unchanged and true, and compare the two saved endpoints in physical inventories, liquid/vapor/bubble compositions, lower stored energy, component/energy rates, pressure, temperature, hydraulic/vapor/product flows, condenser duty, and objective. Tolerances and physical scales shall be frozen before execution; reconstructed compositions shall remain strictly positive and normalized. The canonical endpoint shall be selected deterministically by lowest objective and then lexical start name. Any source-hash change, unrelated failed gate, physical-equivalence failure, invalid composition, property/residual/Jacobian/solve/initializer/timestep/dynamic attempt, or post-result tolerance change shall fail the adjudication. A static pass may authorize only one separately frozen zero-time audit of the canonical saved endpoint; it does not authorize a timestep or dynamics.
- `FR-019be` The first live zero-time audit of the canonical DD-113 endpoint shall reconstruct its exact saved 65-coordinate vector and evaluate all 52 DD-111 DAE/conservation/terminal constraints without a nonlinear solve or timestep. It shall use the unchanged direct DWSIM PR provider, one endpoint residual, graph-colored central-difference constraint Jacobians at `1e-5` and `5e-6`, and one full central-difference cross-check at `1e-5`. Acceptance requires constraint infinity norm `<1e-8`, rank `52/52` for every Jacobian, condition `<1e8`, spectrum change `<0.25`, colored/full difference `<1e-8`, no zero or off-registry coupling, exact pressure ordering and physical compositions/inventories/flows, component/energy conservation below `1e-12`/`1e-10`, and fresh reproduction of saved physical inventory, energy, rate, pressure, temperature, flow, product, and duty fields within precommitted engineering tolerances. Provider ownership, fewer than `50000` calls, and wall time below `180 s` are mandatory. The audit may execute once only after its source hashes, canonical coordinate hash, implementation hashes, gates, and hard stops are committed. Failure stops pressure-enabled initializer handoff without tuning. Passing authorizes only one separately frozen first-step refinement contract, not a timestep campaign or dynamics.
- `FR-019bf` The first dynamic handoff from the accepted DD-114 endpoint shall compare one `1.0 s` conserved-`N/U` backward-Euler step with two sequential `0.5 s` steps to the same `t=1.0 s` endpoint. Each `46 x 46` step shall advance all 15 positive component inventories and four independent lower internal energies, use exact top saturation-manifold storage difference, retain all 46 live DAE/storage/pressure rows, and use one precommitted colored trust-region solver without retry. The accepted DD-114 component and energy rates shall be the initial predictor. Every solve shall close below `1e-8`; endpoint Jacobians shall retain rank `46/46`, condition `<1e8`, stable two-step spectrum, and no zero or off-registry coupling. Coarse/refined agreement shall remain below `1e-4` for scaled inventories and stored energies, `1e-3` for algebraic coordinates and normalized flows, `0.01 psia`, and `0.01 F`; first-step rates shall remain within `1e-3` of the accepted zero-time rates on declared scales. Positive states/flows, ordered pressure, exact discrete component/energy kinematics, conservation, provider ownership, fewer than `100000` calls, and wall time below `180 s` are mandatory. The campaign may execute once only after source, state, rate, coordinate, implementation, solver, grid, and gate hashes are committed. Failure stops dynamic handoff without timestep or solver tuning. Passing authorizes only one separately frozen short open-loop contract, not controllers or production dynamics.
- `FR-019bg` After the frozen DD-115 handoff failure, one term-level diagnostic may evaluate exactly the immutable DD-114 zero-time state and DD-115 refined `t=0.5 s` and `t=1.0 s` states. It shall expand every component and energy balance into signed physical inflow, outflow, feed, product, and duty terms without modifying the governing residual. Each term sum shall reproduce its saved component or energy rate within `1e-10` on the declared rate scale; all saved pressure, temperature, flow, product, and duty fields shall reproduce within precommitted tolerances; and ownership signatures shall remain identical across snapshots. Direct DWSIM PR provider ownership, fewer than `5000` calls, and wall time below `30 s` are mandatory. Exactly three live residual/property evaluations are permitted after source, snapshot, implementation, and gate hashes are committed. No nonlinear solve, Jacobian, timestep, controller, initializer, or trajectory is permitted. A pass may classify DD-115 as an explained non-steady transient and authorize only a property-free structural feasibility audit for a zero-rate or slow-start initializer. It shall not relax or rerun DD-115 or authorize dynamics.
- `FR-019bh` A static reporting-gate adjudication of DD-116 may occur only under explicit user authorization and shall not modify or rerun DD-116. It shall checksum the immutable DD-115/DD-116 contracts and results, require `physical_reproduction` to be the only failed DD-116 gate, and preserve every other gate unchanged and true. The replacement shall exclude the nominal exponential-step rate coordinate from physical-field reproduction, require every actual pressure, temperature, flow, product, and duty metric to retain its frozen limit, and independently reconstruct both refined endpoint inventories, effective finite-step rates, and the reported nominal/effective coordinate mismatch from DD-115's exponential update within `1e-12`. Any unrelated failed source gate, changed source hash, failed physical metric, failed transform reconstruction, or property/residual/Jacobian/solve/initializer/timestep/controller/trajectory attempt shall fail the adjudication. A static pass may authorize only one property-free structural zero-rate feasibility audit; it does not authorize an optimizer or dynamics.
- `FR-019bi` The first zero-rate successor study shall be property-free and shall compare three structures generically in component count: the conserved-`N/U` pressure DAE with all rate variables fixed to zero, that zero-rate core plus every inherited DD-112 global/terminal target, and the original initializer augmented by explicit zero-rate rows. The audit shall report dimensions, structural ranks, equation surplus, maximum matching, and unmatched target owners for both three- and two-component cases. It shall require the zero-rate DAE core to be square and full structural rank, identify exactly `Nc+3` surplus inherited targets when all rates are zero, distinguish the `Nc+1` global component/energy targets from the two terminal total-holdup scale selections, and perform no property, residual, Jacobian, nonlinear solve, initializer, timestep, controller, or trajectory call. A pass may authorize only one frozen live numerical readiness contract that retains terminal holdups provisionally, treats inherited global component/energy totals as diagnostics rather than exact constraints, and audits rank and compatibility before any steady-root solve.
- `FR-019bj` The first live zero-rate readiness audit shall remove all 19 rate coordinates from the unknown vector, fix those rates exactly to zero, and evaluate the unchanged Core V3 residual at the frozen DD-112 canonical state and independently advanced DD-115 refined one-second state. Its 46 coordinates shall contain the 19 conserved-state and 27 algebraic coordinates; its 48 rows shall contain the unchanged 46 DAE/storage/pressure equations plus two terminal total-holdup scale selections. The three global component totals and one global stored-energy total shall be reported only as diagnostics. Graph-colored central-difference Jacobians at `1e-5` and `5e-6` shall be evaluated at both states, with one full canonical cross-check. Acceptance requires DAE-only rank at least 44 and nullity at most two, terminal-augmented rank `46/46`, rank gain equal to the DAE nullity, augmented condition `<1e8`, spectrum change `<0.25`, colored/full difference `<1e-8`, no zero or off-registry coupling, ordered pressure, physical states and flows, machine-precision conservation, direct DWSIM PR ownership, fewer than `20000` provider calls, and wall time below `180 s`. The audit shall perform no nonlinear solve, timestep, controller, retry, fallback, or dynamics. Passing authorizes only one separately frozen zero-rate root contract.
- `FR-019bk` The first zero-rate root campaign shall solve the unchanged DD-119 `48 x 46` terminal-scaled residual from exactly the DD-112 canonical and DD-115 refined one-second starts. It shall use one bounded `scipy.optimize.least_squares(method="trf")` configuration with `ftol=xtol=gtol=1e-12`, `max_nfev=80`, `x_scale=1`, and the unchanged 20-color central-difference Jacobian at `1e-5`. Acceptance requires both starts to succeed, every DAE and terminal row `<1e-8`, endpoint coordinate agreement `<1e-6`, optimality `<1e-8`, endpoint rank `46/46`, condition `<1e8`, two-step spectrum change `<0.25`, canonical colored/full difference `<1e-8`, no zero or off-registry coupling, interior bounds, ordered pressure, physical states and flows, machine-precision conservation, accepted direct-DWSIM provenance, fewer than `100000` calls, and wall time below `180 s`. Released global component and energy totals shall remain diagnostics. Any failure retires the terminal-scaled zero-rate root path without retry, alternate solver, continuation, changed target, changed tolerance, timestep, controller, or dynamics. Passing authorizes only a separately frozen zero-rate dynamic-handoff contract.
- `FR-019bl` Before releasing product rates in a controlled-terminal zero-rate successor, one frozen gauge-invariance audit shall evaluate the same saved DD-120 endpoint twice and exactly four homogeneous terminal perturbations: reflux-drum component inventories at `+/-1%`, and combined-reboiler/sump component inventories plus internal energy at `+/-1%`. Every algebraic coordinate, terminal composition, and bottom specific internal energy shall remain fixed. The 46 DAE residuals shall change by no more than `max(1e-10, 10 * provider repeatability)` while only the applicable terminal total-inventory row changes by the imposed fraction. The audit shall use direct DWSIM PR, perform no Jacobian, nonlinear solve, timestep, controller action, or dynamics, and execute once after commit. Passing establishes terminal amounts as gauge selections and authorizes drafting one square `48 x 48` zero-rate contract that retains both terminal inventory targets and releases positive distillate and bottoms rates as level-controller outputs. Failure requires a hidden terminal-owner audit and prohibits that solve.
- `FR-019bm` The first controlled-terminal zero-rate campaign shall retain the unchanged 46 zero-rate DAE rows and both terminal total-inventory targets, and add positive transformed distillate and bottoms rates as the only new unknowns. The resulting three-component system shall be `48 x 48`, structurally full rank, and use one precommitted bounded `least_squares(method="trf")` configuration from exactly two starts: the DD-120 common endpoint and one independently perturbed interior-inventory/lower-energy state with different `D/B` guesses. It shall save full initial and final residual vectors. Acceptance requires both starts to reach the same interior root within `1e-6`, every row `<1e-8`, optimality `<1e-8`, rank `48/48`, condition `<1e8`, stable two-step spectra, exact registered coupling, positive ordered physical state, machine-precision conservation, accepted provider ownership, fewer than `100000` calls, and wall time below `180 s`. Product-rate deviation from the imported reference shall be reported, not gated. Any failure retires this path without retry or tuning. Passing authorizes only a separately frozen zero-rate dynamic-handoff contract in which `D/B` have explicit terminal level-control ownership; it does not authorize forcing imported throughput, a timestep, or dynamics.
- `FR-019bn` The controlled-terminal dynamic successor shall retain the DD-122 conserved component inventories, four lower internal-energy states, all pressure/equilibrium/hydraulic/energy ownership, and the accepted stationary `D/B` outputs. It shall add exactly two terminal PI-memory states, two corresponding memory rates, two positive product-output algebraics, and four controller equations. Drum and sump level shall be calculated from total liquid inventory divided by live DWSIM liquid molar density and converted through explicit vessel geometry; mass-only or tray-height proxies are prohibited. The horizontal reflux drum shall use its cylindrical shell and two hemispherical heads, and the bottom sump shall use its vertical cylindrical geometry. Controller memory shall be initialized from the DD-122 stationary product outputs so that the handoff is bumpless. For three components the resulting implicit ledger shall be `50 x 50`, structurally full rank, preserve exact component and energy conservation, and remain generic in component count. A property-free structural pass authorizes only one separately frozen live zero-time controller-handoff audit that reconstructs physical level setpoints and verifies the numerical leading system before any timestep. It does not authorize a nonlinear campaign, controller tuning, timestep, or dynamic integration.
- `FR-019bo` Before any controlled-terminal timestep, one frozen live zero-time audit shall reconstruct the two physical level setpoints exactly once from the accepted DD-122 terminal inventories, direct DWSIM liquid molar densities, and DD-123 vessel geometry. It shall initialize both PI memories from the accepted stationary product-output log ratios and evaluate the complete `50 x 50` controlled DAE at zero conserved and controller rates without a nonlinear solve. Two graph-colored central-difference leading Jacobians at `1e-5` and `5e-6` and one full canonical cross-check shall retain rank `50/50`, condition `<1e8`, spectrum change `<0.25`, colored/full difference `<1e-8`, and no zero or off-registry coupling above `1e-7`. The complete scaled residual shall be `<1e-8`, controller rows `<1e-10`, repeated evaluation change `<1e-10`, product-output reproduction `<1e-10` relative, and both physical levels shall lie strictly between `0.01` and `0.99` of vessel height. Ordered pressure, positive inventories/densities/flows, exact conservation, direct provider ownership, fewer than `30000` calls, and wall time below `180 s` are mandatory. The audit may execute once only after its exact state, geometry, memories, reconstruction rule, hashes, steps, and gates are committed. Failure stops controlled-terminal handoff without tuning or retry. Passing authorizes only one separately frozen refined first-step contract, not a timestep campaign or trajectory.
- `FR-019bp` Because DD-124 aborted before its first governed residual on a Python keyword mismatch, one separately versioned correction may be frozen under the user's continuing authorization. The successor shall checksum the immutable DD-124 contract and abort record, change only the kernel/context mapping from `numerical` to `pressure_numerical`, add a direct signature regression test, and retain the exact DD-124 state, geometry, controller memories, setpoint rule, residual/Jacobian steps, tolerances, physical gates, provider limits, and hard stops. It shall not reclassify DD-124 or reuse its contract. After a fresh implementation hash and contract commit, it may execute once under all `FR-019bo` gates. Any further implementation error or scientific gate failure stops this handoff without another corrective successor.
- `FR-019bq` Under the user's explicit authorization to reopen the interface-only stop, one development preflight may replace DD-125's unsupported setpoint-reconstruction label with `evaluation_kind="residual"` and evaluate exactly the setpoint reconstruction plus one baseline controlled-terminal residual. It shall not evaluate a Jacobian, solve a nonlinear system, take a timestep, or integrate. Passing requires a finite 50-row vector below `1e-8`, controller rows below `1e-10`, physical geometry-based levels between `0.01` and `0.99`, positive live densities, exact reproduction of the DD-122 product outputs, accepted provider ownership, fewer than `1000` calls, and audited evaluation time below `30 s`. A pass authorizes one separately frozen live `50 x 50` Jacobian contract; a fail stops the path.
- `FR-019br` The first scientific controlled-terminal numerical gate after DD-126 shall freeze its measured drum and sump level setpoints, exact DD-122 conserved state and PI memories, all 50 zero-time coordinates, direct provider configuration, and the unchanged DD-123 controller ledger. It shall evaluate two identical residuals, graph-colored central-difference Jacobians at `1e-5` and `5e-6`, and one full central-difference cross-check without repeating setpoint reconstruction. Acceptance requires residual `<1e-8`, controller rows and repeatability `<1e-10`, exact stationary product reproduction, physical levels, rank `50/50`, condition `<1e8`, singular-spectrum change `<0.25`, colored/full difference `<1e-8`, no zero or off-registry coupling above `1e-7`, ordered pressure, positive inventories/densities/flows, exact conservation, accepted provider provenance, fewer than `30000` calls, and audited wall time below `180 s`. It may execute once only after commit and shall perform no nonlinear solve, timestep, retry, controller tuning, or dynamic integration. Passing authorizes only one separately frozen refined first-step contract.
- `FR-019bs` The first controlled-terminal timestep gate shall be a stationary root-hold only. It shall advance the accepted DD-127 state with one frozen `1.0 s` backward-Euler solve and two consecutive `0.5 s` solves. The 21 differential rates shall own 15 positive component inventories, four lower internal energies, and two PI memories; top energy remains derived at fixed top pressure. Distillate and bottoms shall remain positive bounded algebraic controller outputs evaluated from endpoint geometry-based levels. All three `50 x 50` roots shall close below `1e-8`; two endpoint finite-difference Jacobians shall retain rank `50/50`, condition `<1e8`, spectrum change `<0.25`, and registered coupling. Inventory, energy, controller memory, product, level, and coordinate motion shall remain below the frozen stationary limits; coarse/refined disagreement shall be `<1e-8`; discrete component, energy, and controller kinematics, pressure order, physicality, conservation, provider ownership, fewer than `20000` calls, and wall time below `180 s` are mandatory. The contract shall be committed before one execution. No perturbation, retry, tuning, or trajectory is permitted. Passing authorizes only a separately frozen moving-step contract.
- `FR-019bt` The first controlled-terminal moving gate shall preserve the accepted physical state and raise both geometry-based level setpoints by exactly `0.1%` relative at `t=0`, changing no feed, duty, thermo, controller tuning, or physical equation. One `1.0 s` and two consecutive `0.5 s` backward-Euler roots shall close below `1e-8`. Both product outputs and PI-memory rates shall move downward while both terminal inventories accumulate, with precommitted minimum signal thresholds. The coarse solver's retained endpoint Jacobian and one independent `5e-6` colored Jacobian shall retain rank `50/50`, condition `<1e8`, spectrum change `<0.25`, and registered coupling. Coarse/refined inventory, energy, memory, coordinate, product, and level differences shall remain below their frozen limits; exact discrete kinematics, pressure order, physicality, conservation, provider ownership, fewer than `16000` calls, and wall time below `180 s` are mandatory. The contract shall be committed before one execution. No retry, changed disturbance, tuning, or trajectory is permitted. Passing authorizes only a separately frozen short controlled trajectory contract.
- `FR-019bu` Under explicit user authorization after DD-129's unclassified serialization abort, one separately versioned successor may repeat the exact DD-129 scientific contract. The successor shall checksum the immutable DD-129 contract and abort, prove that every non-governance scientific field is identical, and change runtime behavior only by recursively converting `numpy.bool_` result values to native Python `bool` before standard JSON serialization. A regression test shall serialize a complete representative result containing nested NumPy booleans. The disturbance, state, grids, solver, Jacobians, gates, thresholds, provider limit, and wall limit shall remain unchanged. The successor may execute once only after its contract and implementation are committed. Scientific failure stops the controlled handoff; passing authorizes only the short trajectory already specified by `FR-019bt`.
- `FR-019bv` After DD-130 passes every scientific gate but fails only its provider-call limit, one property-free modified-Newton efficiency design may be audited. It shall retain the square controlled-terminal step pattern and define exactly one graph-colored central-difference Jacobian and one LU factorization per implicit root, reused for at most 12 corrections with fixed residual-only line-search fractions `1, 0.5, 0.25, 0.125`. Out-of-bound trials shall be rejected without evaluation; clipping, projection, Jacobian rebuild, alternate solver, or fallback is prohibited. The audit shall reconstruct DD-130's 24,165-call ledger exactly, preserve generic full-rank `50 x 50` and `40 x 40` structures, pass linear and mildly nonlinear one-Jacobian fixtures, and prove a worst-case live-successor ceiling below 8,000 calls using the measured 28 calls per complete residual. DD-131 shall make zero live provider, column residual, nonlinear column solve, timestep, or trajectory calls. Passing authorizes only a separately frozen live efficiency proof against the saved DD-130 endpoints.
- `FR-019bw` The first live modified-Newton efficiency proof shall reuse the exact DD-130 physical state, `+0.1%` dual-level setpoint disturbance, `1.0 s` and `0.5 + 0.5 s` grids, equations, scales, bounds, and scientific gates. Each of the three roots shall use exactly one `1e-5` 21-color Jacobian and one LU factorization, at most 12 residual-only corrections, and the four fixed DD-131 line-search fractions. No Jacobian rebuild, alternate solver, clipping, projection, or fallback is permitted. Every root shall close below `1e-8`, retain rank `50/50` and condition `<1e8`, and reproduce its corresponding saved DD-130 inventory, energy, controller-memory, level, product, and coordinate endpoint within `1e-7` normalized. Direction, signal, refinement, exact kinematics, pressure order, physicality, conservation, provider ownership, fewer than 8,000 calls, and wall time below 180 seconds are mandatory. The contract shall be committed before one execution. Failure stops the modified-Newton live path without retry; passing authorizes only a separately frozen short controlled trajectory contract.
- `FR-019bx` Under the user's explicit authorization after DD-132's endpoint-reproduction-only failure, one static physical-equivalence adjudication may compare the immutable DD-130 and DD-132 `coarse`, `half1`, and `half2` endpoints. It shall preserve both source classifications and every inherited gate, permitting only DD-130 `calls` and DD-132 `endpoint_reproduction` as source failures. The comparison shall decode actual physical inventories, holdups, liquid/vapor/bubble compositions, component and lower-energy rates, temperatures, liquid and vapor flows, pressure, condenser duty, controller memories and rates, levels, and product flows. Limits shall be frozen before execution, including `<2e-7` relative vapor-flow difference, `<2e-6` scaled component-rate difference, `<5e-5 F` temperature difference, `<2e-6 psia` pressure difference, and the other recorded DD-133 physical limits. All decoded states shall remain physical and stored products shall reproduce their transformed coordinates. The adjudication shall checksum both contracts and results and make zero property, residual, Jacobian, solve, initializer, timestep, or dynamic calls. Passing may authorize only one separately frozen modified-Newton short controlled-trajectory contract; it does not reclassify DD-130 or DD-132 or authorize immediate execution.
- `FR-019by` The first controlled trajectory after DD-133 shall retain the exact DD-132 state, `+0.1%` dual-level setpoint move, physical equations, provider, controller tuning, modified-Newton algorithm, `1e-5` 21-color Jacobian, and bounds. It shall compare a `10 x 1.0 s` path with an independent `20 x 0.5 s` path over exactly 10 simulated seconds. Every one of the 30 roots shall close below `1e-8` with exactly one Jacobian and one factorization, rank `50/50`, and condition `<1e8`; no rebuild, alternate solver, clipping, projection, fallback, retry, disturbance change, or grid change is permitted. The first coarse step shall reproduce the saved DD-132 physical endpoint under frozen limits. The two trajectory endpoints shall meet frozen inventory, energy, memory, coordinate, product, and level refinement limits. At every step products and PI memories shall retain the commanded direction, while both endpoint level errors shall move closer to the raised setpoints. Exact component, energy, and controller kinematics, positive physical states and flows, ordered pressure, conservation, direct provider ownership, fewer than 80,000 calls, and wall time below 180 seconds are mandatory. The implementation and contract shall be committed before execution. Passing may authorize only a separately frozen extension contract; failure stops the modified-Newton controlled-trajectory path without retry.
- `FR-019bz` The DD-134 globalization diagnosis shall be limited to its saved coarse `t=7 s` and refined `t=3 s` failed roots. At each root, one reconstruction shall compare the original root-start Jacobian with one fresh Jacobian at the saved stalled point while preserving the exact residual, scales, bounds, provider, `1e-5` colored finite-difference step, Armijo rule, and fractions `1, 0.5, 0.25, 0.125`. The saved residual and stale-Jacobian condition shall reproduce within fixed tolerances; all four Jacobians shall retain rank `50/50` and condition `<1e8`; provider provenance, fewer than 7,000 calls, and wall time below 180 seconds are mandatory. Every fraction shall be evaluated diagnostically without accepting a state. No nonlinear root completion, state advance, timestep, trajectory, retry, fallback, clipping, projection, changed grid, or alternate solver is permitted. Fresh-Jacobian success below `1e-8` may authorize only a separately frozen adaptive-refresh solver contract; fresh-Jacobian insufficiency stops that path and redirects investigation to the residual/provider floor.
- `FR-019ca` The DD-135 replay-integrity failure shall be resolved by one residual-only audit at the exact saved coarse and refined points. Three fresh Python/DWSIM processes shall use precommitted grouped-forward, grouped-reverse, and interleaved orders; each process shall evaluate each complete 50-row residual exactly three times. Same-process vector spread shall be compared with `1e-12`, cross-process/order spread with `1e-10`, and every scalar norm with the DD-135 reconstruction within `1e-10`. Full row identity, distinct process IDs, exact order and observation counts, provider provenance, fewer than 1,000 aggregate calls, and wall time below 180 seconds are mandatory audit-integrity gates. Scientific nonrepeatability is a valid classification rather than an invalid audit. No Jacobian, nonlinear solve, state advance, timestep, trajectory, retry, fallback, clipping, or projection is permitted. Deterministic replay with a persistent DD-134 norm gap may authorize only a separately frozen in-process failure-capture contract; provider/process dependence stops solver work.
- `FR-019cb` The DD-136-authorized failure-capture implementation shall be a separately versioned solver and shall not modify historical `modified_newton_v1`. It shall retain immutable independent copies of initial/final coordinates and residual vectors, the frozen Jacobian and rank/condition, every iteration's coordinates/residual/correction, and every line-search trial's fraction, state id, coordinates, bounds status, residual vector/norm, Armijo limit/decision, and evaluation coordinates. It shall record final solver-residual versus retained-evaluation-residual and solver-coordinate versus evaluation-coordinate identity metrics. Property-free fixtures shall cover a 50-dimensional full-rank success, complete four-fraction line-search failure, deliberately shared mutable evaluation buffer, bounds rejection without evaluation, and rank failure. Ordinary fixtures shall have exact identity; the alias fixture shall produce both mismatches at least `0.1`; every captured array shall be read-only. No live provider, column residual/Jacobian, column solve, state advance, timestep, or trajectory is permitted. Passing authorizes only a separately frozen live in-process failure-capture diagnostic contract.
- `FR-019cc` The first live use of the DD-137 tracer shall reconstruct only the isolated coarse `t=7 s` and refined `t=3 s` DD-134 roots from their immutable serialized previous states and root-start coordinates. Each root shall use the unchanged DD-134 residual, provider, scales, bounds, moved setpoints, `1e-5` 21-color Jacobian, one frozen factorization, 12-iteration limit, and four Armijo fractions. Every initial/final vector, Jacobian, correction, and line-search trial shall be retained as immutable DD-137 evidence; final solver/evaluation residual and coordinate identity shall be exact. Rank `50/50`, condition `<1e8`, physical positive inventories/flows, ordered pressure, conservation, provider ownership, fewer than 5,000 calls, and wall time below 180 seconds are mandatory audit-integrity gates. Root convergence is a scientific outcome rather than an integrity gate. No fresh Jacobian, alternate solver, retry, fallback, clipping, projection, state acceptance, timestep, or trajectory is permitted. Two converged reconstructions may authorize only a separately frozen captured short-trajectory contract; a captured failure authorizes static adjudication only.
- `FR-019cd` Under explicit continuation after DD-138's coordinate-identity-only failure, one zero-call static adjudication may replace only `solver_evaluation_identity`. It shall preserve the DD-138 contract/result hashes, raw classification, decision, outcomes, and every other gate. For each root it shall reconstruct actual component-rate coordinates from the saved nominal rates, previous positive inventories, component-rate scale, timestep, and the exact exponential endpoint map used by `conserved_nu_implicit_step_v1`. The reconstructed complete evaluator vector shall match the saved evaluator coordinates below `1e-14`; all non-component coordinates and final residual identity shall remain exact. No property, residual, Jacobian, solve, state advance, timestep, trajectory, retry, fallback, clipping, or projection is permitted. Passing accepts DD-138's numerical evidence without changing its formal raw result and may authorize only a frozen Jacobian repeatability audit.
- `FR-019ce` Before further controlled-trajectory solver work, one frozen no-solve audit shall reconstruct complete `50 x 50` colored central-difference Jacobians at the DD-138 coarse and refined root starts. It shall use the exact saved residual contexts, the frozen 21-color rule, steps `1e-5` and `5e-6`, three fresh Python/DWSIM processes, grouped-forward/grouped-reverse/interleaved orders, and two matrices per point and step per process. Same-process and cross-process/order matrix spread, reproduction of DD-138's captured `1e-5` matrices, rank, condition, singular spectra, and `h` versus `h/2` sensitivity shall be reported. A nonlinear solve, correction, state acceptance, timestep, trajectory, retry, fallback, clipping, or projection is prohibited. Passing repeatability may distinguish finite-difference step sensitivity from solver globalization; it cannot itself authorize dynamics.
- `FR-019cf` Following DD-140's process/order-dependent Jacobian result, one zero-DWSIM adjudication shall test the existing `ThermoProviderV1` density and heat-capacity cache semantics with distinct synthetic states in forward and reverse orders. It shall preserve the production provider implementation, use a deterministic fake property function, record cache hits/misses and retained values, compare DD-134's physical finite-difference perturbations with the rounded cache resolution, and require DD-140's largest discrepancy to scale approximately inversely with the difference step. No live property, residual, Jacobian, solve, correction, state advance, timestep, or trajectory is permitted. Passing may authorize only an exact-state property-cache-key correction with regression tests.
- `FR-019cg` The DD-141-authorized cache correction shall replace approximate temperature, pressure, and composition cache keys with exact normalized-state keys for both liquid density and heat capacity. Exact repeated states shall retain cache reuse; any distinct state shall require a distinct entry. No thermodynamic equation, solver setting, finite-difference step, state, or operating specification may change. Solver work shall remain stopped until a separately frozen post-fix Jacobian repeatability audit passes across fresh processes and both frozen difference steps.
- `FR-019ch` One post-DD-142 live proof shall rebuild complete colored Jacobians at both DD-138 root starts and both frozen difference steps in three fresh grouped-forward, grouped-reverse, and interleaved Python/DWSIM processes. One matrix per point/step/process is sufficient because DD-140 already proved bit-for-bit same-process repetition. All 12 matrices shall remain `50 x 50`, 21-color, full rank, below the frozen condition limit, cross-process/order identical within `1e-10` absolute and relative Frobenius, and stable to step halving. Historical cross-process spread shall improve by at least `1e6`. The audit may not solve, correct, accept a state, advance time, retry, or run a trajectory.
- `FR-019ci` After DD-143 passes, one captured post-cache-fix successor may repeat the exact DD-134 10-second scientific contract: the same accepted state, raised level setpoints, bounds, scales, `10 x 1.0 s` and `20 x 0.5 s` grids, one 21-color frozen Jacobian/factorization per root, gates, and limits. The only implementation changes shall be DD-142 exact-state property keys and DD-137 immutable solver capture. Every attempted step shall retain complete initial/final residuals, frozen Jacobian, corrections, and line-search trials with exact residual identity. Rebuild, alternate solver, retry, fallback, clipping, projection, or grid change is prohibited. Passing may authorize only a separately frozen trajectory extension.
- `FR-019cj` After DD-144 passes, one captured extension may change only the duration from `10 s` to `20 s`, producing `20 x 1.0 s` and `40 x 0.5 s` paths from the same initial state and controller move. Solver, Jacobian, bounds, scales, tolerances, step sizes, physical gates, provider-call limit, wall limit, and complete immutable capture shall remain unchanged. Every root must pass, both endpoints must pass the inherited refinement limits, and no rebuild, alternate solver, retry, fallback, clipping, projection, controller change, or post-result adjustment is permitted. Passing may authorize only a separately frozen longer trajectory; failure stops with replay-complete evidence.
- `FR-019ck` After DD-145 passes, one final brute-force captured extension may change only duration from `20 s` to `60 s`, producing `60 x 1.0 s` and `120 x 0.5 s` paths from the same initial state and controller move. The provider-call ceiling may rise proportionally from `80000` to `240000`; the `180 s` wall limit and every scientific setting, bound, scale, tolerance, gate, and immutable-capture requirement shall remain unchanged. No rebuild, alternate solver, retry, fallback, clipping, projection, controller change, or post-result adjustment is permitted. Passing may authorize only a separately frozen trajectory-efficiency design before multi-minute operation; failure stops with replay-complete evidence.
- `FR-019cl` The first DD-146-authorized efficiency design shall benchmark only the exact first coarse-root `50 x 50`, 21-color Jacobian with no solve or state advance. Three fresh spawn-based runs each at `1`, `2`, and `4` process workers shall use one independently initialized live DWSIM provider per worker and a frozen interleaved schedule. All nine matrices shall reproduce the accepted DD-146 matrix within `1e-10` absolute and relative Frobenius, retain rank `50/50`, condition `<1e8`, stable singular spectra, exactly 42 tasks, and exactly 1,176 governing calls. Four-worker median warmed-Jacobian time shall be no more than `60%` of serial median and projected DD-146-equivalent wall time, including adjusted pool startup, shall be below `75 s`. Worker sharing, retry, fallback, solve, correction, state acceptance, timestep, or trajectory is prohibited. Passing may authorize only a separately frozen parallel colored-Jacobian integration contract; failure retains serial execution.
- `FR-019cm` After DD-147 passes, one integration proof shall reconstruct the exact DD-146 first coarse implicit root with the captured modified-Newton solver twice: once with the accepted serial colored Jacobian and once with a persistent four-process DWSIM pool used only for Jacobian perturbations. Both solves shall use the same main-process residual, settings, point, bounds, and line search. Matrices must be exactly equal; captured corrections, trial coordinates/residuals/decisions, final coordinates, and final residuals must agree within `1e-12`; the parallel root must reproduce DD-146 within `1e-10`. All four workers, exact call counts, rank, condition, residual identity, provider ownership, and immutable capture shall pass. Retry, fallback, endpoint acceptance, timestep, or trajectory is prohibited. Passing may authorize one separately frozen parallel captured short-trajectory contract; failure retains serial execution.
- `FR-019cn` After DD-148 passes, one parallel captured short-trajectory proof may repeat the exact DD-144 `10 s`, `10 x 1.0 s` and `20 x 0.5 s` scientific contract. One persistent spawn-based four-process pool shall supply all 30 colored central-difference Jacobians, while the main process retains residual evaluation, captured modified-Newton globalization, and state acceptance. Every worker task shall carry the actual root's previous inventory, top and lower internal energies, controller memory, and timestep; stale first-root context is prohibited. All 30 roots shall retain complete immutable capture and reproduce every DD-144 accepted step and endpoint within `1e-10`. Exactly 1,260 tasks and 35,280 worker-provider calls, all four workers per matrix, inherited physical/conservation/rank/condition gates, and total wall below both `60%` of DD-144 and `75 s` are required. Rebuild, retry, fallback, clipping, projection, or grid change is prohibited. Passing may authorize only a separately frozen modest parallel trajectory extension; failure retains the serial path.
- `FR-019co` After DD-149 passes, one parallel captured longer-trajectory proof may repeat the exact DD-146 `60 s`, `60 x 1.0 s` and `120 x 0.5 s` scientific contract. The DD-149 persistent four-process implementation and complete capture requirements shall remain unchanged. All 180 roots shall reproduce every DD-146 capture, accepted step, and endpoint within `1e-10`; exactly 7,560 tasks and 211,680 worker-provider calls and all four workers per matrix are required. Every inherited physical, conservation, rank, condition, pressure, controller-direction, kinematic, and refinement gate shall pass. Total wall, including pool startup and shutdown, shall remain below both `60%` of DD-146 and `75 s`. Rebuild, retry, fallback, clipping, projection, controller change, or grid change is prohibited. Passing may authorize one separately frozen multi-minute parallel trajectory; failure retains the validated serial path.
- `FR-019cp` After DD-150 passes, one five-minute parallel controlled-trajectory proof may change only duration from `60 s` to `300 s`, retaining the `1.0 s` and `0.5 s` grids, initial state, controller move, solver, bounds, scales, and scientific gates. One persistent four-process pool shall supply all 900 Jacobians using actual advancing root context. The first `60/120` roots shall reproduce DD-146 within `1e-10`; later roots shall satisfy inherited endpoint-refinement, physicality, pressure, conservation, direction, kinematic, rank, condition, and residual gates. Exactly 37,800 tasks and 1,058,400 worker-provider calls and all workers per matrix are required. A passing campaign may replace full captures with deterministic per-root SHA-256 summaries; failure shall retain full replay evidence. Governed wall shall remain below five times DD-150 and `330 s`. Rebuild, retry, fallback, clipping, projection, controller change, or grid change is prohibited.
- `FR-019cq` Following DD-151's wall-only failure, one zero-call timing audit shall decompose DD-150 and DD-151 wall time into Jacobian, trajectory non-Jacobian, and outside-trajectory contributions. It shall report 60-simulated-second windows for each grid, root-order slopes/correlations, first-to-last-window ratios, solver workload, and excess versus five times DD-150. The audit shall verify all saved root, task, worker-call, prefix-equivalence, and non-wall scientific gates. Static source inspection may record the exact-state density/Cp cache limit and eviction policy, but cache/backend state shall remain an inference unless timing is history-dependent under precommitted thresholds. No DWSIM, provider, residual, Jacobian, solve, correction, state advance, timestep, or trajectory call is permitted. Passing may authorize only a separately frozen saved-state worker-lifetime efficiency probe.
- `FR-019cr` After DD-152 confirms history-dependent Jacobian timing, one saved-state worker-lifetime probe may rebuild the DD-151 Jacobians at coarse roots `60/180/300` and refined roots `120/360/600`. Each state shall run twice in a fresh spawn-based four-worker pool, with the second round reversed. All 12 matrices shall reproduce the saved DD-151 matrices within `1e-10`; exactly 504 tasks and 14,112 governing calls, four independent workers per matrix, repeat spread below `30%`, and wall below `180 s` are required. Worker lifetime is confirmed only if median saved-aged/fresh timing is at least `1.25`, at least four of six checkpoints meet `1.25`, and fresh late/early timing remains within `1.25` on each grid. No nonlinear solve, correction, state acceptance, timestep, trajectory, retry, clipping, projection, or fallback is permitted. Passing may authorize only a separately frozen pool-renewal cadence benchmark.
- `FR-019cs` After DD-153 confirms persistent-worker lifetime slowdown, one zero-call cadence projection may evaluate only `60/120/180/240/300/360/450/900` roots between four-worker pool renewals. It shall use DD-152's frozen coarse/refined aging slopes, DD-153's path-specific fresh-matrix medians and complete pool-lifecycle overhead, and DD-151's fixed non-Jacobian wall. The cadence-900 projection shall be additively calibrated to reproduce DD-151's `476.3494051 s` wall exactly. Nominal selection shall minimize projected wall; slope uncertainty at `0.75x/1.0x/1.25x` shall keep every selected cadence within `120..450` roots, and nominal improvement shall be at least `20%`. No DWSIM, provider, residual, Jacobian, solve, correction, state advance, timestep, or trajectory call is permitted. Passing may authorize only implementation of the selected cadence and a separately frozen saved-state equivalence proof.
- `FR-019ct` After DD-154 rejects complete pool renewal, one saved-state in-worker reset probe may age a single spawn-based four-worker pool through DD-151 coarse roots `2..180`, then rebuild root `180` twice after no reset, Python provider-cache clear, provider reconstruction in the same process, and diagnostic DWSIM calculator/property-package reinitialization. Exactly 187 matrices, 7,854 tasks, 219,912 worker-evaluation provider calls, 232 reset calls, four-worker ownership, and matrix agreement within `1e-10` are required. Aging shall reproduce at least `1.20x` the DD-153 fresh reference. Recovery requires reset/fresh timing no greater than `1.15x` and at least `1.20x` speedup over the aged no-reset stage. No solve, correction, state acceptance, timestep, trajectory, retry, clipping, projection, or fallback is permitted. Direct backend mutation is diagnostic only; passing may authorize only a separately implemented and frozen reset API plus saved-state equivalence proof.
- `FR-019cu` After DD-155 rejects in-worker reset, one saved-state exact-thermo-memoization benchmark may rebuild DD-151 coarse root `180` exactly three times in one fresh four-worker pool: uncached pass-through, cold exact memoization, and warm exact memoization. Diagnostic memoization may cover direct fugacity, phase enthalpy, liquid density, vapor compressibility, and molecular weight using only unrounded phase, temperature, pressure, and composition keys. Exactly 3 matrices, 126 tasks, 3,528 logical governing calls, four-worker ownership, complete cache accounting, and matrix agreement within `1e-10` are required. The uncached timing shall remain within `0.65..1.35` of DD-153's fresh reference; warm-cache speedup shall be at least `1.50x` with hit fraction at least `0.50`. No solve, correction, state acceptance, timestep, trajectory, retry, clipping, projection, fallback, approximate key, or production provider change is permitted. Passing may authorize only a bounded production implementation and a separately frozen saved-state equivalence benchmark.
- `FR-019cv` The DD-156-authorized production memoization layer shall remain disabled by default, use exact normalized-state keys, return copy-safe arrays, expose per-family hit/miss/entry counters, remain bounded, and clear all runtime property entries at a unique caller-owned Jacobian epoch. One saved-state proof shall compare uncached and memoized production matrices at DD-151 coarse root `180` and refined root `360` in one four-worker pool. Exactly 4 matrices, 168 tasks, 4,704 logical governing calls, four-worker ownership, complete memo accounting, and matrix agreement within `1e-10` are required. Each uncached timing shall remain within `0.65..1.50` of its DD-153 fresh reference; each memoized state shall achieve at least `1.50x` speedup and `0.60` hit fraction. No solve, correction, state acceptance, timestep, trajectory, retry, clipping, projection, fallback, approximate key, or cross-epoch reuse is permitted. Passing may authorize only a separately frozen short-trajectory integration proof.
- `FR-019cw` After DD-157 passes, one captured memoized trajectory may repeat the exact DD-149/DD-144 `10 s`, `10 x 1.0 s` and `20 x 0.5 s` scientific contract. One persistent four-worker pool shall derive a unique exact-memo epoch from each colored-Jacobian state prefix. All 30 complete captures, solver decisions, accepted states, and endpoints shall reproduce DD-149 within `1e-10`; exactly 30 roots, 1,260 tasks, 35,280 logical worker-provider calls, 1,176 memo-accounted calls per root, and four-worker ownership are required. Every root shall achieve at least `0.60` exact-cache hit fraction. Trajectory wall shall be no more than `0.75` of DD-149 and total wall shall remain below `30 s`. No rebuild, retry, fallback, clipping, projection, controller change, grid change, approximate key, or cross-Jacobian reuse is permitted. Passing may authorize only a separately frozen longer memoized trajectory.
- `FR-020` The model shall support optional top-drum PSV venting with linear gain and max-vent clamp.
- `FR-020a` In the standard explicit-sump configuration, the reboiler liquid feed shall be drawn from the bottom sump rather than directly from the bottom tray.
- `FR-020b` The runner shall support disabling separate top and bottom boundary states for validation sources whose stage set already includes the total condenser and reboiler.

### 4.5 Controls and Runtime Safeguards
- `FR-021` The runner shall support level PI control for top and bottom inventories.
- `FR-022` The runner shall support top-pressure PI control with MV selection (`top-anchor` or `condenser-duty`).
- `FR-023` The pressure loop shall support optional PV filtering, optional MV slew limiting, and residual-based gain attenuation.
- `FR-024` The runner shall support distillate composition PI control with reflux feasibility limiting.
- `FR-025` The runner shall support bottoms composition PI control with selectable MV (`boilup` or `reboiler-duty`).
- `FR-025a` Before a controlled rigorous solve, the system shall audit controller degrees of freedom. Each active controller shall have one available manipulated variable, and no manipulated variable shall have duplicate active ownership unless a documented selector or cascade structure resolves that ownership.
- `FR-026` The runner may use diagnosed clamps, projections, slew limits, and bound enforcement to protect startup or rejected nonlinear-solver trials.
- `FR-026a` A rigorously accepted algebraic or steady solution shall contain no negative phase amounts and shall not depend on an accepted state projection, binding imported-profile ceiling, previous-step limiter, or other safeguard that replaces the governing closure.
- `FR-026b` Solver reports shall distinguish rejected/attempted projections from projections present in an accepted iterate.
- `FR-031` The runner shall apply deterministic runtime-mode presets for pressure model, vapor-flow model, and liquid-hydraulic override according to selected `--runtime-mode`.

### 4.6 Logging and Ledger
- `FR-027` The system shall write profile and summary CSV logs when enabled.
- `FR-028` The system shall emit diagnostics for control commands, condenser/top-drum behavior, PSV behavior, and mass-closure signals.
- `FR-029` The CLI shall block duplicate exact command identities unless `--allow-repeat-command` is provided.
- `FR-030` The system shall append run registry metadata and regenerate `docs/experiment_ledger.csv` and `docs/experiment_ledger.md` after logged runs.
- `FR-032` Profile logs shall expose per-component dynamic-state K, thermo-equilibrium K, and their ratio (`K_state_*`, `K_thermo_*`, `K_state_over_K_thermo_*`).
- `FR-033` Bottoms-composition MV diagnostics shall reflect the selected MV mode: duty mode logs `Q_reb_cmd_BTUph`/`Q_reb_used_BTUph` as active MV and may leave `Boilup_cmd_lbmolph` undefined (`NaN`).
- `FR-034` The runner shall be able to export a restart workbook from a completed run, including optional `Boundary State`, `Energy State`, and `Controller State` sheets.
- `FR-035` A completed run shall be able to generate a human-readable Word report containing provenance, parameters, wall and simulation time, starting and ending conditions, controller and duty trends, and final stage profiles.
- `FR-036` A completed run shall be able to serialize and reload a native checkpoint containing the packed dynamic state and required runtime/controller memory. Reusable checkpoint artifacts shall pass a reload gate.
- `FR-037` Rigorous model acceptance shall include a physical-closure gate covering at least liquid-flow ownership, net phase/energy consistency, pressure versus vapor-holdup consistency, and non-binding diagnostic profile limits.
- `FR-037a` Unless superseded by a documented case requirement, rigorous local closure targets shall be: component reconstruction relative residual `<1e-8`; energy relative residual `<1e-7`; volume relative residual `<1e-7`; and phase-equilibrium fugacity residual or certified equivalent `<1e-6`. Global targets shall include scaled pressure-drop and vapor-flow residuals `<1e-5`, local-thermo versus global solved-pressure mismatch `<0.1 psi`, zero binding profile/previous-step flow limiters, zero accepted projections, complete terminal mapping, and the robustness requirement in `FR-019h`.

## 5. Traceability Matrix

| Requirement | Primary Implementation Trace | Test Trace |
|---|---|---|
| `UR-001`, `UR-002` | `src/dynamic_distillation/dynamic_run_scaffold_v1.py` | `tests/test_dynamic_run_scaffold_v1.py` |
| `UR-003`, `FR-010`..`FR-014e` | `src/dynamic_distillation/dynamic_run_scaffold_v1.py`, `src/dynamic_distillation/column_rhs_v1.py`, `src/dynamic_distillation/thermo_surrogate_v1.py`, `src/dynamic_distillation/thermo_table_pool_v1.py`, `src/dynamic_distillation/thermo_backend_factory_v1.py`, `src/dynamic_distillation/thermo_relative_volatility_provider_v1.py`, `tools/create_skogestad_column_a_validation_case.py`, `tools/compare_skogestad_column_a_profile.py`, `tools/compare_skogestad_dynamic_response.py`, `tools/plot_skogestad_dynamic_comparison.py`, `docs/cli.md`, `pyproject.toml` | `tests/test_stage_thermo_v1.py`, `tests/test_thermo_surrogate_v1.py`, `tests/test_thermo_table_pool_v1.py`, `tests/test_thermo_backend_factory_v1.py`, `tests/test_thermo_relative_volatility_provider_v1.py`, `tests/test_column_rhs_v1.py` |
| `UR-004`, `UR-013`, `FR-016`..`FR-020b` | `src/dynamic_distillation/dynamic_run_scaffold_v1.py`, `src/dynamic_distillation/column_rhs_v1.py`, `src/dynamic_distillation/state_vector_layout_v1.py` | `tests/test_column_rhs_v1.py`, `tests/test_dynamic_run_scaffold_v1.py`, `tests/test_state_vector_layout_v1.py` |
| `UR-005`, `FR-021`..`FR-026`, `FR-031` | `src/dynamic_distillation/dynamic_run_scaffold_v1.py` | `tests/test_dynamic_run_scaffold_v1.py` |
| `UR-006`, `FR-020` | `src/dynamic_distillation/column_rhs_v1.py`, `src/dynamic_distillation/dynamic_run_scaffold_v1.py` | `tests/test_column_rhs_v1.py`, `tests/test_dynamic_run_scaffold_v1.py` |
| `UR-007`, `FR-005` | `src/dynamic_distillation/excel_case_validator_v1.py`, `src/dynamic_distillation/dynamic_run_scaffold_v1.py` | `tests/test_excel_case_validator_v1.py` |
| `UR-008`, `FR-027`, `FR-028`, `FR-032`, `FR-033` | `src/dynamic_distillation/dynamic_run_scaffold_v1.py`, `src/dynamic_distillation/column_rhs_v1.py` | `tests/test_dynamic_run_scaffold_v1.py`, `tests/test_column_rhs_v1.py` |
| `UR-009`, `FR-029` | `src/dynamic_distillation/dynamic_run_scaffold_v1.py`, `src/dynamic_distillation/experiment_ledger_v1.py` | N/A |
| `UR-010`, `FR-030` | `src/dynamic_distillation/experiment_ledger_v1.py`, `tools/update_experiment_ledger.py` | N/A |
| `UR-012`, `FR-002a`, `FR-009a`, `FR-034` | `src/dynamic_distillation/excel_case_loader_v1.py`, `src/dynamic_distillation/column_spec_builder_v1.py`, `src/dynamic_distillation/state_vector_layout_v1.py`, `src/dynamic_distillation/dynamic_run_scaffold_v1.py` | `tests/test_excel_case_loader_v1.py`, `tests/test_column_spec_builder_v1.py`, `tests/test_state_vector_layout_v1.py`, `tests/test_dynamic_run_scaffold_v1.py` |
| `UR-011` | `src/dynamic_distillation/dynamic_run_scaffold_v1.py` | `tests/test_dynamic_run_scaffold_v1.py` |
| `UR-014`, `FR-009b`, `FR-009c` | `docs/dynamic_column_initialization_strategy.md`, `docs/validation_readiness_gate_2026-05-26.md`, `docs/issue_log.md` (`DD-032`) | Planned; current support is documented policy plus residual/audit tooling under development. |
| `UR-015`..`UR-017`, `FR-007c`, `FR-009d`..`FR-009g`, `FR-019a`..`FR-019z`, `FR-025a`, `FR-026a`, `FR-026b`, `FR-037`, `FR-037a` | `docs/dynamic_model_current_state_2026-07-12.md`, `docs/dd_060_physics_owned_tray_flow_probe_20260712.md`, `docs/dd_065_frozen_checkpoint_uv_hydraulic_closure_20260717.md`, `docs/dd_066_terminal_conserved_inventory_mapping_20260717.md`, `docs/dd_067_conservative_energy_redistribution_probe_20260717.md`, `docs/dd_068_least_movement_redistribution_20260717.md`, `docs/dd_069_terminal_energy_volume_basis_audit_20260717.md`, `docs/dd_070_canonical_checkpoint_repair_20260717.md`, `docs/dd_071_direct_steady_state_registry_20260718.md`, `docs/dd_072_direct_steady_state_numerical_audit_20260718.md`, `docs/dd_073_direct_steady_state_continuation_20260718.md`, `docs/dd_074_merged_continuation_structural_audit_20260718.md`, `docs/dd_075_reduced_column_feasibility_20260718.md`, `docs/dd_076_equilibrium_dae_v2_architecture_contract_20260718.md`, `docs/dd_077_core_v2_structural_registry_20260718.md`, `docs/dd_078_core_v2_source_equation_gate_20260718.md`, `docs/dd_079_core_v2_gate_a_dynamics_20260718.md`, `docs/dd_080_core_v2_gate_b_one_volume_20260718.md`, `docs/dd_081_core_v2_gate_c_five_volume_20260718.md`, `docs/dd_082_core_v2_gate_c_steady_solve_20260718.md`, `docs/dd_083_energy_owned_vapor_flow_architecture_20260718.md`, `docs/gates_explained.md`, `src/dynamic_distillation/core_v2/reduced_topology_v1.py`, `src/dynamic_distillation/core_v2/reduced_column_spec_v1.py`, `src/dynamic_distillation/core_v2/reduced_state_registry_v1.py`, `src/dynamic_distillation/core_v2/reduced_residual_registry_v1.py`, `src/dynamic_distillation/core_v2/source_equation_gate_v1.py`, `src/dynamic_distillation/core_v2/source_equation_dynamics_v1.py`, `src/dynamic_distillation/core_v2/one_volume_property_gate_v1.py`, `src/dynamic_distillation/core_v2/five_volume_residual_gate_v1.py`, `src/dynamic_distillation/core_v2/five_volume_steady_solve_v1.py`, `src/dynamic_distillation/core_v2/energy_owned_vapor_registry_v1.py`, `src/dynamic_distillation/pr_flash_backend_v1.py`, `src/dynamic_distillation/thermo_provider_v1.py`, `tools/audit_core_v2_reduced_registry.py`, `tools/audit_core_v2_source_equation_gate.py`, `tools/audit_core_v2_gate_a_dynamics.py`, `tools/audit_core_v2_gate_b_one_volume.py`, `tools/audit_core_v2_gate_c_five_volume.py`, `tools/run_core_v2_gate_c_steady_solve.py`, `tools/audit_core_v2_energy_owned_vapor_registry.py` | `tests/test_core_v2_reduced_registry_v1.py`, `tests/test_core_v2_source_equation_gate_v1.py`, `tests/test_core_v2_source_equation_dynamics_v1.py`, `tests/test_core_v2_one_volume_property_gate_v1.py`, `tests/test_core_v2_five_volume_residual_gate_v1.py`, `tests/test_core_v2_energy_owned_vapor_registry_v1.py`, `tests/test_pr_flash_backend_v1.py`, `tests/test_thermo_provider_v1.py`; DD-082 retires prescribed-vapor Gate C. DD-083 establishes a full-rank structural-only energy-owned vapor ledger; no live solve or dynamics is authorized. |
| `FR-019aa` | `docs/dd_084_energy_owned_vapor_numerical_audit_contract_20260718.md`, `docs/dd_084_energy_owned_vapor_numerical_audit_20260718.md`, `src/dynamic_distillation/core_v2/energy_owned_vapor_numerical_gate_v1.py`, `tools/audit_core_v2_energy_owned_vapor_numerical.py` | `tests/test_core_v2_energy_owned_vapor_numerical_gate_v1.py`; DD-084 passes at rank `37/37`, worst condition `1.78e6`, and machine-precision conservation. One bounded root campaign may be drafted and frozen; no solve or dynamics is yet authorized. |
| `FR-019ab` | `docs/dd_085_energy_owned_vapor_steady_root_contract_20260718.md`, `docs/dd_085_energy_owned_vapor_steady_root_20260718.md`, `logs/dd085_energy_owned_steady_root_contract_20260718.json`, `logs/dd085_energy_owned_steady_root_20260718.json`, `src/dynamic_distillation/core_v2/energy_owned_vapor_steady_solve_v1.py`, `tools/run_core_v2_energy_owned_vapor_steady_root.py` | `tests/test_core_v2_energy_owned_vapor_steady_solve_v1.py`; all starts reach the same exact, full-rank, interior root, but it fails physical temperature ordering because the reflux drum is `3.233 F` hotter than its supplying rectifying stage. The architecture is retired; no tuning or dynamics is authorized. |
| `FR-019ac` | `docs/dd_086_condenser_phase_stability_architecture_20260718.md`, `logs/dd086_condenser_phase_stable_architecture_20260718.json`, `src/dynamic_distillation/core_v2/condenser_phase_stability_v1.py`, `src/dynamic_distillation/core_v2/condenser_saturated_liquid_registry_v1.py`, `tools/audit_core_v2_condenser_phase_stable_architecture.py` | `tests/test_core_v2_condenser_phase_stability_v1.py`; the DD-085 outlet is confirmed stable vapor despite exact imposed-liquid enthalpy closure. The solved-duty saturated-liquid successor is `40 x 40`, structurally rank `40`, and conservative. One frozen live numerical audit may be designed; no root solve or dynamics is authorized. |
| `FR-019ad` | `docs/dd_087_condenser_saturated_liquid_numerical_audit_contract_20260718.md`, `docs/dd_087_condenser_saturated_liquid_numerical_audit_20260718.md`, `logs/dd087_condenser_saturated_liquid_numerical_contract_20260718.json`, `logs/dd087_condenser_saturated_liquid_numerical_20260718.json`, `src/dynamic_distillation/core_v2/condenser_saturated_liquid_numerical_gate_v1.py`, `tools/audit_core_v2_condenser_saturated_liquid_numerical.py` | `tests/test_core_v2_condenser_saturated_liquid_numerical_gate_v1.py`; DD-087 passes at rank `40/40`, local bubble rank `3/3`, worst condition `2.45e6`, machine-precision conservation, and correct solved-duty ownership. One bounded root campaign may be drafted and frozen; no solve or dynamics is yet authorized. |
| `FR-019ae` | `docs/dd_088_condenser_saturated_liquid_steady_root_contract_20260719.md`, `docs/dd_088_condenser_saturated_liquid_steady_root_20260719.md`, `logs/dd088_condenser_saturated_liquid_steady_root_contract_20260719.json`, `logs/dd088_condenser_saturated_liquid_steady_root_20260719.json`, `src/dynamic_distillation/core_v2/condenser_saturated_liquid_steady_solve_v1.py`, `tools/run_core_v2_condenser_saturated_liquid_steady_root.py` | `tests/test_core_v2_condenser_saturated_liquid_steady_solve_v1.py`; all three starts find the same machine-precision, full-rank, physical root, but the TP incipient-composition cross-check is `1.467e-5` against the frozen `1e-5` limit. DD-088 therefore fails and retires this five-volume architecture without tuning or dynamics. |
| `FR-019af` | `docs/dd_089_dwsim_pr_interface_consistency_contract_20260719.md`, `docs/dd_089_dwsim_pr_interface_consistency_20260719.md`, `logs/dd089_dwsim_pr_interface_consistency_contract_20260719.json`, `logs/dd089_dwsim_pr_interface_consistency_20260719.json`, `src/dynamic_distillation/core_v2/dwsim_phase_interface_consistency_v1.py`, `tools/audit_core_v2_dwsim_phase_interface_consistency.py` | `tests/test_core_v2_dwsim_phase_interface_consistency_v1.py`; three fresh processes are exactly repeatable, flash `K*x_flash` and lever-rule closure are at roundoff, and independent PR supports the direct fugacity bubble. The DD-088 metric is shown to combine and partially cancel a composition-basis substitution with a TP endpoint offset. |
| `FR-019ag` | `docs/dd_090_pr_provider_authority_contract_20260719.md`, `docs/dd_090_pr_provider_authority_20260719.md`, `logs/dd090_pr_provider_authority_contract_20260719.json`, `logs/dd090_pr_provider_authority_20260719.json`, `src/dynamic_distillation/core_v2/pr_provider_authority_contract_v1.py`, `tools/audit_core_v2_pr_provider_authority.py` | `tests/test_core_v2_pr_provider_authority_contract_v1.py`; 11 ownership/evidence checks pass with no mixed-basis gate, cross-interface equality gate, fallback, live property call, column solve, or dynamics. |
| `FR-019ah` | `docs/dd_091_core_v3_provider_governed_architecture_20260719.md`, `logs/dd091_core_v3_provider_governed_structural_20260719.json`, `src/dynamic_distillation/core_v3/provider_governed_registry_v1.py`, `tools/audit_core_v3_provider_governed_registry.py` | `tests/test_core_v3_provider_governed_registry_v1.py`; the three-component registry is `40 x 40`, structural rank `40`, nullity zero, conservative, provider-tagged, and free of prohibited production dependencies. DD-092 live numerical audit only is authorized. |
| `FR-019ai` | `docs/dd_092_core_v3_provider_governed_numerical_contract_20260719.md`, `docs/dd_092_core_v3_provider_governed_numerical_20260719.md`, `logs/dd092_core_v3_provider_governed_numerical_contract_20260719.json`, `logs/dd092_core_v3_provider_governed_numerical_20260719.json`, `src/dynamic_distillation/core_v3/provider_call_audit_v1.py`, `src/dynamic_distillation/core_v3/provider_governed_residual_v1.py`, `tools/audit_core_v3_provider_governed_numerical.py` | `tests/test_core_v3_provider_call_audit_v1.py`, `tests/test_core_v3_provider_governed_residual_v1.py`; DD-092 passes once from contract commit `1ffa504`: both states are rank `40/40`, local bubble rank `3/3`, worst condition `2.733414e6`, and conservative at machine precision with no provider-policy violation. One bounded three-start Core V3 root contract may be drafted; execution and dynamics remain unauthorized. |
| `FR-019aj` | `docs/dd_093_core_v3_steady_root_contract_20260719.md`, `docs/dd_093_core_v3_steady_root_20260725.md`, `logs/dd093_core_v3_steady_root_contract_20260719.json`, `logs/dd093_core_v3_steady_root_20260719.json`, `src/dynamic_distillation/core_v3/provider_governed_steady_root_v1.py`, `tools/run_core_v3_provider_governed_steady_root.py` | `tests/test_core_v3_provider_governed_steady_root_v1.py`; the one authorized execution from `a33749f` stops during first-start report assembly on an invalid scalar-index-as-slice access. No quantitative endpoint is retained, Starts 2/3 do not run, and no common-root decision is possible. The frozen campaign is retired without patch or rerun; no dynamic work is authorized. |
| `FR-019ak` | `docs/dd_094_core_v3_reporting_recovery_contract_20260725.md`, `docs/dd_094_core_v3_steady_root_20260725.md`, `logs/dd094_core_v3_steady_root_recovery_contract_20260725.json`, `logs/dd094_core_v3_steady_root_20260725.json`, `tools/prepare_core_v3_dd094_recovery_contract.py`, `tools/run_core_v3_provider_governed_steady_root.py`, `src/dynamic_distillation/core_v3/provider_governed_steady_root_v1.py` | `tests/test_core_v3_provider_governed_steady_root_v1.py`; the user-approved recovery preserves mathematical checksum `272890348b11...feb01b93`. All three starts pass once from commit `52f132d`, converge within `2.47414e-10`, retain rank `40/40` and local rank `3/3`, and satisfy every residual, condition, spectrum, conservation, phase, physical, bound, and provider gate. Only structural dynamic-DAE contract work is authorized. |
| `FR-019al` | `docs/dd_095_core_v3_dynamic_dae_contract_20260725.md`, `logs/dd095_core_v3_dynamic_dae_contract_20260725.json`, `src/dynamic_distillation/core_v3/dynamic_dae_contract_v1.py`, `tools/audit_core_v3_dynamic_dae_contract.py` | `tests/test_core_v3_dynamic_dae_contract_v1.py`; the three-component implicit derivative/algebraic registry is `38 x 38`, structural rank `38`, nullity zero, with 15 inventory coordinates, provider-derived internal-energy storage, fixed DD-094 product draws, and no controller, terminal-level constraint, profile, or fallback. DD-096 numerical leading-Jacobian contract work only is authorized. |
| `FR-019am` | `docs/dd_096_core_v3_dynamic_dae_numerical_contract_20260725.md`, `docs/dd_096_core_v3_dynamic_dae_numerical_20260725.md`, `logs/dd096_core_v3_dynamic_dae_numerical_contract_20260725.json`, `logs/dd096_core_v3_dynamic_dae_numerical_20260725.json`, `src/dynamic_distillation/core_v3/dynamic_dae_numerical_audit_v1.py`, `tools/audit_core_v3_dynamic_dae_numerical.py` | `tests/test_core_v3_dynamic_dae_numerical_audit_v1.py`; DD-096 passes once from contract commit `42975bc`: both leading matrices retain rank `38/38`, worst condition `35.408732`, storage-gradient step change `8.67e-10`, spectrum change `3.51e-7`, and conservation near machine precision with no structural or provider violation. One implicit-solver contract may be drafted; integration remains unauthorized. |
| `FR-019an` | `docs/dd_097_core_v3_implicit_step_contract_20260725.md`, `docs/dd_097_core_v3_implicit_step_20260725.md`, `logs/dd097_core_v3_implicit_step_contract_20260725.json`, `logs/dd097_core_v3_implicit_step_20260725.json`, `src/dynamic_distillation/core_v3/implicit_step_v1.py`, `tools/run_core_v3_implicit_step.py` | `tests/test_core_v3_implicit_step_v1.py`; DD-097 passes once from contract commit `ac43127`. Zero-rate recovery is rank `23/23`; both finite steps are rank `38/38`, condition below `35.2`, residual below `1.1e-12`, conservative and physical, with endpoint refinement near `1e-12` and no provider violation. One short open-loop trajectory contract may be drafted. |
| `FR-019ao` | `docs/dd_098_core_v3_short_open_loop_contract_20260725.md`, `docs/dd_098_core_v3_short_open_loop_20260725.md`, `logs/dd098_core_v3_short_open_loop_contract_20260725.json`, `logs/dd098_core_v3_short_open_loop_20260725.json`, `src/dynamic_distillation/core_v3/short_trajectory_v1.py`, `tools/run_core_v3_short_open_loop.py` | `tests/test_core_v3_short_trajectory_v1.py`; DD-098 passes once from contract commit `5ded9f6`. All eight requested endpoints pass; perturbed accumulation matches the exact `0.003968319 lbmol` target within `1.29e-11` relative, refinement is below `4.46e-6`, and all physical, conservation, rank, condition, and provider gates pass. One modest longer open-loop contract may be drafted. |
| `FR-019ap` | `docs/dd_099_core_v3_performance_contract_20260725.md`, `docs/dd_099_core_v3_performance_20260725.md`, `logs/dd099_core_v3_performance_contract_20260725.json`, `logs/dd099_core_v3_performance_20260725.json`, `src/dynamic_distillation/core_v3/colored_jacobian_v1.py`, `src/dynamic_distillation/core_v3/implicit_step_v1.py`, `tools/run_core_v3_performance_audit.py` | `tests/test_core_v3_colored_jacobian_v1.py`; DD-099 passes once from contract commit `289b451`. Colored and uncolored endpoints and Jacobians are identical to reported precision, all numerical and physical gates pass, nested bubble calls are zero, and colored solves average `3,000` calls, a `13.56x` reduction from DD-098's per-endpoint mean. One modest longer open-loop contract may be drafted. |
| `FR-019aq` | `docs/dd_100_core_v3_longer_open_loop_contract_20260725.md`, `docs/dd_100_core_v3_longer_open_loop_20260725.md`, `logs/dd100_core_v3_longer_open_loop_contract_20260725.json`, `logs/dd100_core_v3_longer_open_loop_20260725.json`, `src/dynamic_distillation/core_v3/short_trajectory_v1.py`, `tools/run_core_v3_longer_open_loop.py` | DD-100 passes once from contract commit `d92d354`. All 35 endpoints complete without retry; maximum residual is `1.62e-12`, worst condition is `7.39e5`, exact accumulation closes within `1.01e-11` relative, refinement is below `6.81e-6` in algebraic coordinates and `1.61e-5 F`, nested bubble calls are zero, and performance is `3,724.8` calls per endpoint over `69.264 s`. The next dynamic-scope decision may be drafted. |
| `FR-019ar` | `docs/dd_101_core_v3_pressure_layer_contract_20260725.md`, `logs/dd101_core_v3_pressure_layer_20260725.json`, `src/dynamic_distillation/core_v3/pressure_layer_contract_v1.py`, `tools/audit_core_v3_pressure_layer.py` | `tests/test_core_v3_pressure_layer_contract_v1.py`; DD-101 passes the property-free structural gate at `42 x 42`, rank `42`, and nullity zero. Four lower-volume pressure unknowns pair with four uncapped pressure-drop rows while the four vapor links remain energy-owned. There are no unused/unregistered entries, prescribed interior pressures, controllers, profiles, caps, relaxation, fallback, or explicit vapor inventory. One frozen live numerical audit may be drafted. |
| `FR-019as` | `docs/dd_102_core_v3_pressure_layer_numerical_contract_20260725.md`, `docs/dd_102_core_v3_pressure_layer_numerical_20260725.md`, `logs/dd102_core_v3_pressure_layer_numerical_contract_20260725.json`, `logs/dd102_core_v3_pressure_layer_numerical_20260725.json`, `src/dynamic_distillation/core_v3/pressure_layer_numerical_v1.py`, `tools/audit_core_v3_pressure_layer_numerical.py` | `tests/test_core_v3_pressure_layer_numerical_v1.py`; DD-102 passes once from contract commit `b958d00`. All four matrices are rank `42/42`, worst condition is `162.783`, spectrum change is below `3.51e-7`, conservation is at roundoff, and all `9,465` provider calls pass in `8.950 s`. The accepted fixed-pressure profile retains pressure residuals up to `4.444 psi`, confirming it is not a hydraulic root. One frozen pressure-layer steady-root contract is authorized, subject to an explicit bottom-link geometry decision. |
| `FR-019at` | `docs/dd_103_core_v3_pressure_layer_steady_root_contract_20260726.md`, `docs/dd_103_core_v3_pressure_layer_steady_root_20260726.md`, `logs/dd103_core_v3_pressure_layer_steady_root_contract_20260726.json`, `logs/dd103_core_v3_pressure_layer_steady_root_20260726.json`, `src/dynamic_distillation/core_v3/pressure_layer_steady_root_v1.py`, `tools/run_core_v3_pressure_layer_steady_root.py` | `tests/test_core_v3_pressure_layer_steady_root_v1.py`, `tests/test_core_v3_pressure_layer_numerical_v1.py`; DD-103 executes once from contract commit `f908b73`. Both starts terminate at nearly the same interior ordered-pressure state, with rank `27/27`, worst condition `65.959`, roundoff conservation, and all `27,721` provider calls accepted in `16.835 s`. The scaled residual stalls reproducibly near `8.947e-3` and common-root difference is `2.445e-5`, so the frozen gates fail. Algebraic-only pressure repair is retired; only a structural pressure-enabled implicit-DAE contract may follow. |
| `FR-019au` | `docs/dd_104_core_v3_pressure_implicit_dae_contract_20260726.md`, `logs/dd104_core_v3_pressure_implicit_dae_20260726.json`, `src/dynamic_distillation/core_v3/pressure_implicit_dae_contract_v1.py`, `tools/audit_core_v3_pressure_implicit_dae.py` | `tests/test_core_v3_pressure_implicit_dae_contract_v1.py`; DD-104 passes the property-free structural gate at `42 x 42`, rank `42`, and nullity zero. The solve vector contains 15 inventory rates and 27 algebraic coordinates, including four lower pressures and no pressure rates. The terminal return is dry-only, three tray links retain liquid head, all backward-Euler endpoint dependencies are registered, and the conflict-free Jacobian pattern uses 20 colors. One frozen live leading-Jacobian and consistent-rate audit is authorized; no numerical step or integration occurred. |
| `FR-019av` | `docs/dd_105_core_v3_pressure_implicit_first_step_contract_20260726.md`, `docs/dd_105_core_v3_pressure_implicit_first_step_20260726.md`, `logs/dd105_core_v3_pressure_implicit_first_step_contract_20260726.json`, `logs/dd105_core_v3_pressure_implicit_first_step_20260726.json`, `src/dynamic_distillation/core_v3/pressure_implicit_step_v1.py`, `tools/run_core_v3_pressure_implicit_first_step.py` | `tests/test_core_v3_pressure_implicit_step_v1.py`; DD-105 executes once from contract commit `94dba94`. Both grids solve below `9.0e-13`, all endpoint Jacobians are rank `42/42`, and conservation/provider/efficiency gates pass. Refinement fails materially: inventory `6.28e-2`, algebraic `1.67`, and pressure `2.77 psi`. Pressure-enabled stepping is stopped because DD-094 is not a consistent pressure-enabled initial state. |
| `FR-019aw` | `docs/dd_106_core_v3_pressure_consistent_initializer_contract_20260726.md`, `logs/dd106_core_v3_pressure_consistent_initializer_20260726.json`, `src/dynamic_distillation/core_v3/pressure_consistent_initializer_contract_v1.py`, `tools/audit_core_v3_pressure_consistent_initializer.py` | `tests/test_core_v3_pressure_consistent_initializer_contract_v1.py`; DD-106 passes property-free at 57 primal variables and 48 independent exact constraints. The equality ledger has rank 48, the feasible manifold has dimension 9, and the `105 x 105` KKT pattern is full rank with no empty or unregistered entry. One frozen live initializer numerical contract may be drafted; no property call or solve occurred. |
| `FR-019ax` | `docs/dd_107_core_v3_pressure_initializer_readiness_20260726.md`, `logs/dd107_core_v3_pressure_initializer_readiness_20260726.json`, `src/dynamic_distillation/core_v3/pressure_initializer_readiness_v1.py`, `tools/audit_core_v3_pressure_initializer_readiness.py` | `tests/test_core_v3_pressure_initializer_readiness_v1.py`; DD-107 stops DD-106 before live execution. Four pressures are algebraic and nonzero `dN/dt` is permitted, but there are no independent energy states/rates and the only exact storage definition is timestep-dependent. Fixed-pressure-gradient reuse and hidden timestepping are prohibited. A property-free conserved-`N/U` pressure-DAE ownership audit is the only authorized successor. |
| `FR-019ay` | `docs/dd_108_core_v3_conserved_nu_pressure_dae_contract_20260726.md`, `logs/dd108_core_v3_conserved_nu_pressure_dae_20260726.json`, `src/dynamic_distillation/core_v3/conserved_nu_pressure_dae_contract_v1.py`, `tools/audit_core_v3_conserved_nu_pressure_dae.py` | `tests/test_core_v3_conserved_nu_pressure_dae_contract_v1.py`; the initial all-five-`U` assembly is rejected at rank `46/47`. Ownership aligned to pressure gives four independent lower `U/dU` pairs and derived fixed-pressure top energy. The corrected three-component ledger is `46 x 46`, rank 46, nullity zero; the generic two-component case is `36 x 36`, rank 36. One frozen live rank/manifold contract may be drafted; no property call or solve occurred. |
| `FR-019az` | `docs/dd_109_core_v3_conserved_nu_pressure_numerical_contract_20260726.md`, `docs/dd_109_core_v3_conserved_nu_pressure_numerical_20260726.md`, `logs/dd109_core_v3_conserved_nu_pressure_numerical_contract_20260726.json`, `logs/dd109_core_v3_conserved_nu_pressure_numerical_20260726.json`, `src/dynamic_distillation/core_v3/conserved_nu_pressure_numerical_v1.py`, `tools/audit_core_v3_conserved_nu_pressure_numerical.py` | `tests/test_core_v3_conserved_nu_pressure_numerical_v1.py`; DD-109 executes once from contract commit `99b88fd`. Rank, condition, spectrum, registered coupling, storage rank/closure, conservation, provider, pressure, call, and wall gates pass. The formal result fails because the frozen physical gate treats intentionally undefined drum/sump tray-height sentinels as invalid. No rerun occurs and the conserved-`N/U` pressure numerical path remains stopped pending an explicit governance decision. |
| `FR-019ba` | `docs/dd_110_core_v3_dd109_physical_gate_adjudication_contract_20260726.md`, `docs/dd_110_core_v3_dd109_physical_gate_adjudication_20260726.md`, `logs/dd110_core_v3_dd109_physical_gate_adjudication_contract_20260726.json`, `logs/dd110_core_v3_dd109_physical_gate_adjudication_20260726.json`, `src/dynamic_distillation/core_v3/dd109_gate_adjudication_v1.py`, `tools/adjudicate_core_v3_dd109_physical_gate.py` | `tests/test_core_v3_dd109_gate_adjudication_v1.py`; DD-110 passes once from contract commit `464e568`. The exact DD-109 hashes and all inherited gates are unchanged; three applicable Francis heights, two terminal `NaN` sentinels, and dry-only liquid-head ownership pass. Zero property, residual, Jacobian, solve, initializer, or timestep calls occur. One separately frozen conserved-`N/U` initializer contract may be drafted; execution remains unauthorized. |
| `FR-019bb` | `docs/dd_111_core_v3_conserved_nu_pressure_initializer_contract_20260726.md`, `logs/dd111_core_v3_conserved_nu_pressure_initializer_20260726.json`, `src/dynamic_distillation/core_v3/conserved_nu_pressure_initializer_contract_v1.py`, `tools/audit_core_v3_conserved_nu_pressure_initializer.py` | `tests/test_core_v3_conserved_nu_pressure_initializer_contract_v1.py`; DD-111 passes property-free at 65 primals, 52 exact constraints/rank, 13 feasible dimensions, and full KKT rank `117/117`. Four lower `U/dU` pairs and storage closures correct DD-106's missing pressure-aware continuous energy ownership. One frozen live constrained-initializer contract may be drafted; no execution is authorized. |
| `FR-019bc` | `docs/dd_112_core_v3_conserved_nu_pressure_initializer_contract_20260726.md`, `docs/dd_112_core_v3_conserved_nu_pressure_initializer_20260726.md`, `logs/dd112_core_v3_conserved_nu_pressure_initializer_contract_20260726.json`, `logs/dd112_core_v3_conserved_nu_pressure_initializer_20260726.json`, `src/dynamic_distillation/core_v3/conserved_nu_pressure_initializer_numerical_v1.py`, `tools/run_core_v3_conserved_nu_pressure_initializer.py` | `tests/test_core_v3_conserved_nu_pressure_initializer_numerical_v1.py`; DD-112 executes once from contract commit `0f4dac3`. Both starts succeed, close all 52 constraints to at most `8.96e-11`, retain rank `52/52`, condition about `2.05e3`, interior physical states, exact conservation, provider compliance, and accepted efficiency. The common normalized endpoint difference is `1.880119e-6`, above the frozen `<1e-6` gate. The campaign is therefore retired without retry or tuning; zero-time audit and dynamics remain unauthorized. |
| `FR-019bd` | `docs/dd_113_core_v3_dd112_physical_equivalence_adjudication_contract_20260727.md`, `docs/dd_113_core_v3_dd112_physical_equivalence_adjudication_20260727.md`, `logs/dd113_core_v3_dd112_physical_equivalence_adjudication_contract_20260727.json`, `logs/dd113_core_v3_dd112_physical_equivalence_adjudication_20260727.json`, `src/dynamic_distillation/core_v3/dd112_physical_equivalence_adjudication_v1.py`, `tools/adjudicate_core_v3_dd112_physical_equivalence.py` | `tests/test_core_v3_dd112_physical_equivalence_adjudication_v1.py`; DD-113 passes once from contract commit `5d794bd`. All 15 frozen physical-equivalence metrics pass, every inherited DD-112 gate is unchanged and true, compositions are positive and normalized, and zero property/residual/Jacobian/solve/initializer/timestep/dynamic calls occur. The DD-094-start endpoint is selected canonically. One separately frozen zero-time audit may be drafted; DD-112 remains formally failed and dynamics remain unauthorized. |
| `FR-019be` | `docs/dd_114_core_v3_initializer_zero_time_audit_contract_20260727.md`, `docs/dd_114_core_v3_initializer_zero_time_audit_20260727.md`, `logs/dd114_core_v3_initializer_zero_time_audit_contract_20260727.json`, `logs/dd114_core_v3_initializer_zero_time_audit_20260727.json`, `src/dynamic_distillation/core_v3/initializer_zero_time_audit_v1.py`, `tools/audit_core_v3_initializer_zero_time.py` | `tests/test_core_v3_initializer_zero_time_audit_v1.py`; DD-114 passes once from contract commit `6e5538b`. All 52 live rows close below `2.02e-12`; all three Jacobians retain rank `52/52`, worst condition is `2.055e3`, spectrum change is `2.91e-6`, and colored/full difference is zero. Physical reproduction is exact, conservation and provider gates pass, and `6021` calls finish in `7.395 s`. One frozen first-step refinement contract may be drafted; no timestep or dynamics occurred. |
| `FR-019bf` | `docs/dd_115_core_v3_initializer_first_step_refinement_contract_20260727.md`, `docs/dd_115_core_v3_initializer_first_step_refinement_20260727.md`, `logs/dd115_core_v3_initializer_first_step_refinement_contract_20260727.json`, `logs/dd115_core_v3_initializer_first_step_refinement_20260727.json`, `src/dynamic_distillation/core_v3/conserved_nu_implicit_step_v1.py`, `tools/run_core_v3_initializer_first_step_refinement.py` | `tests/test_core_v3_conserved_nu_implicit_step_v1.py`; DD-115 executes once from contract commit `28ba8d9`. All three solves close below `2.74e-13`, remain rank `46/46`, physical, conservative, and provider compliant. Inventory, energy, pressure, temperature, and liquid-flow refinement pass, but algebraic (`3.20e-3`), vapor-flow (`2.08e-3`), and initial-rate (`1.74e-2` to `2.52e-2`) gates fail. Dynamic handoff stops without retry, tuning, or trajectory. |
| `FR-019bg` | `docs/dd_116_core_v3_initializer_handoff_term_audit_contract_20260727.md`, `docs/dd_116_core_v3_initializer_handoff_term_audit_20260727.md`, `logs/dd116_core_v3_initializer_handoff_term_audit_contract_20260727.json`, `logs/dd116_core_v3_initializer_handoff_term_audit_20260727.json`, `src/dynamic_distillation/core_v3/handoff_balance_audit_v1.py`, `tools/audit_core_v3_initializer_handoff_terms.py` | `tests/test_core_v3_handoff_balance_audit_v1.py`; DD-116 executes once from contract commit `f5117ed`. All term reconciliations, physical fields, ownership, provider, call, and wall checks pass, but the aggregate physical gate also compares DD-115's nominal exponential-step rate coordinate with its effective finite-step physical rate and fails at `3.08e-5`. The raw decision stops initializer work without rerun. The immutable evidence localizes the real transient to the energy-owned bottom-to-stripping vapor link. |
| `FR-019bh` | `docs/dd_117_core_v3_dd116_representation_gate_adjudication_contract_20260727.md`, `docs/dd_117_core_v3_dd116_representation_gate_adjudication_20260727.md`, `logs/dd117_core_v3_dd116_representation_gate_adjudication_contract_20260727.json`, `logs/dd117_core_v3_dd116_representation_gate_adjudication_20260727.json`, `src/dynamic_distillation/core_v3/dd116_gate_adjudication_v1.py`, `tools/adjudicate_core_v3_dd116_representation_gate.py` | `tests/test_core_v3_dd116_gate_adjudication_v1.py`; DD-117 passes once from contract commit `faf7850`. Every preserved DD-116 gate remains unchanged and true; actual physical fields, exponential endpoint inventories, effective rates, and reported coordinate mismatches reconstruct with zero error. No property/residual/Jacobian/solve/timestep calls occur. One property-free structural zero-rate feasibility audit is authorized. |
| `FR-019bi` | `docs/dd_118_core_v3_zero_rate_feasibility_20260727.md`, `logs/dd118_core_v3_zero_rate_feasibility_20260727.json`, `src/dynamic_distillation/core_v3/zero_rate_feasibility_v1.py`, `tools/audit_core_v3_zero_rate_feasibility.py` | `tests/test_core_v3_zero_rate_feasibility_v1.py`; DD-118 passes property-free. The three-component zero-rate DAE is `46 x 46`, rank `46`, while all retained initializer targets produce `52 x 46`, rank `46`, with six unmatched target rows. Four global component/energy targets must become diagnostics; two terminal holdups remain provisional scale selections. The generic two-component result also passes. One frozen live numerical zero-rate readiness contract is authorized; no root solve or dynamics is authorized. |
| `FR-019bj` | `docs/dd_119_core_v3_live_zero_rate_readiness_contract_20260727.md`, `docs/dd_119_core_v3_live_zero_rate_readiness_20260727.md`, `logs/dd119_core_v3_live_zero_rate_readiness_contract_20260727.json`, `logs/dd119_core_v3_live_zero_rate_readiness_20260727.json`, `src/dynamic_distillation/core_v3/zero_rate_readiness_v1.py`, `tools/audit_core_v3_zero_rate_readiness.py` | `tests/test_core_v3_zero_rate_readiness_v1.py`; DD-119 passes once from contract commit `a56d2ce`. Both terminal-augmented Jacobians are rank `46/46`, condition below `5.68e3`, spectrum-stable, and structurally exact; the canonical colored/full difference is zero. The DAE-only block is formally full rank but condition `2.54e13` to `3.76e13`, confirming two near-scale freedoms regularized by terminal holdups. All 7,113 calls pass in 3.144 seconds. One frozen zero-rate root contract is authorized; no solve or timestep occurred. |
| `FR-019bk` | `docs/dd_120_core_v3_zero_rate_root_contract_20260727.md`, `docs/dd_120_core_v3_zero_rate_root_20260727.md`, `logs/dd120_core_v3_zero_rate_root_contract_20260727.json`, `logs/dd120_core_v3_zero_rate_root_20260727.json`, `src/dynamic_distillation/core_v3/zero_rate_root_v1.py`, `tools/run_core_v3_zero_rate_root.py` | `tests/test_core_v3_zero_rate_root_v1.py`; DD-120 fails once from contract commit `67b9c51`. Both starts reach the same stationary physical endpoint within `1.36e-9`, and every gate except exact residual closure passes. Terminal rows close below `7.04e-12`, but the DAE residual stalls at `2.4486e-3` with left-null projection `7.6737e-3`. The terminal-scaled zero-rate root path is retired without variation; no timestep or dynamics occurred. |
| `FR-019bl` | `docs/dd_121_core_v3_terminal_gauge_invariance_contract_20260727.md`, `docs/dd_121_core_v3_terminal_gauge_invariance_20260727.md`, `logs/dd121_core_v3_terminal_gauge_invariance_contract_20260727.json`, `logs/dd121_core_v3_terminal_gauge_invariance_20260727.json`, `src/dynamic_distillation/core_v3/terminal_gauge_invariance_v1.py`, `tools/audit_core_v3_terminal_gauge_invariance.py` | `tests/test_core_v3_terminal_gauge_invariance_v1.py`; DD-121 passes once from contract commit `aa4ed13`. Provider repeatability is zero; all top perturbations leave the DAE vector exactly unchanged and bottom perturbations change it by at most `6.00e-14`. The applicable terminal row moves by exactly `+/-0.01`, compositions and bottom specific energy remain invariant, all 169 calls pass, and no Jacobian or solve occurs. One frozen controlled-terminal `48 x 48` contract may be drafted. |
| `FR-019bm` | `docs/dd_122_core_v3_controlled_terminal_zero_rate_contract_20260727.md`, `docs/dd_122_core_v3_controlled_terminal_zero_rate_20260727.md`, `logs/dd122_core_v3_controlled_terminal_zero_rate_contract_20260727.json`, `logs/dd122_core_v3_controlled_terminal_zero_rate_20260727.json`, `src/dynamic_distillation/core_v3/controlled_terminal_zero_rate_v1.py`, `tools/run_core_v3_controlled_terminal_zero_rate.py` | `tests/test_core_v3_controlled_terminal_zero_rate_v1.py`; DD-122 passes once from contract commit `76404ac`. Both starts reach one root within `8.74e-12`; residuals are below `1.38e-13`, rank is `48/48`, condition is about `5.94e3`, and every frozen gate passes. The solved outputs are `D=2255.740878` and `B=4887.233122 lbmol/h`. All 44,129 calls pass in 20.751 s. One separately frozen zero-rate dynamic-handoff contract may be drafted. |
| `FR-019bn` | `docs/dd_123_core_v3_controlled_terminal_dynamic_contract_20260727.md`, `logs/dd123_core_v3_controlled_terminal_dynamic_contract_20260727.json`, `src/dynamic_distillation/core_v3/controlled_terminal_dynamic_contract_v1.py`, `tools/audit_core_v3_controlled_terminal_dynamic_contract.py` | `tests/test_core_v3_controlled_terminal_dynamic_contract_v1.py`; DD-123 passes property-free. The C3/C4 ledger has 21 differential states/rates, 29 algebraics, 50 rows, and structural rank `50/50`; the generic two-component form is `40 x 40`, rank 40. Product outputs own the terminal balances, levels use explicit geometry and live-density semantics, PI memories are initialized from the accepted DD-122 outputs, and inherited conservation remains exact. One frozen live zero-time controller-handoff audit may be drafted; no property call, solve, timestep, or dynamics occurred. |
| `FR-019bo` | `docs/dd_124_core_v3_controlled_terminal_zero_time_contract_20260727.md`, `docs/dd_124_core_v3_controlled_terminal_zero_time_20260727.md`, `logs/dd124_core_v3_controlled_terminal_zero_time_contract_20260727.json`, `logs/dd124_core_v3_controlled_terminal_zero_time_20260727.json`, `src/dynamic_distillation/core_v3/controlled_terminal_zero_time_v1.py`, `tools/audit_core_v3_controlled_terminal_zero_time.py` | DD-124 aborts from contract commit `bd91364` before its first governed residual because the shared `pressure_numerical` keyword did not match the kernel interface. It produces no scientific result and performs no level reconstruction, Jacobian, solve, timestep, retry, or dynamics. |
| `FR-019bp` | `docs/dd_125_core_v3_controlled_terminal_zero_time_contract_20260727.md`, `docs/dd_125_core_v3_controlled_terminal_zero_time_20260727.md`, `logs/dd125_core_v3_controlled_terminal_zero_time_contract_20260727.json`, `logs/dd125_core_v3_controlled_terminal_zero_time_20260727.json`, `tests/test_core_v3_controlled_terminal_zero_time_v1.py` | DD-125 freezes the keyword-only correction and passes 178 Core V3 tests from contract commit `50f83e8`, but execution stops before returning a controlled-terminal residual because setpoint reconstruction uses unsupported evaluation kind `preparation`. No scientific gate is evaluated. Per the frozen hard stop, another corrective successor is not authorized. |
| `FR-019bq` | `docs/dd_126_core_v3_controlled_terminal_zero_time_preflight_20260805.md`, `logs/dd126_core_v3_controlled_terminal_zero_time_preflight_20260805.json`, `tools/preflight_core_v3_controlled_terminal_zero_time.py` | DD-126 passes the explicitly authorized development preflight. The live 50-row residual closes at `3.09e-11`, all four controller rows are exactly zero, physical levels are `0.469288` drum and `0.524957` sump, accepted `D/B` reproduce exactly, and all provider gates pass with 57 calls in `0.995 s` audited evaluation time. No Jacobian, solve, timestep, or dynamics occurs. One frozen live Jacobian contract is authorized. |
| `FR-019br` | `docs/dd_127_core_v3_controlled_terminal_jacobian_contract_20260805.md`, `docs/dd_127_core_v3_controlled_terminal_jacobian_20260805.md`, `logs/dd127_core_v3_controlled_terminal_jacobian_contract_20260805.json`, `logs/dd127_core_v3_controlled_terminal_jacobian_20260805.json`, `tools/audit_core_v3_controlled_terminal_zero_time.py` | DD-127 passes once from contract commit `ce7c1be`. The 50-row residual is `3.09e-11`, controller and repeatability errors are zero, both Jacobians retain rank `50/50`, worst condition is `2.498329e6`, spectrum change is `9.46e-5`, and colored/full difference is zero. Physical, pressure, conservation, provider, call, and wall gates pass with 4,537 calls in `1.793 s`. No solve or timestep occurs. One separately frozen controlled-terminal first-step contract is authorized. |
| `FR-019bs` | `docs/dd_128_core_v3_controlled_terminal_first_step_contract_20260805.md`, `docs/dd_128_core_v3_controlled_terminal_first_step_20260805.md`, `logs/dd128_core_v3_controlled_terminal_first_step_contract_20260805.json`, `logs/dd128_core_v3_controlled_terminal_first_step_20260805.json`, `src/dynamic_distillation/core_v3/controlled_terminal_implicit_step_v1.py`, `tools/run_core_v3_controlled_terminal_first_step.py` | `tests/test_core_v3_controlled_terminal_implicit_step_v1.py`; DD-128 passes once from contract commit `e3b2a36`. All three implicit roots close below `6.90e-13`; both Jacobians retain rank `50/50`, worst condition is `2.084358e5`, and spectrum change is `2.24e-4`. Stationary motion and coarse/refined disagreement remain at machine scale, exact discrete kinematics and all physical/provider gates pass, and 12,125 calls complete in `5.197 s`. One separately frozen moving-step contract is authorized; no perturbation or trajectory occurred. |
| `FR-019bt` | `docs/dd_129_core_v3_controlled_terminal_moving_step_contract_20260805.md`, `docs/dd_129_core_v3_controlled_terminal_moving_step_20260805.md`, `logs/dd129_core_v3_controlled_terminal_moving_step_contract_20260805.json`, `logs/dd129_core_v3_controlled_terminal_moving_step_20260805.json`, `tools/run_core_v3_controlled_terminal_moving_step.py` | DD-129 executes from contract commit `d5c9fcb` but aborts during final result serialization because `gates.controller_direction` is a NumPy boolean. No scientific metrics are retained and the moving gate is not classified. No retry or trajectory occurs. A successor requires explicit authorization and may correct only JSON-safe gate coercion while retaining the scientific contract unchanged. |
| `FR-019bu` | `docs/dd_130_core_v3_controlled_terminal_moving_step_jsonfix_contract_20260805.md`, `docs/dd_130_core_v3_controlled_terminal_moving_step_jsonfix_20260805.md`, `logs/dd130_core_v3_controlled_terminal_moving_step_jsonfix_contract_20260805.json`, `logs/dd130_core_v3_controlled_terminal_moving_step_jsonfix_20260805.json`, `tests/test_core_v3_controlled_terminal_moving_step_jsonfix.py`, `tools/run_core_v3_controlled_terminal_moving_step_jsonfix.py` | DD-130 executes once from contract commit `d149341`. The JSON-only correction works and every scientific, rank, direction, refinement, kinematic, physical, conservation, provider, and wall gate passes. The formal campaign fails only the frozen call limit: 24,165 versus `<16,000`. Of these, 23,520 are from 19 colored Jacobian rebuilds, while each complete residual uses 28 calls. No retry or trajectory occurs. The controlled handoff is stopped pending separately governed solver-efficiency work. |
| `FR-019bv` | `docs/dd_131_core_v3_modified_newton_efficiency_contract_20260805.md`, `docs/dd_131_core_v3_modified_newton_efficiency_20260805.md`, `logs/dd131_core_v3_modified_newton_efficiency_contract_20260805.json`, `logs/dd131_core_v3_modified_newton_efficiency_20260805.json`, `src/dynamic_distillation/core_v3/modified_newton_v1.py`, `tests/test_core_v3_modified_newton_v1.py`, `tools/audit_core_v3_modified_newton_efficiency.py` | DD-131 passes once from contract commit `9b20ee3`. The DD-130 call ledger reconstructs exactly; the deterministic one-Jacobian/one-factorization modified-Newton fixtures pass; generic structures retain rank `50/50` and `40/40`; and the pessimistic live-successor ceiling is 7,673 calls. No live provider, column residual, nonlinear column solve, timestep, or trajectory occurs. One separately frozen live efficiency proof against saved DD-130 endpoints is authorized. |
| `FR-019bw` | `docs/dd_132_core_v3_modified_newton_live_efficiency_contract_20260805.md`, `docs/dd_132_core_v3_modified_newton_live_efficiency_20260805.md`, `logs/dd132_core_v3_modified_newton_live_efficiency_contract_20260805.json`, `logs/dd132_core_v3_modified_newton_live_efficiency_20260805.json`, `tools/run_core_v3_modified_newton_live_efficiency.py` | DD-132 executes once from contract commit `66cf9f6`. All three roots converge in two corrections with one Jacobian each; residual, direction, refinement, kinematic, physical, conservation, provider, call, and wall gates pass. Calls fall to 3,809 from 24,165. The sole formal failure is the half2 transformed bottom-to-stripping vapor-flow coordinate at `1.017993e-7` versus the frozen `<1e-7` endpoint-reproduction limit. No rebuild, fallback, retry, or trajectory occurs. A zero-call physical-equivalence adjudication requires separate authorization. |
| `FR-019bx` | `docs/dd_133_core_v3_dd132_physical_equivalence_adjudication_contract_20260805.md`, `docs/dd_133_core_v3_dd132_physical_equivalence_adjudication_20260805.md`, `logs/dd133_core_v3_dd132_physical_equivalence_adjudication_contract_20260805.json`, `logs/dd133_core_v3_dd132_physical_equivalence_adjudication_20260805.json`, `src/dynamic_distillation/core_v3/dd132_physical_equivalence_adjudication_v1.py`, `tests/test_core_v3_dd132_physical_equivalence_adjudication_v1.py`, `tools/adjudicate_core_v3_dd132_physical_equivalence.py` | DD-133 passes once from contract commit `8c67656`. All 57 physical comparisons pass; the disputed half2 vapor flow differs by `1.017993196e-7` relative against `<2e-7`. Source gates and classifications remain unchanged, decoded states are physical, products match their coordinates, and zero property, residual, Jacobian, solve, timestep, or dynamic calls occur. One separately frozen modified-Newton short controlled-trajectory contract is authorized. |
| `FR-019by` | `docs/dd_134_core_v3_modified_newton_short_controlled_trajectory_contract_20260805.md`, `docs/dd_134_core_v3_modified_newton_short_controlled_trajectory_20260805.md`, `logs/dd134_core_v3_modified_newton_short_controlled_trajectory_contract_20260805.json`, `logs/dd134_core_v3_modified_newton_short_controlled_trajectory_20260805.json`, `src/dynamic_distillation/core_v3/controlled_terminal_trajectory_v1.py`, `tests/test_core_v3_controlled_terminal_trajectory_v1.py`, `tools/run_core_v3_modified_newton_short_controlled_trajectory.py` | DD-134 executes once from contract commit `ff74185`. The coarse and refined paths stop at their `t=7 s` and `t=3 s` roots when frozen-Jacobian line searches fail above the `<1e-8` residual gate. All physical, conservation, pressure, direction, provider, call, and wall evidence passes, and the two grids agree at common `t=3 s`. No retry or rebuild occurs. The one-frozen-Jacobian controlled-trajectory path is retired. |
| `FR-019bz` | `docs/dd_135_core_v3_dd134_globalization_audit_contract_20260805.md`, `docs/dd_135_core_v3_dd134_globalization_audit_20260805.md`, `logs/dd135_core_v3_dd134_globalization_audit_contract_20260805.json`, `logs/dd135_core_v3_dd134_globalization_audit_20260805.json`, `src/dynamic_distillation/core_v3/newton_globalization_audit_v1.py`, `tests/test_core_v3_newton_globalization_audit_v1.py`, `tools/audit_core_v3_dd134_globalization.py` | DD-135 executes once from contract commit `e00a237` and fails its replay-integrity gates. Fresh-process reconstruction changes the coarse/refined stalled residuals from `5.091822e-8`/`1.579973e-8` to `8.499349e-9`/`4.731911e-8`; stale-Jacobian conditions also miss their `1e-6` reproduction gate. All rebuilt matrices retain rank `50/50`, provenance passes, and 5,209 calls finish in `2.461 s`, but the line-search comparison is non-adjudicative because the numerical roots did not replay. DD-134 remains retired; no adaptive-refresh solver is authorized. |
| `FR-019ca` | `docs/dd_136_core_v3_dd134_residual_replay_contract_20260805.md`, `docs/dd_136_core_v3_dd134_residual_replay_20260805.md`, `logs/dd136_core_v3_dd134_residual_replay_contract_20260805.json`, `logs/dd136_core_v3_dd134_residual_replay_20260805.json`, `src/dynamic_distillation/core_v3/residual_replay_audit_v1.py`, `tests/test_core_v3_residual_replay_audit_v1.py`, `tools/audit_core_v3_dd134_residual_replay.py` | DD-136 passes once from contract commit `ca4f249`. All nine complete vectors per point are bit-for-bit identical across three distinct processes and all orders, and both norms reproduce DD-135 exactly. The DD-134 gaps remain exactly `4.241887e-8` and `3.151938e-8`. All provenance and integrity gates pass with 507 calls in `27.172 s`. This excludes DWSIM process noise and order dependence and identifies DD-134 as a non-replay-complete solver artifact. One separately frozen in-process failure-capture contract may be drafted; no solver or trajectory is authorized. |
| `FR-019cb` | `docs/dd_137_core_v3_captured_modified_newton_contract_20260805.md`, `docs/dd_137_core_v3_captured_modified_newton_20260805.md`, `logs/dd137_core_v3_captured_modified_newton_contract_20260805.json`, `logs/dd137_core_v3_captured_modified_newton_20260805.json`, `src/dynamic_distillation/core_v3/captured_modified_newton_v1.py`, `tests/test_core_v3_captured_modified_newton_v1.py`, `tools/audit_core_v3_captured_modified_newton.py` | DD-137 passes once from contract commit `c91ba83`. The 50-dimensional fixture converges exactly; all four failed Armijo trials, two unevaluated bounds rejections, and rank-zero evidence are retained; every capture array is read-only. Ordinary final identities are zero, while the deliberate shared-buffer mutation is detected exactly at `0.125` in residual and coordinates. Historical `modified_newton_v1` remains unchanged. One separately frozen live in-process failure-capture diagnostic contract may be drafted; no live model work is yet authorized. |
| `FR-019cc` | `docs/dd_138_core_v3_captured_failed_root_reconstruction_contract_20260805.md`, `docs/dd_138_core_v3_captured_failed_root_reconstruction_20260805.md`, `logs/dd138_core_v3_captured_failed_root_reconstruction_contract_20260805.json`, `logs/dd138_core_v3_captured_failed_root_reconstruction_20260805.json`, `tools/run_core_v3_captured_failed_root_reconstruction.py` | DD-138 executes once from contract commit `e2d1768` and formally fails only exact solver/evaluation coordinate identity. Residual identity is zero, all other capture/numerical/physical/provider gates pass, and 2,605 calls finish in `3.105 s`. The coordinate differences (`3.92e-9`/`1.95e-9`) occur in component-rate coordinates because the backward-Euler kernel intentionally reports the actual exponential-map rate instead of the nominal solver coordinate. Coarse reproduces a complete failure at `5.091893e-8`; refined converges to `1.686514e-10`. No endpoint advances. A static mapping adjudication requires separate authorization. |
| `FR-019cd` | `docs/dd_139_core_v3_dd138_rate_coordinate_adjudication_contract_20260805.md`, `docs/dd_139_core_v3_dd138_rate_coordinate_adjudication_20260805.md`, `logs/dd139_core_v3_dd138_rate_coordinate_adjudication_contract_20260805.json`, `logs/dd139_core_v3_dd138_rate_coordinate_adjudication_20260805.json`, `src/dynamic_distillation/core_v3/exponential_rate_coordinate_adjudication_v1.py`, `tests/test_core_v3_exponential_rate_coordinate_adjudication_v1.py`, `tools/adjudicate_core_v3_dd138_rate_coordinates.py` | DD-139 passes once from contract commit `6555da8` with zero model calls. Both complete evaluator vectors reconstruct exactly (`0.0` maximum difference); non-component and residual identities are also zero. DD-138 remains formally failed, but all its captured numerical evidence is accepted. Coarse genuinely fails near `5.091893e-8`, while refined converges to `1.686514e-10`. One frozen finite-difference Jacobian repeatability audit may be drafted; no solve or trajectory is authorized. |
| `FR-019ce` | `docs/dd_140_core_v3_dd138_jacobian_repeatability_contract_20260805.md`, `docs/dd_140_core_v3_dd138_jacobian_repeatability_20260805.md`, `logs/dd140_core_v3_dd138_jacobian_repeatability_contract_20260805.json`, `logs/dd140_core_v3_dd138_jacobian_repeatability_20260805.json`, `src/dynamic_distillation/core_v3/jacobian_repeatability_audit_v1.py`, `tests/test_core_v3_jacobian_repeatability_audit_v1.py`, `tools/audit_core_v3_dd138_jacobian_repeatability.py` | DD-140 executes once from contract commit `90c8a2c`. All same-process repetitions are bit-for-bit identical, but cross-process/order relative matrix differences reach `4.1843e-3`; DD-138 matrices do not reproduce. The largest affected entries approximately double when `h` halves, while all matrices remain rank `50/50` and below the condition limit. Solver work stops pending a bounded provider-derivative-state audit. No solve, correction, state advance, timestep, or trajectory occurred. |
| `FR-019cf` | `docs/dd_141_core_v3_thermo_provider_cache_resolution_contract_20260805.md`, `docs/dd_141_core_v3_thermo_provider_cache_resolution_20260805.md`, `logs/dd141_core_v3_thermo_provider_cache_resolution_contract_20260805.json`, `logs/dd141_core_v3_thermo_provider_cache_resolution_20260805.json`, `tests/test_core_v3_thermo_provider_cache_resolution_v1.py`, `tools/audit_core_v3_thermo_provider_cache_resolution.py` | DD-141 passes once from contract commit `ab5a9ce` with zero DWSIM calls. Distinct temperature, pressure, and composition states alias in the density cache; temperature also aliases in the Cp cache. Each pair incurs one miss/one hit, returns the first value twice, and reverses value with query order. DD-140's error ratios are `2.0000`/`2.0021` when `h` halves. An exact-state key correction is authorized. |
| `FR-019cg` | `docs/dd_142_exact_state_property_cache_key_correction_20260805.md`, `src/dynamic_distillation/thermo_provider_v1.py`, `tests/test_core_v3_thermo_provider_cache_resolution_v1.py`, `tests/test_thermo_provider_v1.py` | DD-142 replaces rounded density/Cp cache keys with exact normalized-state keys. Nearby states remain distinct, reverse order preserves state/value association, and exact repeated calls still hit cache. No model evaluation or solver work occurs. A separately frozen post-fix Jacobian repeatability audit is required. |
| `FR-019ch` | `docs/dd_143_core_v3_post_cachefix_jacobian_repeatability_contract_20260805.md`, `docs/dd_143_core_v3_post_cachefix_jacobian_repeatability_20260805.md`, `logs/dd143_core_v3_post_cachefix_jacobian_repeatability_contract_20260805.json`, `logs/dd143_core_v3_post_cachefix_jacobian_repeatability_20260805.json`, `tools/audit_core_v3_post_cachefix_jacobian_repeatability.py` | DD-143 passes once from contract commit `5c1ef05`. All 12 matrices are bit-for-bit identical across fresh process orders; worst step change is `2.33e-10`, spectrum change `2.42e-8`, all ranks are `50/50`, and condition remains below `7.29e5`. All 14,115 calls pass. DD-140's cache-driven derivative defect is eliminated. One separately frozen captured-trajectory successor is authorized. |
| `FR-019ci` | `docs/dd_144_core_v3_post_cachefix_captured_short_trajectory_contract_20260805.md`, `docs/dd_144_core_v3_post_cachefix_captured_short_trajectory_20260805.md`, `logs/dd144_core_v3_post_cachefix_captured_short_trajectory_contract_20260805.json`, `logs/dd144_core_v3_post_cachefix_captured_short_trajectory_20260805.json`, `tools/run_core_v3_post_cachefix_captured_short_trajectory.py` | DD-144 passes once from contract commit `7add86a`. Coarse/refined paths complete `10/10` and `20/20` roots with no rejected line-search fraction; worst residual is `1.09e-10`. Refinement, direction, kinematics, pressure, physicality, conservation, provider, call, wall, and immutable capture gates all pass. DD-134's failures are attributed to the corrected cache defect. One modest captured trajectory extension is authorized. |
| `FR-019cj` | `docs/dd_145_core_v3_extended_post_cachefix_captured_trajectory_contract_20260805.md`, `docs/dd_145_core_v3_extended_post_cachefix_captured_trajectory_20260805.md`, `logs/dd145_core_v3_extended_post_cachefix_captured_trajectory_contract_20260805.json`, `logs/dd145_core_v3_extended_post_cachefix_captured_trajectory_20260805.json`, `tools/run_core_v3_extended_post_cachefix_captured_trajectory.py` | DD-145 passes once from contract commit `283ada2`. Both 20-second paths complete all `20/20` and `40/40` roots with zero rejected line searches and worst residual `1.09e-10`. Endpoint refinement, direction, kinematics, pressure, physicality, conservation, provider, wall, call, and complete capture gates pass with 74,005 calls in `42.222 s`. One separately frozen longer captured trajectory is authorized, with an explicit revised call/evidence budget required. |
| `FR-019ck` | `docs/dd_146_core_v3_longer_post_cachefix_captured_trajectory_contract_20260805.md`, `docs/dd_146_core_v3_longer_post_cachefix_captured_trajectory_20260805.md`, `logs/dd146_core_v3_longer_post_cachefix_captured_trajectory_contract_20260805.json`, `logs/dd146_core_v3_longer_post_cachefix_captured_trajectory_20260805.json`, `tools/run_core_v3_longer_post_cachefix_captured_trajectory.py` | DD-146 passes once from contract commit `74f89ef`. Both 60-second paths complete all `60/60` and `120/120` roots with no rejected line-search or bound step and worst residual `1.09e-10`. Endpoint refinement, direction, kinematics, pressure, physicality, conservation, provider, wall, call, and complete capture gates pass with 221,845 calls in `116.259 s`. Brute-force captured extension is complete; one separately frozen trajectory-efficiency design is authorized before multi-minute operation. |
| `FR-019cl` | `docs/dd_147_core_v3_parallel_dwsim_jacobian_benchmark_contract_20260805.md`, `docs/dd_147_core_v3_parallel_dwsim_jacobian_benchmark_20260805.md`, `logs/dd147_core_v3_parallel_dwsim_jacobian_benchmark_contract_20260805.json`, `logs/dd147_core_v3_parallel_dwsim_jacobian_benchmark_20260805.json`, `src/dynamic_distillation/core_v3/parallel_colored_jacobian_v1.py`, `tests/test_core_v3_parallel_colored_jacobian_v1.py`, `tools/benchmark_core_v3_parallel_dwsim_jacobian.py` | DD-147 passes once from contract commit `09fdd30`. All nine matrices are bit-for-bit identical to DD-146, rank/condition are unchanged, call counts are exact, and requested process participation passes. Median Jacobian time falls from `0.813660 s` serial to `0.417453 s` with two workers and `0.225274 s` with four, giving `3.612x` four-worker speedup. Projected DD-146 wall is `42.126 s`. One separately frozen parallel colored-Jacobian integration contract is authorized. |
| `FR-019cm` | `docs/dd_148_core_v3_parallel_captured_first_root_contract_20260805.md`, `docs/dd_148_core_v3_parallel_captured_first_root_20260805.md`, `logs/dd148_core_v3_parallel_captured_first_root_contract_20260805.json`, `logs/dd148_core_v3_parallel_captured_first_root_20260805.json`, `tests/test_core_v3_parallel_colored_jacobian_v1.py`, `tools/run_core_v3_parallel_captured_first_root.py` | DD-148 passes once from contract commit `28f4058`. Serial and four-worker captured roots have exactly identical matrices, residuals, corrections, line-search trials, final coordinates, and solver decisions; the parallel result also reproduces DD-146 exactly. All four workers and 1,176 parallel calls pass. Integrated Jacobian time falls from `0.554112 s` to `0.213437 s` (`2.596x`). One separately frozen parallel captured short trajectory is authorized. |
| `FR-019cn` | `docs/dd_149_core_v3_parallel_captured_short_trajectory_contract_20260805.md`, `docs/dd_149_core_v3_parallel_captured_short_trajectory_20260805.md`, `logs/dd149_core_v3_parallel_captured_short_trajectory_contract_20260805.json`, `logs/dd149_core_v3_parallel_captured_short_trajectory_20260805.json`, `src/dynamic_distillation/core_v3/controlled_terminal_trajectory_v1.py`, `tests/test_core_v3_controlled_terminal_trajectory_v1.py`, `tools/run_core_v3_parallel_captured_short_trajectory.py` | DD-149 passes once from contract commit `0f8051b`. All 30 complete captures and accepted steps reproduce DD-144 exactly. One persistent four-worker pool supplies exactly 1,260 tasks and 35,280 worker calls, with all workers participating in every matrix. Total wall falls from `48.472 s` to `22.949 s` (`0.473x`, `2.112x` speedup), including `6.193 s` startup. One separately frozen modest parallel trajectory extension is authorized. |
| `FR-019co` | `docs/dd_150_core_v3_parallel_captured_longer_trajectory_contract_20260805.md`, `docs/dd_150_core_v3_parallel_captured_longer_trajectory_20260805.md`, `logs/dd150_core_v3_parallel_captured_longer_trajectory_contract_20260805.json`, `logs/dd150_core_v3_parallel_captured_longer_trajectory_20260805.json`, `tools/run_core_v3_parallel_captured_longer_trajectory.py` | DD-150 passes once from contract commit `837198e`. All 180 complete captures and accepted steps reproduce DD-146 exactly. One persistent four-worker pool supplies exactly 7,560 tasks and 211,680 worker calls, with all workers participating in every matrix. Governed wall falls from `116.259 s` to `66.596 s` (`0.573x`, `1.746x` speedup), including `6.343 s` startup. One separately frozen multi-minute parallel trajectory is authorized. |
| `FR-019cp` | `docs/dd_151_core_v3_parallel_captured_multiminute_trajectory_contract_20260805.md`, `docs/dd_151_core_v3_parallel_captured_multiminute_trajectory_20260805.md`, `logs/dd151_core_v3_parallel_captured_multiminute_trajectory_contract_20260805.json`, `logs/dd151_core_v3_parallel_captured_multiminute_trajectory_20260805.json`, `tests/test_core_v3_parallel_capture_evidence_v1.py`, `tools/run_core_v3_parallel_captured_short_trajectory.py`, `tools/run_core_v3_parallel_captured_multiminute_trajectory.py` | DD-151 executes once from contract commit `d7e069c` and formally fails only the two wall gates. Both paths complete all `300/600` roots; residual, refinement, controller, kinematic, pressure, physicality, conservation, provider, rank, condition, exact work-count, worker-ownership, immutable-capture, and DD-146 prefix-equivalence gates pass. Governed wall is `476.349 s` versus `<332.979 s` and `<330 s`. Full 91.3 MB replay evidence is retained. Further live extension stops pending zero-call timing analysis. |
| `FR-019cq` | `docs/dd_152_core_v3_multiminute_timing_audit_contract_20260806.md`, `docs/dd_152_core_v3_multiminute_timing_audit_20260806.md`, `logs/dd152_core_v3_multiminute_timing_audit_contract_20260806.json`, `logs/dd152_core_v3_multiminute_timing_audit_20260806.json`, `tests/test_core_v3_multiminute_timing_audit.py`, `tools/audit_core_v3_multiminute_timing.py` | DD-152 passes once from contract commit `addd248` with zero model/provider calls. Jacobian construction accounts for `88.1%` of positive excess wall; mean Jacobian time rises from `0.2252` to `0.4224 s/root`. The refined path increases `1.373x` with root-order correlation `0.953`, passing the frozen history-dependence gate. A saved-state persistent-versus-fresh-pool efficiency probe is authorized; cache/backend causality remains unproven. |
| `FR-019cr` | `docs/dd_153_core_v3_worker_lifetime_efficiency_probe_contract_20260806.md`, `docs/dd_153_core_v3_worker_lifetime_efficiency_probe_20260806.md`, `logs/dd153_core_v3_worker_lifetime_efficiency_probe_contract_20260806.json`, `logs/dd153_core_v3_worker_lifetime_efficiency_probe_20260806.json`, `tests/test_core_v3_worker_lifetime_efficiency_probe.py`, `tools/probe_core_v3_worker_lifetime_efficiency.py` | DD-153 passes once from contract commit `e926c41`. All 12 fresh matrices reproduce DD-151 bit-for-bit with exact work/process counts. Every checkpoint exceeds `1.25x`; median aged/fresh is `1.689x`, while fresh late/early is only `1.064x` coarse and `0.983x` refined. Persistent-worker lifetime slowdown is confirmed. A pool-renewal cadence benchmark is authorized; cache versus other backend state remains unresolved. |
| `FR-019cs` | `docs/dd_154_core_v3_pool_renewal_cadence_contract_20260806.md`, `docs/dd_154_core_v3_pool_renewal_cadence_20260806.md`, `logs/dd154_core_v3_pool_renewal_cadence_contract_20260806.json`, `logs/dd154_core_v3_pool_renewal_cadence_20260806.json`, `tests/test_core_v3_pool_renewal_cadence.py`, `tools/benchmark_core_v3_pool_renewal_cadence.py` | DD-154 executes once from contract commit `2950f3d` with zero model/provider calls. All source, calibration, selection, uncertainty, zero-call, and wall gates pass. Every slope scenario selects `300` roots, but nominal improvement is only `11.86%`, below the frozen `20%` requirement. Pool renewal and another trajectory are not authorized. |
| `FR-019ct` | `docs/dd_155_core_v3_inworker_reset_efficiency_contract_20260806.md`, `docs/dd_155_core_v3_inworker_reset_efficiency_20260806.md`, `logs/dd155_core_v3_inworker_reset_efficiency_contract_20260806.json`, `logs/dd155_core_v3_inworker_reset_efficiency_20260806.json`, `tests/test_core_v3_inworker_reset_efficiency.py`, `tools/probe_core_v3_inworker_reset_efficiency.py` | DD-155 executes once from contract commit `22f3ba3`. All 187 matrices reproduce DD-151 bit-for-bit with exact work, calls, ownership, and wall. Aging reproduces at `1.373x` fresh, but cache clear, provider rebuild, and DWSIM object reinitialization remain `1.400x`, `1.317x`, and `1.403x` fresh. The recovery gate fails; no reset API or trajectory is authorized. |
| `FR-019cu` | `docs/dd_156_core_v3_exact_thermo_memoization_contract_20260806.md`, `docs/dd_156_core_v3_exact_thermo_memoization_20260806.md`, `logs/dd156_core_v3_exact_thermo_memoization_contract_20260806.json`, `logs/dd156_core_v3_exact_thermo_memoization_20260806.json`, `tests/test_core_v3_exact_thermo_memoization.py`, `tools/benchmark_core_v3_exact_thermo_memoization.py` | DD-156 passes once from contract commit `37ffd8b`. All three matrices reproduce DD-151 bit-for-bit with exact work and cache accounting. Cold memoization hits `69.56%` and runs `2.281x` faster; warm memoization hits `83.25%` and runs `3.069x` faster. A bounded production implementation plus saved-state equivalence benchmark is authorized; no trajectory is authorized. |
| `FR-019cv` | `src/dynamic_distillation/thermo_provider_v1.py`, `tools/run_core_v3_parallel_captured_short_trajectory.py`, `docs/dd_157_core_v3_production_exact_memoization_contract_20260806.md`, `docs/dd_157_core_v3_production_exact_memoization_20260806.md`, `logs/dd157_core_v3_production_exact_memoization_contract_20260806.json`, `logs/dd157_core_v3_production_exact_memoization_20260806.json`, `tests/test_core_v3_exact_thermo_memoization_production.py`, `tests/test_core_v3_production_exact_memoization_proof.py`, `tools/prove_core_v3_production_exact_memoization.py` | DD-157 passes once from contract commit `8de7d17`. Coarse/refined production matrices reproduce DD-151 exactly with unique cold epochs, `69.56%` hits, and `1.877x/1.862x` speedups. All source, work, memo, ownership, representativeness, scientific, and wall gates pass. One separately frozen short-trajectory integration proof is authorized. |
| `FR-019cw` | `docs/dd_158_core_v3_memoized_captured_short_trajectory_contract_20260806.md`, `docs/dd_158_core_v3_memoized_captured_short_trajectory_20260806.md`, `logs/dd158_core_v3_memoized_captured_short_trajectory_contract_20260806.json`, `logs/dd158_core_v3_memoized_captured_short_trajectory_20260806.json`, `tests/test_core_v3_memoized_captured_short_trajectory.py`, `tools/run_core_v3_memoized_captured_short_trajectory.py`, `tools/run_core_v3_parallel_captured_short_trajectory.py` | DD-158 passes once from contract commit `ae2bde8`. All 30 complete captures and accepted states reproduce DD-149 exactly. Every root records `818/1,176` exact hits (`69.56%`). Trajectory wall falls from `7.413` to `5.338 s` (`0.720x`), and total wall is `21.950 s`. One separately frozen longer memoized trajectory is authorized. |
| `FR-014f` | `src/dynamic_distillation/thermo_backend_factory_v1.py`, `src/dynamic_distillation/dynamic_run_scaffold_v1.py`, `docs/issue_log.md` (`DD-059`) | Partial; Clapeyron fail-fast exists, DWSIM systematic-failure fail-fast remains open. |
| `FR-035` | `src/dynamic_distillation/run_report_v1.py`, `docs/run_reports.md` | `tests/test_run_report_v1.py` |
| `FR-036` | `src/dynamic_distillation/dynamic_run_scaffold_v1.py`, `tools/evaluate_initialization_dynamic_gate.py` | `tests/test_dynamic_run_scaffold_v1.py`, checkpoint reload-gate tests where available. |
| `FR-001`..`FR-004` | `src/dynamic_distillation/excel_case_loader_v1.py`, `src/dynamic_distillation/column_spec_builder_v1.py` | `tests/test_excel_case_loader_v1.py`, `tests/test_column_spec_builder_v1.py`, `tests/test_excel_case_validator_v1.py` |
| `FR-006`..`FR-009`, `FR-026` | `src/dynamic_distillation/state_vector_layout_v1.py`, `src/dynamic_distillation/dynamic_run_scaffold_v1.py` | `tests/test_state_vector_layout_v1.py`, `tests/test_dynamic_run_scaffold_v1.py` |

## 6. Notes and Limits
- This document is implementation-derived and intentionally reflects current behavior, not a future roadmap.
- Architecture requirements `FR-019a` through `FR-019aw`, `FR-037`, and `FR-037a` were identified and refined by DD-060 through DD-106. DD-070 retires checkpoint repair. DD-071 and DD-072 establish full-system structural and numerical readiness. DD-073 stops at local closure, DD-074 retires manual staged continuation, and DD-075 retires the prior direct conserved formulation. DD-076 selects the new equilibrium-DAE family. DD-077 through DD-081 pass the bounded structural, source, one-volume, and five-volume pre-solve gates. DD-082 retires the prescribed-vapor specification after a common residual floor and active bound. DD-083 and DD-084 establish a distinct, full-rank, conservative `37 x 37` energy-owned ledger. DD-085 finds a common machine-precision root but rejects its fixed-duty total-condenser boundary. DD-086 and DD-087 establish and numerically validate the solved-duty saturated-liquid `40 x 40` successor. DD-088 finds a common machine-precision physical root but formally rejects it because one precommitted TP/forced-phase consistency-floor criterion fails. DD-089 does not revise that decision; it diagnoses the mixed-basis and endpoint effects. DD-090 passes a prospective provider-authority contract. DD-091 establishes the separately versioned, provider-tagged Core V3 `40 x 40` structural architecture. DD-092 independently implements and live-validates that Core V3 residual at two frozen states. DD-093 is retired after a post-solve reporting failure, and the user-authorized reporting-only DD-094 successor preserves the mathematical campaign and accepts one common, interior, provider-governed reduced root. DD-095 establishes a full-rank `38 x 38` structural dynamic ledger. DD-096 passes the live storage-gradient and leading-Jacobian audit with rank `38/38`, condition about `35.4`, stable finite differences, exact registered coupling, and machine-precision conservation. DD-097 passes zero-rate recovery and both independent refined backward-Euler steps with full rank, condition below `35.2`, machine-level stationary motion, physical endpoints, discrete conservation, and exact provider ownership. DD-098 passes all root-hold and nonzero refined trajectory gates: global accumulation is exact, every endpoint is physical and conservative, and refined states agree. DD-099 corrects trial-state storage ownership, validates 17-color backward-Euler Jacobians, and reduces mean calls per endpoint by `13.56x` with exact colored/uncolored agreement. DD-100 passes 35 repeated stationary and moving endpoints with exact external accumulation, grid refinement, and accepted efficiency. DD-101 establishes the full-rank `42 x 42` algebraic pressure-layer structure while retaining energy-owned vapor flow. DD-102 validates its live numerical ownership, DD-103 retires algebraic-only pressure repair after the frozen-inventory steady system reaches a repeatable nonzero residual floor, DD-104 restores a square full-rank pressure-enabled implicit ledger, DD-105 stops timestep-dependent pressure handoff, and DD-106 defines the full-rank conservation-constrained initializer selection problem.
- DD-102 extends that sequence through `FR-019as`: the live `42 x 42` pressure-layer audit passes at both frozen states and supersedes DD-101's numerical-audit-only authorization. One separately frozen pressure-layer steady-root contract is now authorized, subject to an explicit reduced bottom-link geometry decision.
- DD-103 extends that sequence through `FR-019at`: the terminal dry-only geometry decision removes the false sump-as-tray liquid head, but the frozen `42 x 27` zero-rate solve reaches a reproducible `8.947e-3` residual floor despite full algebraic column rank, good conditioning, conservation, provider compliance, and an almost-closed hydraulic pressure profile. Algebraic-only pressure repair is retired. A property-free pressure-enabled implicit-DAE structural contract is the only authorized successor; no numerical step or integration is authorized.
- DD-104 extends that sequence through `FR-019au`: restoring 15 inventory rates beside the 27 algebraic coordinates gives a square, full-rank `42 x 42` pressure-enabled implicit ledger. Terminal dry-only ownership, tray liquid-head ownership, exact conservation, and the complete backward-Euler dependency chain pass structurally with a conflict-free 20-color pattern. One frozen live consistent-rate and leading-Jacobian audit may be drafted; stepping and integration remain unauthorized.
- DD-105 extends that sequence through `FR-019av`: exact pressure-dependent backward-Euler storage produces machine-precision, full-rank first-step roots on both frozen grids, but the endpoints are strongly timestep dependent. Pressure-enabled stepping and trajectories are stopped. A successor must solve pressure-enabled consistent initialization with explicit conserved-state and terminal-inventory ownership.
- DD-106 extends that sequence through `FR-019aw`: all 42 pressure-enabled DAE rows remain exact while three component totals, total stored energy, and two terminal total-holdup constraints define a full-row-rank `48 x 57` equality ledger. Its normalized selection objective yields a full-rank `105 x 105` structural KKT candidate without timestep dependence. One frozen live numerical initializer contract may be drafted; execution and dynamics remain unauthorized.
- DD-107 extends that sequence through `FR-019ax` and supersedes DD-106's live-contract authorization: nonzero inventory rates with algebraic pressure require an exact continuous pressure-aware `dU/dt`, but DD-106 contains only a discrete backward-Euler storage definition and no independent energy state/rate ownership. Live DD-106 execution is stopped before property calls. One property-free conserved-`N/U` pressure-DAE ownership audit is authorized.
- DD-108 extends that sequence through `FR-019ay`: an all-five-volume independent-energy assembly is structurally singular because fixed top pressure and bubble equilibrium already determine top storage. Assigning independent `U/dU` only to the four lower algebraic-pressure volumes yields a generic, conservative, full-rank `46 x 46` contract. One frozen live leading-Jacobian and constraint-manifold numerical contract is authorized; no initializer execution or dynamics is authorized.
- DD-109 extends that sequence through `FR-019az`: two complete conserved-energy pressure states, exact storage/rate scales, two 15-color finite-difference steps, one full canonical Jacobian comparison, provider ownership, physical gates, and hard efficiency stops are frozen before the first live evaluation. One execution of the numerical rank/manifold audit is authorized only after commit; no nonlinear solve, initializer, repair, or timestep is authorized.
- DD-109 executes once from commit `99b88fd` and formally fails two physical-reporting gates. Every numerical, manifold, conservation, provider, pressure, and efficiency gate passes, including rank `46/46`, storage rank `4/4`, worst condition `4.30341e5`, exact colored/full agreement, `5993` calls, and `3.564 s` wall time. The failed gates incorrectly require finite positive tray heights for the reflux drum and combined reboiler/sump, whose governing property record intentionally uses `NaN` because they are not Francis tray volumes. The result is preserved without rerun or reclassification. A successor requires an explicit governance decision.
- DD-110 extends that sequence through `FR-019ba` under the user's explicit reporting-only authorization. It freezes a static adjudicator against the exact DD-109 hashes, limits replacement to the two failed physical gates, validates geometry only where ownership exists, requires the terminal sentinels, and inherits every numerical gate unchanged. Contract preparation performs no property, residual, Jacobian, solve, initializer, or timestep evaluation. One static execution is authorized after commit.
- DD-110 passes once from commit `464e568`: both ownership-aware replacement gates and the new terminal-sentinel gate pass, every inherited DD-109 gate is unchanged, and the source evidence hash is preserved. The static action makes zero property, residual, Jacobian, solve, initializer, or timestep calls. The DD-109 numerical rank/manifold evidence is therefore accepted prospectively through the authorized reporting-only exception. One conserved-`N/U` pressure-consistent initializer contract may be drafted and frozen; its execution and dynamics remain unauthorized.
- DD-111 extends that sequence through `FR-019bb`: four independent lower `U/dU` pairs and four live storage closures are incorporated into the exact initializer manifold while top energy remains derived at fixed pressure. The resulting 52 constraints have full structural row rank over 65 primals, and the normalized quadratic selection KKT pattern is full rank `117/117`. The generic two-component case also passes. One live constrained-initializer contract may now be drafted and frozen; no property call or initializer execution occurred.
- DD-112 extends that sequence through `FR-019bc`: the exact DD-094/DD-096 conservation targets, two independent pressure/storage starts, transformed bounds, `1/10/1` objective, one SLSQP solver, 21-color derivative strategy, two endpoint steps, full canonical cross-check, and all numerical/physical/provider/efficiency hard stops are frozen before execution. Preparation performs no property evaluation or solve. The campaign may execute once only after commit; no retry or dynamics is authorized.
- DD-112 executes once from commit `0f4dac3` and formally fails only the frozen common-solution gate. Both starts converge to objective `2.2902290774`, close all 52 constraints below `9e-11`, retain rank `52/52` and condition about `2.05e3`, and pass KKT, bounds, pressure, physicality, conservation, provider, call, and wall gates. Their normalized endpoint difference is `1.880119e-6` versus the required `<1e-6`; the largest difference lies in a transformed reflux-drum bubble-composition coordinate, while physical pressure differs by only `5.56e-8 psia`. Per `FR-019bc`, the campaign is retired without retry or gate relaxation. A zero-time audit, timestep, and dynamics are not authorized.
- DD-113 extends that sequence through `FR-019bd` under the user's explicit static-adjudication authorization. It freezes the exact DD-112 hashes, preserves every source gate except the failed transformed-coordinate comparison, reconstructs saved liquid/vapor/bubble compositions without property calls, compares all material, energy, pressure, temperature, flow, product, duty, and objective fields on declared physical scales, and selects a canonical endpoint deterministically. Contract preparation performs no property, residual, Jacobian, solve, initializer, timestep, or dynamic evaluation. One static execution is authorized only after commit.
- DD-113 passes once from commit `5d794bd`: all 15 physical-equivalence metrics pass, including inventory `1.706e-6` scaled, liquid composition `5.540e-7`, vapor composition `4.844e-7`, pressure `5.564e-9` scaled, and temperature `5.303e-5 F`. Every inherited DD-112 gate is unchanged and true, reconstructed compositions are physical, and the static adjudication makes zero property/residual/Jacobian/solve/initializer/timestep/dynamic calls. The lower-objective DD-094-start endpoint is canonical. One separately frozen zero-time audit may be drafted; no timestep or dynamics is authorized.
- DD-114 extends that sequence through `FR-019be`: the exact canonical DD-113 endpoint, source and coordinate hashes, all 52 live constraints, two colored Jacobian steps, one full cross-check, rank/condition/spectrum/registry gates, fresh physical reproduction tolerances, provider ownership, and `50000`-call/`180 s` limits are frozen before execution. Preparation makes no property, residual, Jacobian, solve, initializer, timestep, or dynamic call. One live zero-time audit is authorized only after commit.
- DD-114 passes once from commit `6e5538b`: all 52 live constraints close below `2.02e-12`; both colored and the full Jacobian retain rank `52/52` with worst condition `2.055e3`; spectrum change is `2.91e-6`; colored/full difference and saved physical reproduction error are zero. Pressure, physicality, conservation, provider, call, and wall gates pass with `6021` calls in `7.395 s`. The endpoint is accepted as a consistent dynamic initial condition with nonzero equation-owned rates, not as a steady state. One separately frozen first-step refinement contract may be drafted; no timestep or dynamics occurred.
- DD-115 executes once from frozen commit `28ba8d9` and formally fails. All three implicit roots close below `2.74e-13`; every Jacobian is rank `46/46` with worst condition `7.45e5`; physicality, pressure ordering, exact kinematics, conservation, provider ownership, call count, and wall time pass. Coarse/refined inventory (`8.12e-5`), stored energy, pressure (`6.47e-4 psi`), temperature (`2.33e-3 F`), and liquid flow pass. Algebraic (`3.20e-3`) and vapor-flow (`2.08e-3`) refinement fail, while first-step component/energy rates differ from the accepted zero-time rates by `2.39e-2`/`1.74e-2` even on the half-step. The discrepancy is led by the generic stripping-to-feed vapor link and bottom-volume rates. Stop this initializer dynamic handoff without timestep, tolerance, scale, or solver tuning; no short trajectory is authorized.
- DD-116 executes once from frozen commit `f5117ed` and formally fails one aggregate gate. All three term ledgers reconcile saved component and energy rates below `2.74e-13` scaled; pressure, temperature, flows, products, duty, ownership, provider, call, and wall checks pass exactly. The failed `component_coordinate_reproduction` metric compares DD-115's nominal logarithmic step coordinate with its separately saved effective finite-step rate, giving `3.08e-5`; it is not a failed physical state or balance reconstruction. The substantive transient is led by a `437.64 lbmol/h` rise in the energy-owned bottom-to-stripping vapor link, accounting for `-307.70 lbmol/h` of the bottom n-butane rate change and `-0.956 MMBTU/h` of its energy-rate change. Preserve the raw stop without rerun. Any static reporting-only adjudication requires explicit authorization; no structural successor or dynamics is presently authorized.
- DD-117 passes once from commit `faf7850`: every inherited DD-116 gate except the replaced aggregate remains unchanged and true; all actual physical reproduction metrics are zero; and both refined endpoint inventories, effective finite-step rates, and reported nominal/effective coordinate mismatches reconstruct with exactly zero error. The adjudication makes zero property, residual, Jacobian, solve, initializer, timestep, controller, or trajectory calls and changes no DD-116 evidence. DD-116's balance-explained transient is accepted. One property-free structural zero-rate feasibility audit is authorized; numerical initialization and dynamics remain unauthorized.
- DD-118 extends that sequence through `FR-019bi` and passes without live calls. For three components, fixing 19 rates at zero leaves a square `46 x 46`, rank-46 DAE core. Preserving all six DD-112 inventory/energy/terminal targets instead gives `52 x 46`, rank 46, and the augmented original system is `71 x 65`, rank 65; the unmatched rows are exactly the three global component totals, global stored energy, and two terminal total holdups. The generic two-component case has the expected five-row surplus. The corrected successor shall demote the `Nc+1` inherited global totals to diagnostics and provisionally retain the two terminal holdups as physical scale selections. One frozen live numerical residual/rank/compatibility audit is authorized before any root solve; no initializer execution or dynamics is authorized.
- DD-119 extends that sequence through `FR-019bj`: the DD-112 canonical and DD-115 refined one-second states, exact zero-rate projection, `48 x 46` terminal-scaled residual, two 20-color finite-difference steps, one full canonical cross-check, numerical rank/compatibility rules, provider ownership, and efficiency hard stops are frozen before execution. Preparation performs no property evaluation, solve, or timestep. The readiness audit may execute once only after commit.
- DD-119 executes once from commit `a56d2ce` and passes all gates. Both DAE-only matrices are formally rank `46/46` but nearly singular at condition `2.54e13` to `3.76e13`; the two terminal holdup rows regularize the augmented `48 x 46` matrices to condition below `5.68e3`. Spectrum change is below `4.73e-7`, colored/full difference is zero, conservation and provider gates pass, and `7113` calls finish in `3.144 s`. Residuals remain about `0.06` because no root solve occurred. One frozen zero-rate root contract is authorized to test exact compatibility of the overdetermined terminal-scaled system.
- DD-120 extends that sequence through `FR-019bk`: the two DD-119 states, unchanged `48 x 46` residual, transformed bounds, one 20-color bounded trust-region least-squares solver, strict all-row and common-root gates, two endpoint Jacobian steps, one full cross-check, physical/provider gates, and hard efficiency stops are frozen before execution. Preparation performs no property evaluation or solve. The campaign may execute once only after commit.
- DD-120 executes once from commit `67b9c51` and fails only exact residual closure. Both starts terminate successfully at the same endpoint within `1.36e-9`; terminal rows close below `7.04e-12`, optimality is below `5.60e-9`, and every rank, condition, spectrum, coloring, bounds, pressure, physical, conservation, provider, call, and wall gate passes. The DAE residual stalls at `2.4486e-3` with left-null projection `7.6737e-3`. The inherited terminal targets are therefore not exactly compatible with the current zero-rate DAE under the frozen specifications. This terminal-scaled path is retired without retry or target variation.
- DD-121 extends that sequence through `FR-019bl`: homogeneous `+/-1%` perturbations prove that drum and sump amount are gauge selections for the zero-rate DAE. The DAE vector is invariant to at worst `6.00e-14`, only the selected terminal row changes by exactly `+/-0.01`, and provider repeatability is exact. This narrows DD-120's interpretation: incompatibility was established only for fixed terminal amounts combined with fixed product rates. A square successor may retain the terminal amounts while releasing distillate and bottoms rates as level-controller outputs.
- DD-122 extends that sequence through `FR-019bm`: adding only positive `D/B` outputs produces a structurally full-rank `48 x 48` system. Two materially different starts converge to the same interior machine-precision root within `8.74e-12`; all numerical, physical, conservation, provider, and efficiency gates pass. The stationary level-control outputs are `D=2255.740878` and `B=4887.233122 lbmol/h`, whose sum matches feed. This is the first accepted pressure-enabled Core V3 zero-rate initial condition. A separately frozen dynamic handoff may now be designed, but imported throughput remains a diagnostic unless another valid operating degree of freedom owns it.
- DD-123 extends that sequence through `FR-019bn`: the accepted stationary state now has an explicit dynamic terminal-control owner. Two geometry-based levels, two PI-memory states/rates, and two positive product outputs extend the inherited conserved DAE to a square, structurally full-rank `50 x 50` ledger; the generic two-component form also passes. The DD-122 product outputs initialize the controller memories bumplessly. This is a structural authorization only: the physical setpoint levels must be reconstructed and the live numerical leading system must pass a separately frozen zero-time audit before any timestep.
- DD-124 and DD-125 extend the record through `FR-019bo` and `FR-019bp` but do not produce scientific controller-handoff evidence. Both stop at explicit Python interface guards before returning a controlled-terminal residual. DD-124's keyword-only successor is preserved, and DD-125's second implementation abort activates the precommitted hard stop. The accepted DD-122 stationary root and DD-123 structural controller ledger remain valid; live level reconstruction, numerical controller rank, and dynamic handoff remain unresolved.
- DD-126 extends the record through `FR-019bq` under the user's explicit authorization to reopen only the interface stop. Using the supported residual label returns a complete live 50-row vector at `3.09e-11`; controller rows are exactly zero, drum/sump levels are `46.93%`/`52.50%`, product outputs are bumpless, and all 57 provider calls pass. This resolves the DD-124/DD-125 plumbing uncertainty but remains a development preflight. One separately frozen `50 x 50` live Jacobian audit is now authorized before any timestep.
- DD-127 extends the record through `FR-019br`: the frozen live leading-system audit passes at rank `50/50`, worst condition `2.498329e6`, spectrum change `9.46e-5`, and exact colored/full agreement. The stationary controlled residual, physical levels, product outputs, conservation, and provider gates all pass. One separately frozen implicit root-hold step may now be drafted; a perturbed step or trajectory remains unauthorized.
- DD-128 extends the record through `FR-019bs`: the controlled-terminal backward-Euler system preserves the accepted stationary root on `1.0 s` and `0.5 + 0.5 s` grids. All roots, finite-step rank, conditioning, refinement, exact kinematics, physicality, conservation, and provider gates pass. This removes the artificial-startup concern at the root itself. One separately frozen moving-step contract may now be drafted; a trajectory remains unauthorized.
- DD-129 extends the record through `FR-019bt` but yields no scientific moving-step result. All requested numerical paths reach result assembly, where a NumPy boolean fails standard JSON serialization. The metrics are not retained, so the campaign is neither passed nor failed. DD-128 remains the accepted boundary and no retry or trajectory is authorized without explicit governance of a JSON-coercion-only successor.
- DD-130 extends the record through `FR-019bu` and resolves DD-129's reporting defect without changing its scientific contract. The moving response, all three roots, refined-grid agreement, rank, conditioning, direction, exact kinematics, physicality, and conservation pass. The sole formal failure is 24,165 provider calls against the frozen `<16,000` cap. The ledger localizes `97.3%` of calls to repeated finite-difference Jacobian reconstruction. No trajectory is authorized; the next useful work is a separately governed, zero-call modified-Newton/Jacobian-reuse design.
- DD-131 extends the record through `FR-019bv`: a property-free modified-Newton design reconstructs the DD-130 ledger exactly and replaces 19 prospective Jacobian rebuilds with one frozen Jacobian/factorization per root. Linear and nonlinear fixtures, generic full-rank structures, no-clipping/no-fallback rules, and a 7,673-call worst-case budget all pass without a live provider or column evaluation. One separately frozen live efficiency proof may now be drafted against the immutable DD-130 endpoints; trajectories remain unauthorized.
- DD-132 extends the record through `FR-019bw` and proves the modified-Newton efficiency objective: one Jacobian and two corrections per root reduce calls by `84.2%` to 3,809 while every scientific and physical gate passes. The formal campaign nevertheless fails one transformed-coordinate reproduction metric at `1.017993e-7` against `<1e-7`. Preserve the no-retry result. A static physical-equivalence adjudication requires separate authorization; trajectories remain unauthorized.
- DD-133 extends the record through `FR-019bx`: a checksum-guarded static adjudication decodes and compares all three immutable DD-130/DD-132 physical endpoints. All 57 metrics pass, including the disputed half2 vapor flow at `1.017993196e-7` relative against `<2e-7`; no source evidence changes and zero model calls occur. DD-130 and DD-132 retain their formal classifications, while one separately frozen modified-Newton short controlled-trajectory contract is now authorized.
- DD-134 executes the frozen `FR-019by` contract once. The first step reproduces DD-132 and all completed states remain physical, conservative, pressure ordered, correctly directed, and mutually grid-consistent at common `t=3 s`. The coarse and refined paths nevertheless stop at `t=7 s` and `t=3 s` when their frozen-Jacobian line searches cannot reduce residuals below `1e-8`. This retires the one-frozen-Jacobian controlled-trajectory path without retry; the failure is numerical globalization, not demonstrated model divergence.
- DD-135 executes the frozen `FR-019bz` audit once and stops on replay integrity. Neither saved DD-134 stalled residual nor stale-Jacobian condition reproduces in the shortened fresh-process reconstruction. Although all rebuilt matrices remain full rank and every reconstructed direction appears acceptable, those results apply to different numerical roots and cannot adjudicate stale-Jacobian causality. No state advances and DD-134 remains retired. A successor must first establish same-point residual/provider repeatability and a complete replay artifact.
- DD-136 executes the frozen `FR-019ca` audit once and passes decisively. All 18 complete residual vectors are bit-for-bit repeatable across three distinct processes and all evaluation orders, and DD-135 reproduces exactly. The fixed gaps to DD-134 prove that provider noise and call order are not responsible; DD-134 did not serialize enough exact in-process solver evidence to replay its line-search failure. No Jacobian or state advance occurs. One separately frozen in-process failure-capture contract may now be drafted.
- DD-137 executes the frozen `FR-019cb` gate once and passes. The separately versioned tracer retains immutable complete globalization evidence, detects deliberate mutable-buffer aliasing at exactly `0.125`, preserves exact ordinary solver/evaluation identity, and leaves historical `modified_newton_v1` unchanged. One separately frozen live in-process failure-capture diagnostic contract may now be drafted; no state advance or trajectory is authorized.
- DD-138 executes the frozen `FR-019cc` diagnostic once and formally fails only its exact solver/evaluation coordinate-identity gate. The difference is expected representation behavior: nominal component-rate coordinates are converted to actual finite-step rates by the exponential positive-inventory map. Residual identity and every other gate pass. Coarse reproduces the line-search failure near `5.09e-8`, while refined converges to `1.69e-10`. No endpoint advances. A static mapping adjudication may be considered without rerunning DD-138.
- DD-139 executes the frozen `FR-019cd` adjudication once and passes with exact zero difference for both complete evaluator vectors, all non-component coordinates, and residual identity. DD-138 remains formally failed, but its numerical evidence is accepted: coarse fails its frozen-Jacobian line search while refined converges. With residual/provider repeatability already established, one finite-difference Jacobian repeatability audit is now authorized before any solver or trajectory work.
- DD-140 executes the frozen `FR-019ce` audit once and passes every integrity gate, but classifies the Jacobian as process/order dependent. All 12 same-process comparisons are bit-for-bit exact; fresh-process relative differences reach `4.1843e-3`, and the largest affected entries nearly double when `h` halves. Rank remains `50/50`, condition remains below `7.31e5`, all 28,227 provider calls pass, and no solve or state advance occurs. This reconciles exact residual replay with non-reproducible solver behavior. Solver work is stopped; only a separately frozen plus/minus residual/property-packet audit of selected affected coordinates is authorized.
- DD-141 executes the frozen `FR-019cf` adjudication once and confirms the rounded-cache defect without DWSIM calls. Distinct temperature, pressure, and composition states collide, the first queried density/Cp value is returned for both states, and reversing order reverses the retained value. The frozen Jacobian perturbations reach or fall below the cache resolution, while DD-140's discrepancy nearly doubles with step halving. A tested exact-state property-cache-key correction is authorized; solver work remains stopped until a post-fix Jacobian repeatability proof passes.
- DD-142 implements the `FR-019cg` correction only. Density and Cp caches now retain exact-state reuse without merging neighboring finite-difference states. Thermodynamics and solver settings are unchanged. A fresh-process post-fix Jacobian audit remains mandatory before solver work can resume.
- DD-143 executes the frozen `FR-019ch` proof once and passes decisively. Every matrix is bit-for-bit identical across three fresh process orders; `h` versus `h/2` differences fall to `2.33e-10` and spectrum change to `2.42e-8`, with full rank and accepted conditioning. The rounded cache was the cause of DD-140's derivative instability. One separately frozen captured-trajectory successor may now test the repaired numerical path; this audit made no correction or state advance.
- DD-144 executes the frozen `FR-019ci` repeat once and passes. All 30 roots complete, no line-search trial is rejected, worst residual is `1.09e-10`, and coarse/refined endpoints agree within every inherited limit. All capture identities, physical, conservation, provider, efficiency, and controller-direction gates pass with 37,045 calls in `48.472 s`. This establishes that the rounded property cache caused the DD-134 trajectory stops. One separately frozen modest captured extension is authorized; broader dynamics remain staged.
- DD-145 executes the frozen `FR-019cj` contract once and passes. All 60 roots converge with no rejected line-search fraction and worst residual `1.09e-10`; coarse/refined endpoint differences remain within every inherited limit at `t=20 s`. Direction, exact kinematics, pressure, physicality, conservation, provider, and immutable capture gates all pass with 74,005 calls in `42.222 s`. The repaired Core V3 controlled DAE is coherent through 20 seconds. One separately frozen longer captured trajectory is authorized, but the successor must explicitly revise its evidence/call budget because DD-145 nearly consumes the existing call ceiling.
- DD-146 executes the frozen `FR-019ck` contract once and passes. All 180 roots converge with no rejected line-search or bound step and worst residual `1.09e-10`; coarse/refined endpoint differences remain within every inherited limit at `t=60 s`. Direction, exact kinematics, pressure, physicality, conservation, provider, and immutable capture gates all pass with 221,845 calls in `116.259 s`. Core V3 is coherent through 60 seconds. The brute-force fully captured extension path is complete; a separately frozen trajectory-efficiency design is required before multi-minute operation.
- DD-147 executes the frozen `FR-019cl` benchmark once and passes decisively. Every matrix, singular spectrum, rank, and condition reproduces DD-146 exactly; all provider-call and process-isolation gates pass. Four workers reduce median Jacobian wall time by `72.3%` (`3.612x`) and project the DD-146 trajectory at `42.126 s`. No solve or state advance occurs. One separately frozen integration contract may now connect four-worker process-isolated evaluation to the captured solver path.
- DD-148 executes the frozen `FR-019cm` integration once and passes exactly. Matrix, initial residual, corrections, all line-search evidence, final root, and DD-146 reproduction have zero difference. Four-worker Jacobian time is `0.213437 s` versus `0.554112 s` serial (`2.596x`), and all capture/provider/process gates pass. No state advances. One separately frozen parallel captured short trajectory is authorized.
- DD-149 executes the frozen `FR-019cn` contract once and passes exactly. All 30 roots complete with zero capture or accepted-step difference from DD-144; advancing inventory, energy, controller memory, and timestep are evaluated by one persistent process-isolated pool. Exact task/provider counts, four-worker participation per matrix, inherited scientific gates, and all hard stops pass. Total wall is `22.949 s`, or `47.3%` of DD-144. One separately frozen modest parallel trajectory extension is authorized.
- DD-150 executes the frozen `FR-019co` contract once and passes exactly. Both 60-second paths complete all 180 roots with zero capture or accepted-step difference from DD-146. Exact task/provider counts, four-worker participation per matrix, advancing worker context, and every inherited scientific and hard-stop gate pass. Governed wall is `66.596 s`, or `57.3%` of DD-146. One separately frozen multi-minute parallel trajectory is authorized.
- DD-151 executes the frozen `FR-019cp` contract once and formally fails only performance. All 900 roots converge; every scientific, physical, conservation, provider, work-count, worker-ownership, immutable-capture, and DD-146 prefix-equivalence gate passes. Coarse/refined endpoint differences remain accepted at `1.62e-6` inventory, `1.60e-6` energy, `1.05e-6` memory, `5.72e-6` coordinates, `5.72e-6` product, and `8.45e-7` level. Governed wall is `476.349 s`, exceeding both `<5x` DD-150 (`332.979 s`) and `<330 s`. Full replay evidence is retained. This establishes five-minute scientific coherence but does not authorize another live extension; a zero-call timing audit is next.
- DD-152 executes the frozen `FR-019cq` audit once and passes in `1.118 s` with zero model/provider calls. DD-151's excess wall is Jacobian-dominated: `177.529 s` Jacobian excess versus `23.964 s` non-Jacobian excess, or `88.1%` of positive attribution. Refined Jacobian windows rise from `0.3893` to `0.5344 s/root` (`1.373x`) with correlation `0.953`, proving history-dependent timing under the frozen thresholds. Exact-state 2,000-entry cache clear-on-overflow behavior is a specific candidate but not proven causal. One separately frozen saved-state worker-lifetime probe is authorized; DD-151 remains formally failed and another trajectory remains unauthorized.
- DD-153 executes the frozen `FR-019cr` probe once and passes in `155.335 s` with exactly 14,112 governing calls and no solve/state advance. All 12 matrices reproduce DD-151 exactly. Aged/fresh ratios span `1.439x` to `2.496x`, median `1.689x`, with all six checkpoints above threshold. Fresh late/early timing remains nearly flat at `1.064x` coarse and `0.983x` refined, isolating worker lifetime from physical-state cost. Fresh startup averages `6.814 s`, so one separately frozen pool-renewal cadence benchmark is authorized before implementation or trajectory work.
- DD-154 executes the frozen `FR-019cs` projection once in `0.962 s` with zero model/provider calls. All three slope scenarios select a `300`-root cadence, projecting `419.851 s` nominal wall versus DD-151's calibrated `476.349 s`. The `11.86%` improvement fails the precommitted `20%` gate because three complete pool lifecycles cost `38.142 s`. Pool renewal and another trajectory are not authorized. A future efficiency probe should target a cheaper in-worker provider/cache/backend reset rather than process recreation.
- DD-155 executes the frozen `FR-019ct` probe once in `79.171 s` with 220,260 governed calls and no solve/state advance. All 187 matrices reproduce DD-151 exactly, and aged timing reproduces at `1.373x` fresh. Python cache clear (`1.400x` fresh), provider reconstruction (`1.317x`), and DWSIM calculator/property-package reinitialization (`1.403x`) all fail the recovery gate. The process/CLR lifetime effect remains below the resettable object layer. Reset implementation and another trajectory are not authorized; further reset variants should stop.
- DD-156 executes the frozen `FR-019cu` benchmark once in `12.437 s` and passes every gate. Uncached, cold-cache, and warm-cache matrices reproduce DD-151 exactly. Wall falls from `0.190101 s` to `0.083330 s` cold and `0.061935 s` warm (`3.069x`), while exact hit fractions reach `69.56%` and `83.25%`. The opportunity spans fugacity, enthalpy, density, and vapor Z. A bounded production exact-memoization implementation and separate saved-state proof are authorized; no trajectory is authorized yet.
- DD-157 executes the frozen `FR-019cv` proof once in `14.331 s` and passes every gate. Production coarse/refined memoized matrices reproduce DD-151 exactly, each with `818/1,176` hits (`69.56%`) and speedups of `1.877x` and `1.862x`. Unique epochs prove cold per-Jacobian lifecycle isolation. The disabled-by-default production implementation is authorized for one separately frozen short captured trajectory; no longer operation is authorized yet.
- DD-158 executes the frozen `FR-019cw` trajectory once and passes every gate. All 30 complete captures and accepted states reproduce DD-149 exactly. Each root has `818` exact hits and `358` delegate calls (`69.56%`), and trajectory wall falls from `7.413` to `5.338 s` (`0.720x`). Governed total wall is `21.950 s`, with startup still dominant. One separately frozen 60-second memoized trajectory is authorized; five-minute operation is not.
- `FR-019cx` freezes one 60-second memoized captured trajectory. It must reproduce DD-150's `60 x 1.0 s` and `120 x 0.5 s` paths exactly, retain all 180 immutable captures, use one exact memo epoch per Jacobian, record exactly 1,176 logical thermo calls per root with at least a 60% hit fraction, complete trajectory work within `0.80x` DD-150 and total governed work below `60 s`, and prohibit retries, fallbacks, projection, clipping, controller changes, or grid changes. Passing may authorize only a separately frozen five-minute memoized trajectory.
- DD-159 executes the frozen `FR-019cx` trajectory once and passes every gate. All `60/60` coarse and `120/120` refined roots reproduce DD-150 with zero capture or accepted-state difference. Every root records `818` exact hits and `358` delegate calls (`69.56%` hits). Trajectory wall falls from DD-150's `47.778 s` to `31.636 s` (`0.662x`), and governed total wall is `51.547 s`. One separately frozen five-minute memoized trajectory is authorized; no longer operation is authorized.
- `FR-019cy` freezes one five-minute memoized captured trajectory. It must preserve DD-151's `300 x 1.0 s` and `600 x 0.5 s` scientific contract, reproduce all 900 accepted states and complete capture digests exactly, use one exact memo epoch per Jacobian, record exactly 1,176 logical thermo calls per root with at least a 60% hit fraction, complete trajectory work within `0.60x` DD-151 and total governed work below `300 s`, and prohibit retries, fallbacks, projection, clipping, controller changes, or grid changes. Passing establishes a five-minute controlled Core V3 trajectory but does not authorize a longer run.
- DD-160 executes the frozen `FR-019cy` trajectory once and passes every gate. All `300/300` coarse and `600/600` refined roots, accepted states, solver decisions, and complete capture digests reproduce DD-151 exactly. Every root records `818` exact hits and `358` delegate calls (`69.56%` hits). Trajectory wall falls from DD-151's `440.383 s` to `263.645 s` (`0.5987x`), and governed total wall falls from `476.349 s` to `292.399 s` (`0.6138x`). Worst residual remains `1.09e-10`, endpoint refinement is unchanged, and every scientific, physical, conservation, provider, ownership, accounting, and hard-stop gate passes. This establishes the first accepted five-minute Core V3 controlled trajectory. No longer run is authorized.
- `FR-019cz` permits an optional Core V3 thermodynamic provider identity only when every governing call records that identity and no requested imposed phase can fall back to a stable-phase property. A parameter-aligned Clapeyron qualification shall compare direct liquid/vapor fugacity, molecular weight, enthalpy, density, vapor compressibility, repeatability, and a representative 28-call property packet at the accepted DD-160 state. Passing fugacity agreement and performance alone may authorize only a separately frozen fugacity-acceleration design. Full provider substitution additionally requires prospective caloric and liquid-density equivalence; otherwise DWSIM retains those authorities. No qualification probe may perform a nonlinear solve, timestep, or trajectory.
- DD-161 executes the `FR-019cz` property qualification. Clapeyron 0.6.26 direct imposed-phase fugacity matches parameter-aligned DWSIM PR within `6.4410e-5` liquid and `1.8452e-7` vapor, molecular weights agree within `1.4211e-14 lbm/lbmol`, and 100 repeated fugacity evaluations are bit-for-bit stable. Fifty neighboring 28-call packets are `9.2397x` faster warm. Liquid density differs by up to `7.445%` and latent enthalpy by up to `172.946 BTU/lbmol`, so full substitution is not authorized. One separately frozen hybrid fugacity residual/Jacobian benchmark is authorized; DWSIM remains the energy, density, molecular-weight, and diagnostic-flash authority.
- `FR-019da` permits one saved-state hybrid fugacity benchmark only. Clapeyron may own direct imposed-phase fugacity while DWSIM retains every bulk and diagnostic property. The complete residual and two-step colored Jacobian must retain rank `50`, remain stable to finite-difference step halving, stay within the frozen residual/matrix/spectrum/condition limits, and improve warm matrix wall time by at least `1.25x`. No nonlinear solve, state acceptance, timestep, or trajectory may occur.
- DD-162 executes `FR-019da` and passes every gate. The residual shift is `1.6411e-4`; the relative Jacobian difference is `1.3811e-6`; both matrices retain rank `50`; the condition ratio is `1.0000503`; and warm matrix speedup is `1.3949x`. One separately frozen hybrid-root reconstruction is authorized. This result does not make Clapeyron the accepted Core V3 root basis.
- `FR-019db` permits one captured hybrid-root reconstruction from the exact accepted DD-160 zero-time root. It fixes one Jacobian, one factorization, the existing four line-search fractions, the `1e-8` residual limit, endpoint rank/condition, physicality, conservation, provider ownership, and engineering-equivalence limits. Any retry, smaller fraction, fresh Jacobian, clipping, projection, fallback, timestep, or trajectory is prohibited. Failure retires direct hybrid root-basis substitution.
- DD-163 executes `FR-019db` once and stops cleanly. Although the frozen and endpoint Jacobians remain full-rank and every non-solver integrity gate passes, all four prescribed fractions increase the residual and the solver accepts no correction. Direct Clapeyron-fugacity/DWSIM-bulk root ownership is therefore rejected. DWSIM remains the governing residual/root basis, and no hybrid trajectory or tuned retry is authorized.
- `FR-019dc` permits one derivative-only root proof after DD-163. DWSIM must own the initial residual, every line-search trial, convergence test, and endpoint audit. Clapeyron fugacity may contribute only to a frozen approximate Jacobian whose bulk properties remain DWSIM-owned. The unchanged captured solver must close below `1e-8`, reproduce the accepted DD-148 root within `1e-6`, retain rank/condition and provider gates, and improve warm matrix time by at least `1.10x`. No root acceptance, state advance, retry, timestep, or trajectory may occur.
- DD-164 executes `FR-019dc` and passes every gate. The approximate matrix differs from DWSIM by `6.3866e-7` relative and is `1.1013x` faster. DWSIM residuals converge in the same two full corrections to `1.0873e-10`, and the accepted reference root is reproduced within `4.0311e-12` coordinates. This authorizes only a separately frozen parallel/startup economics benchmark before any derivative-accelerated trajectory; direct hybrid residual ownership remains retired.
- `FR-019dd` freezes one exact DD-158 10-second trajectory using four persistent hybrid-Jacobian workers. DWSIM must retain every main-process residual and decision; the trajectory must preserve all accepted scientific gates and captures within `1e-10`, retain exact memo accounting with at least `60%` hits per root, improve trajectory work by at least `5%`, and project below DD-160 wall after charging Julia startup. No retry, fallback, clipping, projection, controller change, or grid change is allowed.
- DD-165 enters `FR-019dd` once but aborts before a Newton correction because the Clapeyron provider lacks the memoization-statistics API required by the worker. No root is accepted and no state advances. This is non-adjudicative scientifically. DD-165 shall not be rerun. A disabled-by-default exact-key imposed-phase fugacity cache with complete statistics may be implemented and tested; only a separately frozen unchanged-science successor may repeat the trajectory.
- `FR-019de` permits one DD-165 successor with no scientific-contract change. The sole correction is a disabled-by-default, exact-key, unrounded Clapeyron imposed-phase fugacity cache and statistics API. Every DD-165 solver, grid, controller, ownership, equivalence, memo, wall, projection, and hard-stop requirement remains in force.
- DD-166 executes `FR-019de` once. All roots complete and memoization passes, but complete capture equivalence and both performance gates fail. Trajectory work is `1.887x` DD-158, startup is `28.792 s`, and the frozen five-minute projection is `548.640 s`, substantially worse than DD-160. The derivative-only parallel Clapeyron acceleration path is retired. Production Core V3 shall retain DWSIM residuals and four-worker memoized DWSIM Jacobians. The optional Clapeyron provider and exact memo API remain available for diagnostics and future provider/API developments, not this trajectory path.
- `FR-019df` requires Core V3 scale-up to proceed by generated generic topology and bounded gates. The first successor shall contain one reflux drum, two rectifying interior volumes, one feed volume, two stripping interior volumes, and one combined reboiler/sump volume while preserving the five-volume equation ownership, DWSIM provider authority, energy-owned vapor links, Francis-only liquid links, and solved total-condenser duty. The structural gate must be square, full rank, symbolically conservative, free of imported profiles and mixed thermo bases, and perform no property evaluation, solve, timestep, or integration. A pass authorizes only one separately frozen live-DWSIM residual and Jacobian audit.
- DD-167 executes the structural portion of `FR-019df` and passes. The generated three-component seven-volume ledger is `56 x 56`, has structural rank `56`, zero nullity, six energy-owned vapor links, five Francis liquid links, exact symbolic component and energy cancellation, and unchanged provider-ownership gates. The original five-volume default remains unchanged. No property call, nonlinear solve, timestep, or integration occurs. One separately frozen seven-volume live-DWSIM residual/Jacobian audit is authorized; root solving and dynamics remain unauthorized.
- `FR-019dg` freezes the single DD-168 seven-volume numerical audit. One source-mapped `56 x 56` state shall be evaluated with DWSIM Peng-Robinson governing every live property. Two uncolored central-difference Jacobians at `1e-5` and `5e-6` must retain full rank `56`, local condenser rank `3`, condition below `1e8`, spectrum change below `0.25`, exact registered coupling, physicality, provider ownership, TP-flash and independent-PR validation, component/energy telescoping, fewer than `20,000` calls, and less than `120 s` wall time. Preparation may use only local feed-enthalpy, condenser-bubble, condenser-duty, and PR-parameter calls. No full residual is evaluated before commit, and no nonlinear solve, root acceptance, timestep, initializer, or dynamics is authorized.
- DD-168 executes `FR-019dg` once from frozen commit `5568250` and passes every gate. Both live `56 x 56` Jacobians retain rank `56`, local condenser rank `3`, zero unexpected couplings, worst condition `2.937784e6`, and spectrum change `1.063791e-7`. Component and energy telescoping are `1.145944e-16` and `3.404828e-17` relative; all physical, condenser-phase, TP-flash, independent-PR, ownership, and provider gates pass. The audit uses `7,749` calls in `6.082 s`. The source-mapped point's `0.397863` scaled residual is diagnostic because no root solve occurred. One separately frozen seven-volume stationary-root campaign is authorized; dynamic work remains unauthorized.
- `FR-019dh` freezes one DD-169 seven-volume stationary-root campaign. Exactly two complete starts are allowed: the unsolved DD-168 source mapping and one independently constructed smooth topology-scale state. Both shall use the same bounded transformed coordinates, fixed residual scales, DWSIM Peng-Robinson governing properties, `least_squares(method="trf")`, central-difference Jacobian, tolerances, and physical inputs. Each endpoint must close below `1e-8`, remain interior and physical, retain full rank `56`, local condenser rank `3`, condition below `1e8`, stable singular spectra, exact conservation and provider ownership, and pass phase diagnostics. Physical root agreement must be below `1e-7`; total logical calls must remain below `500,000` and wall time below `600 s`. Exact unrounded memoization may reduce delegated work but shall be cleared per start and cannot alter acceptance. No retry, continuation, alternate solver, bound/scale/tolerance change, timestep, initializer, or dynamics is permitted.
- DD-169 executes `FR-019dh` once from frozen commit `87470e1` and passes every gate. The source-mapped and independent smooth starts close to scaled residuals `4.98e-13` and `5.55e-15` in 50 and 51 function evaluations and agree physically within `1.918327e-12`. All endpoint Jacobians retain rank `56`, local rank `3`, worst condition `3.5130e3`, stable spectra, no active bounds, exact conservation, physical ordering, provider ownership, and phase validation. Exact memoization serves `332,192` repeated requests; the two-start campaign completes `354,104` logical calls in `49.436 s`. The accepted root has `D=2220.952`, `B=4922.022 lbmol/h`, and `Q_C=-51.776179 MMBTU/h`. One structural seven-volume conserved dynamic-DAE contract is authorized; timestep and dynamic work remain unauthorized.
- `FR-019di` requires the first seven-volume dynamic increment to be structural only. It shall use the DD-169 topology and accepted root, retain component inventories as the only differential states, derive internal-energy rates by the provider-consistent chain rule, and assign temperatures, equilibrium compositions, Francis liquid flows, energy-owned vapor flows, and condenser duty as algebraic variables. Pressure, feed, reflux, reboiler duty, geometry, and DD-169 product rates remain fixed open-loop parameters. The ledger must be square, full structural rank, conservative, topology-generated, free of controllers and profiles, and perform no property evaluation, numerical mass-matrix evaluation, solve, timestep, or integration. A pass authorizes only one separately frozen live leading-Jacobian and consistent-derivative audit.
- DD-170 executes `FR-019di` and passes. The seven-volume, three-component ledger has 21 component-inventory states, 21 derivative variables, 33 algebraic variables, and 54 equations. Its `54 x 54` incidence matrix has rank `54` and zero nullity. Equation ownership comprises 21 component balances, seven energy balances, 18 full-fugacity equations, five Francis equations, and three condenser-bubble equations. Dependencies are generated from generic topology links; only top and bottom retain explicit terminal roles. No controller, profile, property call, mass-matrix evaluation, solve, timestep, or integration occurs. One separately frozen live numerical leading-Jacobian and consistent-derivative audit is authorized; dynamic integration remains unauthorized.
- `FR-019dj` freezes one DD-171 live seven-volume dynamic-DAE numerical audit at the accepted DD-169 root. The audit shall reconstruct 21 component inventories and 33 algebraic coordinates, evaluate provider-consistent internal-energy storage gradients at relative steps `1e-5` and `5e-6`, evaluate the zero-rate 54-row residual, and build complete central-difference `54 x 54` leading Jacobians at `1e-5` and `5e-6`. Both matrices must retain rank `54`, condition below `1e8`, spectrum change below `0.25`, exact registered coupling, conservation, physical storage reconstruction, and DWSIM provider ownership. Zero-rate closure must be below `1e-8`; storage-gradient step change below `1e-3`; bubble residual below `1e-10`; logical calls below `30,000`; wall below `120 s`. Exact unrounded memoization is permitted. No state solve, timestep, controller, or integration may occur. A complete pass authorizes only one separately frozen stationary root-hold implicit-step contract.
- DD-171 executes `FR-019dj` once from frozen commit `c5db4db` and passes every gate. The accepted DD-169 root closes the zero-rate dynamic residual at `4.979842e-13`; component and energy-storage rates are exactly zero. Storage-gradient step change is `2.021471e-9`, and the worst local bubble residual is `7.771561e-15`. Both complete leading matrices retain rank `54/54`, have no zero rows, zero columns, or unexpected couplings, worst condition `50.970640`, and spectrum change `1.095288e-10`. Component and energy conservation remain at `1.432430e-16` and `7.660863e-17`. Exact memoization serves `9,096` of `11,494` logical requests; execution completes in `8.446 s`. One separately frozen stationary root-hold implicit-step contract is authorized. Moving dynamics remain unauthorized.
- `FR-019dk` freezes one DD-172 stationary seven-volume implicit-step campaign. It shall solve one `1.0 s` backward-Euler root and two successive `0.5 s` roots from the accepted DD-169 state using the same `least_squares(method="trf")` solver, topology-generated colored central-difference Jacobian at `1e-5`, fixed scales, and exact unrounded memoization. All three roots must close below `1e-8`, retain rank `54`, condition below `1e8`, stationary component rates below `1e-4 lbmol/h`, inventory motion below `1e-9`, algebraic motion below `1e-7`, equilibrium closure below `1e-10`, conservation and discrete kinematics, physicality, and full/refined endpoint agreement. Calls must remain below `30,000` and wall below `120 s`. No disturbance, controller, retry, alternate solver, or trajectory is permitted. A complete pass authorizes only one separately frozen moving-step contract.
- DD-172 executes `FR-019dk` once from frozen commit `ed8cbde` and passes every gate. The full step and two successive half steps each converge in two function evaluations with residuals from `1.0051e-13` to `3.8107e-13`; all Jacobians retain rank `54`, with worst condition `8.974027e5`. Maximum component rate is `8.19e-9 lbmol/h`, relative inventory motion remains below `1.04e-12`, and algebraic motion below `1.57e-12`. Full/refined inventory, rate-coordinate, and algebraic differences are `1.032387e-14`, `2.245103e-13`, and `6.593615e-13`. Physicality, equilibrium, conservation, kinematic, provider, call, and wall gates pass with `7,344` logical requests in `4.447 s`; memoization serves `6,077`. One separately frozen small moving-step contract is authorized. Controllers and trajectories remain unauthorized.
- `FR-019dl` freezes one DD-173 seven-volume open-loop moving-step campaign. Every feed component flow and total feed enthalpy shall increase by exactly `0.1%`, preserving feed composition and specific enthalpy; pressure, reflux, duties, product draws, geometry, and all other inputs remain unchanged. One `1.0 s` backward-Euler root and two successive `0.5 s` roots shall use the exact DD-172 solver, colored Jacobian, scales, and memoization. Every root must close below `1e-8`, retain rank `54`, condition below `1e8`, physicality, equilibrium, conservation, and discrete kinematics. Both paths must produce a positive total inventory increase between `1e-4` and `1e-2 lbmol`, satisfy global component accumulation within `1e-6 lbmol`, and agree within `1e-7` relative inventory, `1e-5` rate/algebraic coordinates, and `1e-6 lbmol` total inventory. Calls must remain below `30,000` and wall below `120 s`. No controller, retry, alternate disturbance, or trajectory is permitted. A complete pass authorizes only one separately frozen short open-loop trajectory contract.
- DD-173 executes `FR-019dl` once from frozen commit `55f4d5d` and formally fails only the frozen per-component refinement gate. All three roots converge in four evaluations with residuals below `4.49e-13`, rank `54`, worst condition `8.974025e5`, physicality, equilibrium, exact kinematics, conservation, provider ownership, positive response, and correct global component accumulation. Full and refined total inventory increases are both `0.00198415944 lbmol` and differ by only `5.7465e-13 lbmol`. Rate and algebraic refinement pass. Maximum relative component-inventory difference is `1.522960e-6` versus `<1e-7`; its offending absolute difference is `5.9660e-6 lbmol`. The campaign uses `13,328` requests in `6.798 s`. DD-173 shall not be rerun or reclassified, and no trajectory is authorized. One zero-call physical-scale adjudication of the immutable endpoints may be considered under a separate frozen contract.
- `FR-019dm` freezes one DD-174 zero-call adjudication of the immutable DD-173 endpoints. It must preserve DD-173's formal failure and verify that no other DD-173 root, response, or refinement gate failed. The full/refined component-inventory difference must remain below `1e-4 lbmol` maximum absolute, `1e-5` relative to `max(initial, 1 lbmol)`, `1e-6` relative to each volume's total initial holdup, and `2e-4 lbmol` in L1 norm. Its absolute signed total must remain below `1e-9 lbmol`; inherited total-response refinement and global component identity must remain below `1e-6 lbmol`. Model, provider, solver, and endpoint-regeneration calls are prohibited. A complete pass authorizes only one separately frozen smaller-timestep moving proof; failure stops the moving dynamic path.
- DD-174 executes `FR-019dm` once from frozen commit `590e953` and passes every gate with zero model, provider, solver, or endpoint-regeneration calls. Maximum absolute component difference is `3.275352e-5 lbmol`; maximum state-floor-relative and volume-holdup-relative differences are `1.522960e-6` and `7.203869e-7`; L1 difference is `1.164692e-4 lbmol`; and signed total difference is `-5.746514e-13 lbmol`. DD-173 remains formally failed. One separately frozen proof comparing a `0.25 s` moving step with two `0.125 s` steps is authorized; no trajectory is authorized.
- `FR-019dn` freezes one DD-175 smaller-timestep moving proof using the unchanged DD-173 initial state, `+0.1%` feed-rate and feed-enthalpy disturbance, solver, colored Jacobian, scaling, DWSIM property ownership, and exact memoization. It shall compare one `0.25 s` backward-Euler step with two successive `0.125 s` steps. Every DD-173 root, rank, condition, equilibrium, conservation, kinematic, physicality, response, provider, call, and wall gate remains in force, including the original `<1e-7` maximum relative component-inventory refinement limit. Every DD-174 physical-scale limit also remains in force. No retry, alternate grid, controller, or trajectory is permitted. A complete pass authorizes only one separately frozen short open-loop trajectory on the smaller grid; failure stops the moving path.
- DD-175 executes `FR-019dn` once from frozen commit `9404c2b` and formally fails only the retained maximum relative component-inventory refinement gate at `1.172326e-7` versus `<1e-7`. All three roots close below `3.72e-12`, retain rank `54`, remain physical and conservative, and pass every response, provider, performance, and DD-174 physical-scale gate. Reducing the full timestep from `1.0 s` to `0.25 s` reduces the disputed metric by `12.99x`; maximum absolute component difference is only `2.535865e-6 lbmol`, and full/refined total accumulation differs by `8.13e-14 lbmol`. This is strong numerical refinement, but the frozen hard stop controls: DD-175 shall not be rerun or tuned, and no open-loop trajectory is authorized under this moving-step campaign.
- `FR-019do` prospectively replaces the unfloored maximum component-relative inventory difference as a standalone Core V3 moving-step and trajectory gate. Every new contract shall use the topology-neutral physical refinement policy with predeclared maximum absolute component, `1 lbmol`-floor-relative, volume-holdup-relative, L1, and signed-total limits. The first seven-volume limits remain the values frozen before DD-174: `<1e-4 lbmol`, `<1e-5`, `<1e-6`, `<2e-4 lbmol`, and `<1e-9 lbmol`. The old unfloored ratio remains mandatory diagnostic output but cannot independently veto a campaign. Same-horizon coarse/refined comparison, root closure, rank, condition, equilibrium, physicality, kinematics, conservation, response direction, rate/algebraic refinement, provider ownership, and performance gates remain mandatory. DD-173 and DD-175 retain their formal failed classifications and cannot be rerun. DD-175's immutable evidence may support one separately frozen short open-loop trajectory contract under this new policy; no trajectory or controller is authorized by the policy change alone.
- `FR-019dp` freezes one DD-177 short seven-volume open-loop trajectory under the DD-176 policy. From the accepted DD-169 root and unchanged `+0.1%` feed-rate/enthalpy disturbance, it shall compare `8 x 0.25 s` with `16 x 0.125 s` over two seconds. All 24 roots must close below `1e-8`, retain rank `54`, condition below `1e8`, physicality, equilibrium, conservation, and exact kinematics. Both paths must complete with positive monotone total accumulation and global component identity below `1e-6 lbmol`. Every one of eight shared-time pairs must pass the DD-176 physical inventory limits and `<1e-5` rate/algebraic refinement. The unfloored component ratio is diagnostic only. Calls must remain below `150,000` and wall below `180 s`. Retry, alternate grid, controller, clipping, projection, fallback, or continuation is prohibited. A complete pass authorizes only one separately frozen modest open-loop extension; failure stops the physical-policy trajectory path.
- DD-177 executes `FR-019dp` once from frozen commit `63c52d2` and passes every gate. Both `8 x 0.25 s` and `16 x 0.125 s` paths complete all 24 roots with worst residual `4.140815e-12`, rank `54`, worst condition `1.434601e7`, and no physical, equilibrium, conservation, kinematic, provider, call, or wall failure. All eight shared-time comparisons pass; worst absolute, volume-relative, L1, rate, and algebraic differences are `1.558675e-5 lbmol`, `3.428178e-7`, `5.535587e-5 lbmol`, `1.127481e-6`, and `1.234961e-6`. Total accumulation is `0.003968318883 lbmol` on both paths and agrees within `4.84e-13 lbmol`. The legacy diagnostic reaches `7.258663e-7`, confirming why it must not be a standalone veto. The campaign uses `103,190` logical calls in `28.494 s`. One separately frozen modest open-loop extension is authorized; controllers remain unauthorized.
- `FR-019dq` freezes one DD-178 ten-second open-loop extension with no scientific change from DD-177. It shall compare `40 x 0.25 s` with `80 x 0.125 s`, retain all 120 per-root closure, rank `54`, condition `<1e8`, equilibrium, physicality, conservation, kinematic, response, provider, and DD-176 physical-refinement gates, and evaluate all 40 same-time pairs. The legacy unfloored ratio remains diagnostic only. Compact evidence shall retain every root's scalar gate data, complete path endpoints, total-inventory histories, and all shared-time metrics. Calls must remain below `650,000` and wall below `240 s`. Retry, alternate grid, controller, clipping, projection, fallback, or continuation is prohibited. A complete pass authorizes only one separately frozen longer open-loop trajectory; failure stops the physical-policy trajectory path.
- DD-178 executes `FR-019dq` once from frozen commit `f84c7a6` and formally fails only the inherited `<0.01 lbmol` maximum-response gate, which is incompatible with the known ten-second net feed increment of `0.019841594413 lbmol`. Both paths complete all 120 roots with worst residual `4.740065e-12`, rank `54`, worst condition `1.434602e7`, and every physicality, equilibrium, conservation, kinematic, shared-refinement, provider, call, and wall gate passing. Coarse/refined accumulation is `0.019841594414340` and `0.019841594413359 lbmol`, matching integrated external flow within `1.34e-12 lbmol` and each other within `9.82e-13 lbmol`. All 40 shared pairs pass. DD-178 remains formally failed and shall not be rerun. One separately frozen zero-call duration-scaled response adjudication may inspect the immutable evidence; no live extension or controller is authorized.
- `FR-019dr` freezes one DD-179 zero-call adjudication of DD-178's immutable response evidence. DD-178 must retain its formal failure, both response paths must have failed only the inherited `bounded` gate, and every non-response campaign gate must remain passing. Each actual total accumulation must match its path-integrated expected external flow within `1e-6` relative; coarse/refined actual totals must agree within `1e-9 lbmol`; global component identities must remain below `1e-6 lbmol`; and both responses must remain positive and monotone. Model, provider, solver, and endpoint-regeneration calls are prohibited. A pass establishes a prospective duration-scaled response policy and authorizes only one separately frozen longer open-loop trajectory; failure stops the path.
- DD-179 executes `FR-019dr` once from frozen commit `bd8f4fa` and passes every gate with zero model, provider, solver, or endpoint-regeneration calls. Coarse and refined actual accumulation match their integrated expected external flow within `6.7445e-11` and `1.7977e-11` relative; their actual totals differ by `9.8155e-13 lbmol`; component identities remain below `8.21e-13 lbmol`; and both responses are positive and monotone. Every non-response DD-178 gate remains passing, while DD-178 retains its formal failed classification. Future trajectory response acceptance shall scale to integrated expected external flow over duration rather than inherit a fixed absolute ceiling. One separately frozen longer open-loop trajectory is authorized; controllers remain unauthorized.
- `FR-019ds` freezes one DD-180 thirty-second open-loop trajectory under the DD-176 physical-refinement and DD-179 duration-scaled response policies. It shall compare `120 x 0.25 s` with `240 x 0.125 s`; all 360 roots must close below `1e-8`, retain rank `54`, condition `<1e8`, equilibrium, physicality, conservation, and exact kinematics. All 120 shared-time pairs must pass the physical inventory and `<1e-5` rate/algebraic gates. Each path's actual accumulation must match integrated expected external flow within `1e-6` relative, remain positive and monotone, and retain component identity below `1e-6 lbmol`; cross-grid totals must agree within `1e-9 lbmol`. No absolute duration-independent response ceiling is permitted. Calls must remain below `2,000,000` and wall below `600 s`. Retry, alternate grid, controller, clipping, projection, fallback, or continuation is prohibited. A complete pass authorizes only a structural terminal-inventory-control contract; failure stops the path.
- DD-180 executes `FR-019ds` once from frozen commit `968813e` and passes every gate. Both `120 x 0.25 s` and `240 x 0.125 s` paths complete all 360 roots with worst residual `5.290149e-12`, rank `54`, worst condition `1.434628e7`, and no physicality, equilibrium, conservation, kinematic, provider, call, or wall failure. All 120 shared-time comparisons pass; worst absolute, volume-relative, L1, rate, and algebraic differences are `5.143323e-5 lbmol`, `5.623436e-7`, `1.658663e-4 lbmol`, `1.406931e-6`, and `2.011474e-6`. Actual accumulation matches integrated expected flow within `1.10e-11` relative, and grids agree within `1.0569e-12 lbmol`. The campaign uses `1,577,770` logical calls in `419.083 s`. Open-loop proof is complete for this stage. Only structural terminal-inventory-control design is authorized; no live controller or further trajectory is.
- `FR-019dt` freezes DD-181 as a performance-only audit and benchmark at the DD-180 first coarse-step initial point. It shall derive production and refinement wall attribution from immutable DD-180 evidence, then build the complete `54 x 54`, 17-color central-difference Jacobian through fresh isolated pools of one, two, and four DWSIM workers in a fixed nine-run schedule. Every matrix must agree with the serial reference within `1e-10` absolute and relative Frobenius limits, retain rank `54`, condition below `1e8`, and singular-spectrum agreement below `1e-8`. Four-worker median matrix time must be no more than `75%` of one-worker time, and the projected 30-second production path must be below `105 s`. A nonlinear solve, state advance, controller, retry, or trajectory is prohibited. A pass authorizes only persistent parallel step-solver design.
- DD-181 executes `FR-019dt` once from frozen commit `1e5fd6c` and passes every gate. DD-180's production-equivalent path accounts for `136.517 s` of its `419.083 s` wall, while the refinement path accounts for `272.968 s`. All nine parallel matrices preserve numerical integrity and exactly `246` delegated thermo evaluations. Median Jacobian time is `0.271654 s`, `0.197377 s`, and `0.102903 s` at one, two, and four workers, respectively; four workers deliver `2.640x` speedup and project the production path to `55.742 s` per `30 s` simulated. Four-worker startup is `5.714 s`, so production integration must retain a persistent isolated pool across steps. No solve or state advance occurs. One separately frozen persistent-parallel step-solver equivalence proof is authorized; controller and longer-trajectory work remain unauthorized.
- `FR-019du` freezes DD-182 as one serial-versus-parallel integration proof at the DD-180 first `0.25 s` coarse root. Both roots shall use the same seven-volume equations, state, disturbance, scales, SciPy `trf` settings, residual path, and 17-color central-difference rule. The serial root shall use the existing in-process colored builder; the parallel root shall delegate only the same 34 perturbation residuals through one persistent four-process DWSIM pool. Every corresponding Jacobian and SciPy decision must be exact; endpoint quantities must agree within `1e-12`. Both roots must close below `1e-8`, retain rank `54`, condition below `1e8`, physicality, conservation, and provider ownership. Parallel solve wall excluding startup must be at least `10%` lower. State acceptance, a second timestep, controller, trajectory, retry, or fallback is prohibited. A pass authorizes only one separately frozen short persistent-parallel trajectory.
- DD-182 executes `FR-019du` once from frozen commit `1039d9c` and passes every gate. Serial and parallel roots both close at `1.192235e-12` in `4/4` function/Jacobian evaluations. All four paired Jacobians and every solver-decision and endpoint quantity differ by exactly `0.0`; rank remains `54`, condition remains below `1e8`, and provider ownership passes. Solve wall excluding startup falls from `1.252650 s` to `0.534514 s`, a `2.344x` speedup. Persistent-pool startup is `5.010 s`, and workers execute `4,624` logical property requests. No endpoint is accepted as a state advance. One short persistent-parallel trajectory contract is authorized; controller and longer-trajectory work remain unauthorized.
- `FR-019dv` freezes DD-183 as one four-second serial-versus-persistent-parallel trajectory proof under the unchanged DD-180 `+0.1%` feed-rate/enthalpy disturbance. Each path shall execute `16 x 0.25 s` roots with identical equations, state, grid, scales, solver, and main-process residual ownership. The parallel path shall use exactly one four-worker pool. Every worker must rebuild each evolving root's inventory, provider-derived storage, component-rate scales, and physical-state template exactly once on that root's first Jacobian and never on later Jacobians. Every paired Jacobian and SciPy decision must be exact; accepted states must agree within `1e-12`. Both paths must pass closure, rank `54`, condition, physicality, conservation, monotone response, expected accumulation, and provider gates. Parallel trajectory wall must be no more than `65%` of serial excluding startup and no more than `85%` including adjusted startup. Controller, retry, alternate grid, projection, clipping, and fallback are prohibited. A pass authorizes the persistent-parallel production step path.
- DD-183 executes `FR-019dv` once from frozen commit `ec7f5d4` and passes every gate. Both paths complete all 16 roots with worst residual `1.504662e-12`; all 56 paired Jacobians, SciPy decisions, and accepted states differ by exactly `0.0`. All four workers rebuild every new root basis exactly once on its first Jacobian and zero times thereafter. Serial and parallel trajectory wall are `23.359 s` and `12.480 s`, a `1.872x` speedup; including `1.875 s` adjusted startup, governed speedup remains `1.627x`. Actual response `0.007936637767 lbmol` matches expected `0.007936637778 lbmol`, and workers execute `66,912` logical property requests. The persistent four-worker DWSIM Jacobian path is authorized for production step execution. Controller equations and a controlled trajectory remain separately gated.
- `FR-019dw` requires the first seven-volume terminal-inventory-control increment to be structural only. Distillate and bottoms shall be removed from fixed parameters and replaced by positive algebraic product-rate outputs driven by the geometry-derived reflux-drum and combined reboiler/sump level fractions. Each terminal shall own one PI memory state and rate, one integrator equation, and one output equation. Product component draws shall use live terminal liquid composition. Controller dependencies shall remain confined to the top and bottom balance rows; all interior balances, equilibrium relations, Francis hydraulics, and energy-owned vapor links shall remain unchanged. The ledger must be square, full structural rank, topology-generic, and free of property calls, solves, controller execution, timestep selection, or integration. A pass authorizes only one separately frozen live zero-time residual and leading-Jacobian audit; tuning and controlled trajectories remain unauthorized.
- DD-184 executes `FR-019dw` and passes. The controlled seven-volume ledger has 23 states, 23 derivative variables, 35 algebraic variables, and 58 equations. Its `58 x 58` incidence matrix has rank `58` and zero nullity. The two fixed DD-169 product parameters are absent, every top and bottom component/energy balance owns the appropriate live product output, no interior row depends on a controller variable, and the generic eight-volume/four-component test also passes. The established vessel geometry and prior positive PI constants are carried only to define units and signs; no tuning is accepted. No property, residual, Jacobian, solve, controller, timestep, or integration call occurs. One separately frozen live zero-time residual and leading-Jacobian audit is authorized.
- `FR-019dx` freezes one DD-185 live numerical audit of the DD-184 terminal-control ledger at the accepted DD-169 root. The audit shall reuse DD-171's accepted storage gradient, reconstruct terminal levels once from live DWSIM liquid density and the frozen vessel geometry, and use those exact levels as setpoints with zero controller memories, rates, and product log ratios. The complete 58-row residual and controller rows must close below `1e-8` and `1e-10`; levels must lie strictly between `0.01` and `0.99`; product rates must reproduce DD-169 within `1e-12` relative. Two central-difference `58 x 58` Jacobians at `1e-5` and `5e-6` must retain rank `58`, condition below `1e8`, spectrum change below `0.25`, exact registered coupling, conservation, and provider ownership. Calls must remain below `35,000` and wall below `120 s`. No nonlinear state solve, controller-memory advance, tuning, timestep, or integration is permitted. A complete pass authorizes only one separately frozen controlled stationary root-hold step contract.
- DD-185 executes `FR-019dx` once from frozen commit `8590e4a` and passes every gate. Live top and bottom level fractions are `0.459899` and `0.427453`; the controller rows close exactly, level error is zero, and distillate/bottoms remain exactly `2220.952340` and `4922.021660 lbmol/h`. The complete residual remains `4.979842e-13`. Both `58 x 58` Jacobians retain rank `58`, have no unexpected couplings, worst condition `1.432411e3`, and spectrum change `8.604470e-11`. Conservation and DWSIM ownership pass with `7,956` logical calls, `7,682` exact memo hits, and `4.818 s` wall. One separately frozen controlled stationary root-hold implicit-step contract is authorized; tuning and disturbed controlled trajectories remain unauthorized.
- `FR-019dy` freezes one DD-186 controlled stationary root-hold campaign. It shall compare one `1.0 s` backward-Euler step with two successive `0.5 s` steps using the exact DD-185 levels as setpoints, zero initial PI memories and product log ratios, fixed DD-169 product reference rates, the DD-185 `58 x 58` equations, `least_squares(method="trf")`, and a topology-colored central-difference Jacobian. All three roots must close below `1e-8`, retain rank `58`, condition below `1e8`, physicality, equilibrium, conservation, and exact component, energy, and controller-memory kinematics. Inventory, algebraic, PI-memory, level, and product movement must remain below their frozen stationary limits, and full/refined endpoints must agree. Calls must remain below `40,000` and wall below `120 s`. No disturbance, tuning, retry, or trajectory is permitted. A complete pass authorizes only one separately frozen small controlled moving-step contract.
- DD-186 executes `FR-019dy` once from frozen commit `2459d3b` and passes every gate. All three roots converge in two function/Jacobian evaluations with residuals below `3.82e-13`, rank `58`, and worst condition `8.973645e5`. Maximum controller-memory motion is `3.496572e-17`, product-rate motion is `8.499913e-15` relative, level error is below `1e-15`, and component rates remain below `8.19e-9 lbmol/h`. Full/refined inventory, rate, algebraic, controller-memory, and product differences all pass, led by `1.282663e-14` relative inventory difference. Provider ownership passes with `7,344` logical calls, `5,924` memo hits, and `5.002 s` wall. One separately frozen small controlled moving-step contract is authorized; tuning and controlled trajectories remain unauthorized.
- `FR-019dz` freezes one DD-187 small controlled moving-step campaign. It shall apply the unchanged DD-173 `+0.1%` feed-component-rate and feed-enthalpy disturbance while preserving feed composition, feed specific enthalpy, DD-185 level setpoints, DD-184 PI constants, and fixed DD-169 product reference rates on every continuation step. One `0.25 s` backward-Euler endpoint shall be compared with two successive `0.125 s` endpoints. All three `58 x 58` roots must close below `1e-8`, retain rank `58`, condition below `1e8`, physicality, equilibrium, component/energy/controller kinematics, conservation, and exact controller-equation closure. Both paths must have a positive detectable bounded response and exact integrated component identity. Coarse/refined inventories must pass `FR-019do` physical-scale gates; rate, algebraic, PI-memory, product-rate, and level differences must pass their frozen limits. Calls must remain below `40,000` and wall below `120 s`. Tuning, retry, alternate disturbance, and trajectory are prohibited. A complete pass authorizes only one separately frozen short controlled trajectory contract.
- DD-187 executes `FR-019dz` once from frozen commit `7e7eec2` and passes every gate. The fixed product-reference semantics added in commit `f0cb255` prevent continuation-step compounding. All three roots close below `1.63e-12`, retain rank `58`, and have worst condition `1.434585e7`. Full/refined total accumulation is `4.960398e-4 lbmol` with component identity below `5.02e-13 lbmol`. Maximum absolute and L1 inventory differences are `2.535865e-6` and `9.051784e-6 lbmol`; rate, algebraic, controller-memory, product, and level refinement all pass. Maximum product-rate movement is `6.823268e-11` relative, appropriate to the quarter-second horizon and not a tuning qualification. DWSIM ownership passes with `14,484` logical calls in `6.836 s`. One separately frozen short controlled trajectory contract is authorized; controller tuning remains unauthorized.
- `FR-019ea` freezes one DD-188 two-second controlled trajectory under the unchanged DD-187 disturbance, setpoints, PI constants, fixed product references, solver, and DWSIM authority. It shall compare `8 x 0.25 s` with `16 x 0.125 s`; all 24 roots must close below `1e-8`, retain rank `58`, condition below `1e8`, physicality, equilibrium, conservation, component/energy/controller kinematics, and controller-equation closure. Both paths must be positive and monotone and match integrated external flow within `1e-6` relative with component identity below `1e-6 lbmol`. All eight shared times must pass `FR-019do` physical inventory gates and `<1e-5` rate/algebraic, `<1e-7` PI-memory, `<1e-6` product, and `<1e-8` level refinement. Coarse/refined total accumulation must agree within `1e-9 lbmol`. Calls must remain below `150,000` and wall below `180 s`. Tuning, retry, alternate grid, projection, clipping, and fallback are prohibited.
- DD-188 executes `FR-019ea` once from frozen commit `3c05da4` and formally fails only the `<1e-9 lbmol` signed-total/cross-grid accumulation gate. Both paths complete all 24 roots with worst residual `3.410004e-12`, rank `58`, worst condition `1.434585e7`, and every per-root gate passing. Each path is positive and monotone and matches its own integrated external flow within `8.29e-11` relative. Every shared-time absolute, floor-relative, volume-relative, L1, rate, algebraic, PI-memory, product, and level metric passes; the sole failed physical subgate is signed total from `t=1.25 s`, reaching `5.928153e-9 lbmol` at `t=2 s`, which is exactly the coarse/refined total-response difference. DD-188 shall not be rerun or reclassified. No longer live controlled trajectory is authorized; at most one separately frozen zero-call adjudication may determine whether controlled trajectories require a duration/response-scaled signed-total criterion.
- `FR-019eb` freezes one zero-call DD-189 adjudication of DD-188's immutable response evidence. DD-188 must retain its formal failure; its only failed campaign families must be response and shared-time refinement, with cross-grid total and signed total as their only failed subgates. Every root and all other shared-time gates must remain passing. Each path must match its integrated external flow within `1e-6` relative and component identity below `1e-6 lbmol`. The actual coarse/refined total difference must match the difference between their independently integrated external flows within `1e-10 lbmol` and remain below `1e-5` of total response. Model, provider, solver, and endpoint-regeneration calls are prohibited. A pass prospectively makes signed total diagnostic for controlled trajectories with evolving external outputs while retaining every other `FR-019do` gate, and authorizes only one separately frozen modest controlled trajectory.
- DD-189 executes `FR-019eb` once from frozen commit `eaf7fe6` and passes every gate with zero model, provider, solver, or endpoint-regeneration calls. DD-188 remains failed and is not rerun. Actual coarse-minus-refined accumulation is `-5.928153e-9 lbmol`, while the independently integrated external-flow difference is `-5.928164e-9 lbmol`; only `1.051936e-14 lbmol` is unexplained. The difference is `1.493874e-6` of response, individual path identities are below `8.29e-11` relative, and component identity is below `5.54e-13 lbmol`. Future controlled trajectories with evolving product outputs shall report signed total diagnostically and require both external-flow explanation `<1e-10 lbmol` and response-relative grid difference `<1e-5`; all other physical, controller, closure, and conservation gates remain unchanged. One separately frozen modest controlled trajectory is authorized under this policy.
- `FR-019ec` freezes one DD-190 ten-second controlled trajectory under the unchanged DD-187 disturbance, setpoints, PI constants, fixed product references, solver, and DWSIM authority plus the DD-189 controlled signed-total policy. It shall compare `40 x 0.25 s` with `80 x 0.125 s`; all 120 roots must close below `1e-8`, retain rank `58`, condition below `1e8`, physicality, equilibrium, conservation, component/energy/controller kinematics, and controller-equation closure. Both paths must be positive and monotone and match integrated external flow within `1e-6` relative with component identity below `1e-6 lbmol`. All 40 shared times must retain every non-signed-total `FR-019do` gate, `<1e-5` rate/algebraic, `<1e-7` PI-memory, `<1e-6` product, and `<1e-8` level refinement. Each total-grid difference must match independently integrated external-flow difference within `1e-10 lbmol` and remain below `1e-5` of response. Calls must remain below `650,000` and wall below `240 s`. Tuning, retry, alternate grid, projection, clipping, fallback, and parallel substitution are prohibited.
- DD-190 executes `FR-019ec` once from frozen commit `bf4112b` and formally fails shared-time refinement. Both paths complete all 120 roots with worst residual `3.953959e-12`, rank `58`, worst condition `1.434586e7`, and every per-root gate passing. Both responses are positive and monotone, match integrated external flow within `1.55e-11` relative, and have component identity below `2.84e-13 lbmol`; unexplained cross-grid total difference remains below `7.98e-13 lbmol`. Absolute, floor-relative, volume-relative, L1, rate, algebraic, PI-memory, and product refinements pass. Response-relative total difference exceeds `<1e-5` at `4.75 s` and reaches `5.313564e-5`; level refinement exceeds `<1e-8` at `7.25 s` and reaches `1.754111e-8`. DD-190 shall not be rerun. The evidence identifies insufficient grid convergence for the ten-second evolving-controller response, not loss of closure, physicality, or conservation. No controller tuning, parallel integration, or longer controlled trajectory is authorized until a new numerical-integration contract is selected explicitly.
- `FR-019ed` freezes DD-191 as a performance-and-equivalence proof at the first DD-190 refined controlled root. The serial and four-worker roots shall use the same `58 x 58`, 17-color central-difference system, state, scales, DWSIM authority, product references, controller equations, and SciPy settings. Every paired Jacobian and solver decision must be exact; every endpoint quantity must agree within `1e-12`; both roots must close below `1e-8`, retain rank `58`, condition below `1e8`, physicality, conservation, and provider ownership. Parallel solve wall excluding startup must be at least `10%` lower. Endpoint acceptance, a second timestep, trajectory, tuning, retry, or fallback is prohibited.
- DD-191 executes `FR-019ed` once from frozen commit `1bc51b5`. Both roots close at `1.621788e-12` in identical `4/4` function/Jacobian evaluations, retain rank `58` and condition `1.434585e7`, and produce bit-identical Jacobians, decisions, and endpoint quantities. Parallel solve wall falls from `2.071781 s` to `1.086940 s`, a `1.906x` speedup; all root, provider, call, and wall gates pass. The formal campaign fails only because a nondeterministically scheduled four-task startup ping observes three process IDs, even though every actual 34-task Jacobian records all four worker IDs. DD-191 remains failed and is not rerun. A separately frozen zero-call adjudication may decide whether actual Jacobian-task evidence governs worker participation.
- `FR-019ee` freezes DD-192 as that zero-call adjudication. DD-191 must retain its formal worker-participation-only failure, and every numerical, endpoint, provider, call, and performance gate must remain unchanged and passing. All four actual Jacobians must contain the same four distinct worker IDs. Startup ping identity is diagnostic only because process-pool scheduling does not guarantee one trivial task per process. Model, provider, solver, endpoint-regeneration, and DD-191 rerun/reclassification calls are prohibited.
- DD-192 executes `FR-019ee` once from frozen commit `37ce61f` and passes. Each of DD-191's four actual parallel Jacobians used the same four worker processes, while all preserved numerical and performance gates remain true. The adjudication makes zero model, provider, solver, or endpoint-regeneration calls and leaves DD-191 formally failed. Persistent four-worker Jacobian execution is authorized for controlled Core V3 steps under actual-task participation evidence. DD-190's timestep-accuracy stop remains in force; the next authorized live work is one separately frozen finer-grid controlled refinement proof, not controller tuning or a longer trajectory.
- `FR-019ef` freezes DD-193 as that finer-grid proof: the unchanged ten-second controlled case on `80 x 0.125 s` and `160 x 0.0625 s` grids through one persistent four-worker DWSIM pool. All DD-190 scientific limits remain unchanged. Every actual Jacobian must use the same four workers, each worker must rebuild the evolving root basis exactly once, logical calls must remain below `1.3e6`, trajectory wall below `208.074 s`, startup below `30 s`, and total wall below `240 s`. Tuning, retry, alternate grid, fallback, clipping, and longer duration are prohibited.
- DD-193 executes once from frozen commit `43fb3bd` but is operator-stopped after at least `1503 s`, more than `6.26x` the frozen total-wall limit. The parent and four workers remain active and CPU-bound until stopped; no complete result artifact or endpoint is produced, so scientific trajectory and refinement gates are explicitly unevaluated. Code inspection identifies the scaling defect: each worker invokes `ProviderCallAudit.report()` after every perturbation task, while that report rescans and groups the complete accumulated ledger. DD-193 is retired without retry and establishes no model, thermo, controller, or grid-convergence verdict. A successor is authorized only to implement and test incremental worker audit evidence plus an in-execution wall deadline before any separately frozen short fine-grid proof.
- `FR-019eg` freezes DD-194 as a two-second qualification of that correction before any ten-second retry. Provider audit length shall be constant-time and worker validation shall inspect only records appended by the current task. The trajectory runner shall stop between roots at an in-execution deadline and preserve an explicit incomplete result. One persistent pool shall compare `16 x 0.125 s` with `32 x 0.0625 s`, retain every DD-193 scientific/worker/provider gate, finish trajectory work below `80%` of the `55.4865 s` projected serial baseline, and finish all governed work below `90 s`. DD-193 remains retired; no ten-second conclusion, tuning, retry, or alternate grid is authorized.
- DD-194 executes once from frozen commit `bf2db7f` and formally fails only its performance gate. All 48 roots complete with worst residual `1.1064e-11`, rank `58`, worst condition `5.7377e7`, and every scientific, physical, response, worker-participation, basis-refresh, provider, call, startup, and total-wall gate passing. Shared-time response-relative total difference is `5.8920e-7`, level difference `2.5612e-10`, absolute component difference `7.9243e-6 lbmol`, and unexplained total difference `1.0027e-12 lbmol`. Incremental audit removes DD-193's runaway scaling, but trajectory wall is `52.396 s` versus `55.487 s` projected serial: `1.059x` speedup and `0.9443x` ratio, above the required `<0.8`. DD-194 is not rerun and does not authorize the ten-second finer-grid campaign. Brute-force timestep halving and parallel-worker tuning are stopped; the next permissible work is a property-free structural contract for a higher-order implicit integration formula.
- `FR-019eh` requires that higher-order work to begin with a property-free constant-step BDF2 ownership contract. It shall preserve the controlled Core V3 solve variables, rows, topology-generated dependencies, positive inventory map, live terminal output ownership, and provider-derived energy definition. Two fixed history levels shall explicitly own every component inventory, derived volume internal energy, and PI memory. Exactly one accepted existing backward-Euler step shall create startup history; any timestep change shall invalidate BDF2 history and require a new startup. No property, residual, solve, timestep, or trajectory call is permitted. A full-rank generic structural pass may authorize only a BDF2 residual implementation and property-free identity tests.
- DD-195 implements `FR-019eh` and passes. The seven-volume/three-component system remains `58 x 58`, structural rank `58`, nullity zero, with 60 explicit fixed history values: `2 x 21` component inventories, `2 x 7` derived internal energies, and `2 x 2` PI memories. The eight-volume/four-component case remains `82 x 82`, rank `82`, with 84 history values. Component and energy derivatives use `(3*y[n+1]-4*y[n]+y[n-1])/(2*dt)`; PI memory has the exact corresponding endpoint map; positive inventories retain the exponential endpoint map with effective BDF2 rates. The contract is constant-step only and uses one accepted backward-Euler startup. No live call or integration occurs. One BDF2 residual implementation plus property-free stationary identity audit is authorized; DWSIM roots and trajectories remain stopped.
- `FR-019ei` requires the DD-195 BDF2 implementation to preserve the accepted controlled residual ownership while explicitly mapping two fixed history levels to the endpoint component inventories, provider-derived internal energies, and PI memories. The component balances shall receive the exact BDF2 rates implied by the positive exponential endpoint map; energy storage shall enter each volume energy row exactly once; PI-memory coordinates shall reproduce the BDF2 derivative exactly. Identical histories and a zero-rate endpoint shall produce an exactly zero property-free assembled residual with zero provider calls. Linear/quadratic kinematics, generic topology, invalid history, and timestep-change rejection shall be tested. No live residual, Jacobian, solve, timestep, or trajectory is permitted.
- DD-196 implements `FR-019ei` and passes. The constant-step history, positive endpoint map, effective component rates, provider-derived energy storage rates, PI-memory inverse, and controlled residual assembler are implemented without changing physical row ownership. Identical history produces exactly zero component, energy, PI, and assembled residual motion with zero provider calls; linear and quadratic derivative checks are exact, and PI inverse round-trip error is `1.609823385706477e-15`. Eighteen focused DD-195/DD-196 tests pass, including eight-volume/four-component genericity and strict invalid-history/timestep rejection. No live property evaluation, Jacobian, solve, accepted step, or trajectory occurs. One separately frozen live stationary residual/Jacobian parity audit is authorized; root solving and dynamics remain stopped.
- `FR-019ej` freezes one DD-197 live stationary parity audit at the accepted DD-185 seven-volume controller handoff with identical current/prior BDF2 histories and `dt=0.125 s`. It shall compare the BDF2 stationary residual against the backward-Euler reference, build dense central-difference BDF2 Jacobians at `1e-5` and `5e-6` plus one backward-Euler Jacobian at `1e-5`, retain rank `58`, condition below `1e8`, stable BDF2 spectra, registered structure, exactly stationary inventory/component/energy/PI motion, physicality, equilibrium, conservation, and provider ownership. BDF2/backward-Euler matrix difference is diagnostic because the formulas have different derivative weights. A nonlinear solve, accepted timestep, tuning, retry, or trajectory is prohibited.
- DD-197 executes `FR-019ej` once from frozen commit `46a84c7` and passes every gate. The BDF2 residual is `4.979842e-13` and differs from the backward-Euler stationary residual by exactly `0.0`. Both BDF2 matrices and the backward-Euler reference retain rank `58`; worst condition is `3.172742e7`, and BDF2 finite-difference spectrum change is `1.078595e-7`. Inventory, component, energy, and PI-memory motion are exactly zero. The BDF2/backward-Euler matrix difference is `0.3330511`, consistent with different time-derivative weighting and not an acceptance gate. Physicality, equilibrium, conservation, provider, call, and wall gates pass with `11,934` calls in `5.230 s`. No solve or timestep occurs. One separately frozen BDF2 moving-step contract is authorized; trajectories and tuning remain stopped.
- `FR-019ek` freezes DD-198 as one moving `0.125 s` BDF2 endpoint after the accepted DD-187 backward-Euler startup step under the unchanged `+0.1%` feed-rate/feed-enthalpy disturbance. The stationary DD-185 state and accepted DD-187 first half-step shall provide the two fixed history levels. Solver, scales, controller settings, product references, DWSIM authority, and timestep shall remain fixed. The one root must close below `1e-8`, retain rank `58`, condition below `1e8`, physicality, equilibrium, conservation, controller closure, exact BDF2 kinematics, and provider ownership. Its endpoint must remain within frozen inventory/rate/algebraic/controller/product/level limits of DD-187's second half-step and be closer than refined backward Euler to the fixed first-order Richardson inventory estimate. Retry, alternate timestep, tuning, fallback, or trajectory is prohibited.
- DD-198 executes `FR-019ek` once from frozen commit `e7a60ae` and passes every gate. The BDF2 root closes at `3.528219e-12` in `4/2` residual/Jacobian evaluations, retains rank `58` and condition `3.172741e7`, and passes every physical, equilibrium, conservation, controller, kinematic, provider, call, and wall gate. Maximum BDF2/backward-Euler inventory difference is `8.664731e-7 lbmol`; L1 difference is `3.094640e-6 lbmol`; rate and algebraic differences remain below `7.16e-8`. BDF2's Richardson maximum error is `1.669391e-6 lbmol` versus `2.535865e-6 lbmol` for refined backward Euler, a ratio of `0.658313` and about `34.2%` improvement. Execution uses `2,550` calls in `6.252 s`. No retry, alternate step, tuning, or trajectory occurs. One separately frozen short BDF2 grid-refinement contract is authorized; longer dynamics remain stopped.
- `FR-019el` freezes DD-199 as a two-second controlled BDF2 refinement proof on `8 x 0.25 s` and `16 x 0.125 s` grids. Each path shall use exactly one backward-Euler startup followed by constant-step BDF2. Every root must retain closure, rank `58`, condition below `1e8`, physicality, equilibrium, conservation, controller closure, provider ownership, and bounded work. Eight shared times must pass the existing physical/controller refinement policy, response differences must be explained by the BDF2 external-flow recurrence, and worst shared maximum/L1 inventory differences must each be below `0.8` times immutable DD-188 backward-Euler evidence. Retry, alternate grid, tuning, fallback, or longer duration is prohibited.
- DD-199 executes once from frozen commit `ed6ffbf` and aborts before a scientific result. The backward-Euler startup and first coarse BDF2 endpoint complete, but history handoff before the third root reads `endpoint_inventory_lbmol` directly from `TerminalInventoryControlBDF2Evaluation`; BDF2 stores accepted inventory, energy, and PI memory under `kinematics`. No path completes, no result endpoint is accepted, and no shared-time or scientific gate is classified. DD-199 is retired without rerun or reclassification. A method-aware accessor correction plus a regression chaining at least three BDF2-shaped evaluations may authorize one separately numbered unchanged-science successor; no equation, provider, grid, tolerance, or controller change is authorized.
- DD-200 repeats the unchanged `FR-019el` science once from corrected contract commit `4018820` and formally fails only the inherited absolute signed-total subgate at `1.75 s` and `2.0 s`. All 24 roots complete with worst residual `4.326583e-12`, rank `58`, worst condition `3.172742e7`, and every root, response, provider, call, wall, and non-signed-total refinement gate passing. Worst shared inventory max/L1 differences are `5.061785e-6` and `1.803948e-5 lbmol`, only `0.324749` and `0.325916` of DD-188 backward Euler. Final cross-grid total difference is `1.982463e-9 lbmol`, explained by external-flow difference within `2.509548e-12 lbmol` and only `4.995733e-7` of response. DD-200 remains failed and is not rerun. One zero-call DD-201 adjudication may apply DD-189's prospective controlled-response policy at every shared time; live work remains stopped.
- `FR-019em` freezes DD-201 as a zero-call adjudication of DD-200 under DD-189's prospective controlled-response policy. DD-200 must remain formally failed; its only failed shared subgate must be absolute signed total, and every other root, physical, accuracy, response, provider, call, and wall gate must remain passing. At all eight shared times, coarse/refined total difference must match the difference reconstructed independently from saved feed/product rates using one BE startup plus the BDF2 recurrence within `1e-10 lbmol` and remain below `1e-5` of response. Model, provider, solver, endpoint-regeneration, rerun, and reclassification calls are prohibited.
- DD-201 executes `FR-019em` once from frozen commit `e84a0bf` and passes every gate with zero model, provider, solver, or endpoint-regeneration calls. DD-200 remains formally failed. All eight cross-grid total differences are independently explained; worst unexplained difference is `1.364242e-12 lbmol`, and worst response-relative difference is `4.994035e-7`. Every non-signed-total DD-200 gate remains passing. The combined evidence validates the two-second BDF2 refinement and its approximately `67.5%` reduction in worst max/L1 grid error versus DD-188 backward Euler. One separately frozen modest BDF2 trajectory contract is authorized; controller tuning remains stopped.
- `FR-019en` freezes DD-202 as the ten-second BDF2 counterpart to DD-190 under the unchanged disturbance, controllers, product references, DWSIM authority, and `40 x 0.25 s` / `80 x 0.125 s` grids. Each path shall use one backward-Euler startup followed by constant-step BDF2. All 120 roots and 40 shared times must retain closure, rank `58`, condition below `1e8`, physicality, equilibrium, conservation, controller closure, and the DD-189/DD-201 response-scaled total policy. Worst maximum and L1 inventory differences must each be below `0.8` times the immutable DD-190 backward-Euler values. Calls must remain below `600,000` and wall below `360 s`. Retry, tuning, alternate grid, fallback, or longer duration is prohibited.
- DD-202 executes `FR-019en` once from frozen commit `9b40404` and passes every gate. All 120 roots and 40 shared times pass with worst residual `8.375332e-12`, rank `58`, and worst condition `3.172745e7`. Worst maximum/L1 inventory differences are `5.061785e-6` and `1.803948e-5 lbmol`, only `0.189315` and `0.149508` of DD-190 backward Euler, reductions of `81.07%` and `85.05%`. Worst level refinement is `1.225224e-9`; worst response-relative total difference is `5.203283e-6`; external flow explains cross-grid total differences within `7.275958e-12 lbmol`. Provider ownership passes with `542,368` calls in `159.195 s`. The DD-190 timestep-accuracy stop is resolved for this ten-second case. One separately frozen controlled BDF2 integration milestone is authorized; tuning and unrestricted production dynamics remain stopped.
- `FR-019eo` requires the DD-202-authorized BDF2 integration milestone to expose production orchestration without altering physical equations or accepted default sequencing. The runner shall retain one backward-Euler startup plus constant-step BDF2, accept separately qualified startup and BDF2 solver callables, enforce a finite monotonic deadline before every root, report deadline and root-failure stops explicitly, expose requested duration and the final accepted outcome, and reject endpoint access when no root completed. Property-free tests shall cover default and injected routing, both deadline boundaries, both root-failure families, history chaining, result metadata, and invalid grids. Passing authorizes only one frozen live serial/performance-path equivalence proof; longer integration and tuning remain stopped.
- DD-203 implements `FR-019eo` and passes 15 focused property-free tests. The default serial DD-202 sequence is unchanged; optional startup/BDF2 solver injection, between-root deadline enforcement, explicit `deadline`/`root_failure` reporting, requested duration, and endpoint access are added to the trajectory contract. No property, residual, Jacobian, nonlinear-solve, timestep, or trajectory call occurs. One separately frozen live equivalence proof may route the accepted startup and first BDF2 root through existing qualified performance machinery. It must establish identical solver decisions, Jacobians, and endpoint states before a performance-enabled trajectory is accepted.
- `FR-019ep` freezes DD-204 as one live serial-versus-persistent-parallel equivalence proof containing exactly one `0.125 s` backward-Euler startup and one `0.125 s` BDF2 root. Both paths shall retain all DD-202 root gates. Every paired 17-color Jacobian shall agree within `1e-10`; all solver metadata and endpoint quantities shall agree within `1e-12`. Every actual matrix shall use four workers, with each worker rebuilding the current root basis exactly once. Provider ownership and fallback gates shall pass; logical calls shall remain below `60,000`, adjusted startup below `30 s`, and governed wall below `180 s`. The warm-pool parallel trajectory shall be at least `1.10x` faster than serial. Retry, alternate step, tuning, clipping, projection, fallback, and longer integration are prohibited.
- DD-204 executes `FR-019ep` once from frozen commit `72473eb` and passes every gate. Both paths complete the startup and BDF2 root with worst residual `1.868842e-12`, rank `58`, and worst condition `3.172741e7`. All seven paired Jacobians, solver decisions, and endpoint quantities differ by exactly `0.0`. Every matrix uses four workers; each worker rebuilds exactly once per root. Provider ownership passes without fallback in `17,068` logical calls. Warm-pool wall falls from `9.543881 s` serial to `7.369045 s` parallel, a `1.295x` speedup; adjusted startup is `2.035537 s` and governed wall is `25.378223 s`. The persistent four-worker path is authorized for one separately frozen longer BDF2 trajectory. Long-horizon economics and dynamics remain unproven; tuning and unrestricted production use remain stopped.
- `FR-019eq` freezes DD-205 as an exact persistent-parallel replay of DD-202's `40 x 0.25 s` and `80 x 0.125 s` BDF2 paths. One four-worker pool shall serve all 120 roots. Every original root, physical, accuracy, response, conservation, and provider gate shall pass; all saved scientific evidence shall agree within `1e-12`; every Jacobian shall use all workers; each worker shall rebuild its basis exactly once per root; logical calls shall remain below `650,000`; adjusted startup and governed wall shall remain below `30 s` and `180 s`; and trajectory wall shall improve by at least `1.10x`. Retry, alternate grid, tuning, fallback, clipping, and projection are prohibited.
- DD-205 executes `FR-019eq` once from frozen commit `69e88f2` and formally fails only the saved-result representation gate. All 120 roots and all scientific, physical, accuracy, response, worker, provider, call, and performance gates otherwise pass. Across 11,999 numeric leaves the maximum difference from DD-202 is exactly `0.0`. The comparator nevertheless treats in-memory diagnostic index tuples as metadata-different from their saved JSON list representations at 40 shared times. Once serialized, the complete coarse, refined, shared-time, response, cross-grid, and response-gate objects are exactly equal. Trajectory wall falls from `159.194887 s` to `77.766701 s`, a `2.047x` speedup; governed wall is `93.154234 s`. DD-205 remains formally failed and shall not be rerun. One separately frozen zero-call persisted-artifact adjudication is authorized; another live trajectory is not.
- `FR-019er` freezes DD-206 as a zero-call adjudication of DD-205's sole evidence-representation failure. It shall compare the persisted DD-202 and DD-205 coarse, refined, shared-time, response, cross-grid, and response-gate objects by exact canonical JSON; require DD-205's numerical comparison to be zero; require its only failed campaign gate to be saved-result equivalence; and require exactly the known tuple/list diagnostic-index mismatch pattern. Model, provider, residual, solver, timestep, trajectory, retry, and result regeneration calls are prohibited.
- DD-206 executes `FR-019er` once from frozen commit `32495a1` and passes every gate in `0.031217 s` with zero model/provider/solver/trajectory calls. The complete persisted scientific objects are exactly equal and share canonical hash `f3e71c2a441f49086fa1931f92892e8a1431e63dfed49e11d8e40fceb69d0f42`; all 120 representation paths are exactly the expected diagnostic-index tuple/list set. DD-205 remains formally failed and is not rerun. Its 120-root live evidence is scientifically accepted, and the persistent four-worker BDF2 trajectory path is adopted. Future production integration shall preserve main-process nonlinear ownership, per-root worker-basis refresh, actual-task participation evidence, and the DD-203 deadline contract.
- `FR-019es` requires the DD-206-authorized persistent-parallel BDF2 path to become reusable Core V3 production code without live work. A generic coordinator shall operate on a caller-owned persistent executor, preserve deterministic colored central differences, enforce complete results and actual participation by every worker, require exactly one basis rebuild per worker and root, reject provider fallback, and retain immutable evidence. A method-aware adapter shall package backward-Euler startup and complete two-level BDF2 bases. The production trajectory shall accept the backend directly, reject conflicting individual overrides, retain the serial default, and preserve DD-203 between-root deadlines. Property, residual, Jacobian, solve, timestep, and trajectory calls are prohibited.
- DD-207 implements `FR-019es` and passes 450 Core V3 tests. `PersistentParallelColoredJacobian` now owns deterministic task dispatch, evidence, worker participation, basis-lifecycle, and provider gates around a caller-owned executor. `TerminalInventoryControlBDF2ParallelStepSolvers` packages distinct startup and BDF2 worker bases and supplies the existing main-process step solvers. The trajectory accepts one `step_solver_backend`, rejects mixed override ownership, and retains its unchanged serial default and deadline stops. No live call or scientific setting changes. One separately frozen short live equivalence replay through the reusable source backend is authorized; longer integration remains stopped.
- `FR-019et` freezes DD-208 as one live replay through the DD-207 production source backend. It shall execute exactly one `0.125 s` backward-Euler startup and one `0.125 s` BDF2 root, compare both serialized root reports exactly with DD-204's accepted parallel reports, produce the same seven Jacobians, use all four workers per matrix, rebuild each worker basis exactly once per root, retain every inherited numerical/physical/provider gate, remain below `30,000` logical calls and `60 s` governed wall, and keep trajectory wall within `1.25x` DD-204. Retry, alternate step, tuning, fallback, clipping, projection, and longer integration are prohibited.
- DD-208 executes `FR-019et` once from frozen commit `e949a6f` and passes every gate. Both root reports reproduce DD-204 exactly with maximum numeric difference `0.0`; worst residual is `1.868842e-12`, rank is `58`, and worst condition is `3.172741e7`. All seven Jacobians use four workers, and both roots rebuild exactly four worker bases. Provider ownership passes without fallback in `8,602` logical calls. Production-backend trajectory wall is `8.551296 s`, `1.160x` DD-204 and inside the `1.25x` gate; governed wall is `17.103413 s`. Campaign-local parallel closures are retired for future execution and retained only as historical evidence. The reusable production backend is the accepted parallel BDF2 path.
- `FR-019eu` freezes DD-209 as one 30-second controlled BDF2 horizon test through the accepted reusable production backend. It shall preserve DD-202's disturbance, controllers, equations, DWSIM PR authority, solver settings, and `0.25/0.125 s` grids while extending each path to `30 s` (`120/240` roots). All `360` roots and `120` shared times must pass the frozen numerical, physical, equilibrium, conservation, controller, absolute refinement, and response-scaled gates. Every Jacobian must use all four workers with exactly one basis rebuild per worker and root; logical calls must remain below `2,000,000`, adjusted startup below `30 s`, and governed wall below `300 s`. DD-202 historical error ratios are diagnostic only at the longer horizon. Retry, tuning, alternate grid, fallback, clipping, projection, and equation changes are prohibited.
- DD-209 executes `FR-019eu` once from frozen commit `dcea15b` and passes every gate. Both `120 x 0.25 s` and `240 x 0.125 s` paths complete all `360` roots. Worst residual is `8.375332e-12`, rank remains `58`, and worst condition is `3.172800e7`. All `120` shared times pass; worst maximum/L1 inventory differences are `5.061785e-6` and `1.803948e-5 lbmol`, with coarse/refined total accumulation of `0.0586497688/0.0586501094 lbmol`. All `1,362` Jacobians use four workers, all roots satisfy basis lifecycle, and DWSIM ownership passes without fallback. The run uses `1,640,840` logical calls, `219.115 s` trajectory wall, and `250.435 s` governed wall. One separately frozen 60-second production BDF2 horizon test is authorized; unrestricted duration, controller tuning, and production economics remain unauthorized.
- `FR-019ev` freezes DD-210 as one four-versus-eight worker scaling proof through the accepted production backend. Each path shall execute exactly one `0.125 s` backward-Euler startup and one `0.125 s` BDF2 root from the same state with unchanged equations, DWSIM authority, solver, scales, controllers, and central-difference pattern. Full Jacobians and serialized root reports must agree within `1e-12`; every matrix must use every configured worker and every root must rebuild exactly one basis per worker. Eight-worker warm trajectory wall must improve by at least `1.30x`; logical calls must remain below `30,000`, adjusted startup below `30 s`, and governed wall below `120 s`. Retry, alternate worker count, tuning, fallback, clipping, projection, and longer integration are prohibited.
- DD-210 executes `FR-019ev` once from frozen commit `f0aa4ca` and passes every gate. Four- and eight-worker paths complete both roots and seven Jacobians with zero matrix and report difference. Every matrix uses all configured workers and each root rebuilds exactly four or eight worker bases. Warm trajectory wall falls from `8.253709 s` to `5.376950 s`, a `1.535017x` speedup; adjusted startup is `1.784917/2.421944 s`. Provider ownership passes without fallback in `17,340` combined logical calls, and governed wall is `31.264918 s`. Eight workers are adopted as the production Jacobian default for this eight-physical-core machine. One separately frozen default-off linear coordinate-predictor proof is authorized; Jacobian reuse, derivative changes, and longer integration remain independently gated.
- `FR-019ew` permits a property-free, default-off BDF2 initial-guess policy. The accepted default shall remain the previous accepted coordinate vector. The optional policy may linearly extrapolate the complete next solve-coordinate guess from the two most recent accepted coordinate vectors. It shall not change physical history, equations, residuals, Jacobians, bounds, scales, provider ownership, solver settings, or state acceptance. Coordinate history may advance only after an accepted root. Tests must cover multi-step extrapolation, unchanged default behavior, invalid-policy rejection, and backend compatibility. No property, residual, Jacobian, solve, timestep, or trajectory call is authorized by implementation.
- DD-211 implements `FR-019ew` and passes 22 focused property-free trajectory/backend tests. `bdf2_initial_guess_policy="accepted_endpoint"` preserves all existing behavior; `"linear_extrapolation"` computes `q[n] + (q[n] - q[n-1])` from accepted coordinate history only. Unknown policies fail before live work, and failed roots cannot advance coordinate history. No physical equation, BDF2 history, provider, Jacobian, solver, scale, bound, or acceptance rule changes. The predictor remains disabled in production. One separately frozen eight-worker live performance/equivalence proof is authorized; failure retains the accepted-endpoint policy without retry.
- `FR-019ex` freezes DD-212 as one ten-second, eight-worker comparison of `accepted_endpoint` and `linear_extrapolation` initial guesses on the same `40 x 0.25 s` controlled BDF2 path. Each path shall use a separate fresh worker pool and unchanged DD-209 science, equations, DWSIM authority, solver, bounds, scales, controllers, central differences, and acceptance gates. Both must complete all roots; accepted inventories, rate/algebraic coordinates, controller memory, levels, products, and physical diagnostics must agree within `1e-9`. The predictor must reduce Jacobian count by at least `10%`, logical calls by at least `8%`, and warm trajectory wall by at least `1.10x`. All worker/provider/call/startup/wall gates remain mandatory. Retry, alternate predictor/grid/duration, tuning, clipping, projection, fallback, and equation changes are prohibited.
- DD-212 executes `FR-019ex` once from frozen commit `5521bd6` and passes every gate. Both paths complete all 40 roots. The maximum accepted-science difference is `3.046807e-10`. Jacobians fall from `142` to `114` (`19.718%`), logical calls fall from `171,360` to `138,822` (`18.988%`), and warm trajectory wall falls from `24.007053 s` to `18.844021 s`, a `1.273988x` speedup. Every matrix uses all eight workers, every root satisfies basis lifecycle, and provider ownership passes without fallback. `linear_extrapolation` is adopted for explicit production controlled-BDF2 recipes; the library default remains unchanged for compatibility. One separately frozen 60-second two-grid production milestone using eight workers and the predictor is authorized. Jacobian reuse, derivative changes, and unrestricted duration remain unauthorized.
- `FR-019ey` freezes DD-213 as one 60-second, two-grid production milestone using the accepted eight-worker backend and explicit `linear_extrapolation`. It shall preserve DD-209 science, DWSIM PR authority, equations, solver, bounds, scales, controllers, and `0.25/0.125 s` grids while completing `240/480` roots and all 240 shared-time comparisons. All root, physical, equilibrium, conservation, response, worker, provider, call, startup, and absolute refinement gates remain mandatory. Logical calls must remain below `3,200,000`, adjusted startup below `30 s`, and governed wall below `300 s`. Retry, alternate grid, tuning, fallback, clipping, projection, and equation changes are prohibited.
- DD-213 executes `FR-019ey` once from frozen commit `4686e89` and completes all `720/720` requested roots without a solver or provider stop. Every scientific, physical, equilibrium, conservation, response, worker, basis, provider, call, and startup gate passes. Worst residual is `9.633348e-12`, worst condition is `3.172937e7`, and worst shared max/L1 inventory differences remain `5.061785e-6/1.803949e-5 lbmol`. The sole failed gate is governed wall: `308.084 s` against `<300 s`, despite `276.857 s` trajectory wall. Source timing isolates `27.933 s` between trajectory completion and executor exit; response analysis and serialization occur later and are excluded. DD-213 is not rerun or reclassified. Longer integration remains unauthorized; a property-free reusable production-session lifecycle may be designed so DWSIM worker teardown is not charged to every trajectory segment.
- `FR-019ez` permits a property-free reusable production-session lifecycle around the accepted parallel BDF2 backend. The session may build one caller-supplied executor, retain it across uniquely named trajectory calls, inject only the accepted parallel step-solver backend, record startup/segment/shutdown timing, and close exactly once under caller control. It must reject duplicate trajectory names, backend overrides, use after close, restart after close, and invalid configuration before executor construction. Failed names remain reserved. Startup ping participation is diagnostic; all-worker enforcement remains mandatory on every Jacobian. No property, residual, Jacobian, solve, timestep, or trajectory call is authorized by implementation.
- DD-214 implements `FR-019ez` and passes 24 focused property-free session/backend/trajectory tests, including seven new lifecycle tests. One backend remains live across multiple trajectory calls, no shutdown occurs between calls, shutdown is measured exactly once on explicit or context-managed close, and closed sessions cannot expose the dead backend. The implementation composes the accepted Jacobian coordinator, parallel step solvers, and trajectory without changing science. One separately frozen short live two-trajectory session proof is authorized; DD-213 remains formally failed and longer integration remains unauthorized.
- `FR-019fa` freezes DD-215 as one short live proof of the DD-214 lifecycle. One eight-worker DWSIM session shall execute exactly `8 x 0.25 s` and `16 x 0.125 s` controlled BDF2 trajectories from the same accepted root with explicit linear prediction. The session must remain open with no shutdown between uniquely named paths, close exactly once afterward, and report startup, each segment, shutdown, and total wall separately. All inherited root, physical, equilibrium, conservation, response, worker, basis, provider, and refinement gates remain mandatory. Calls must remain below `180,000`; startup below `30 s`, shutdown below `60 s`, and complete session wall below `120 s`. Retry, alternate grid, tuning, fallback, clipping, projection, and equation changes are prohibited.
- DD-215 executes `FR-019fa` once from frozen commit `a44eec9` and passes every gate. Both paths complete all `24/24` roots with worst residual `5.644387e-12`, worst condition `3.172742e7`, and accepted shared max/L1 inventory differences of `5.061785e-6/1.803949e-5 lbmol`. Every Jacobian uses all eight workers and all roots satisfy basis lifecycle. The session remains open between paths and closes once at the end. Startup, active trajectories, shutdown, and total session wall are `3.015/22.603/8.310/33.929 s`; calls total `84,048`. The reusable session lifecycle is adopted. This proves lifecycle amortization, not faster nonlinear roots, and does not reclassify DD-213. A separately frozen production-accounting contract must define active-segment and complete-session performance gates before another long run.
- `FR-019fb` requires executable production-session timing policy before another long live run. The policy shall freeze unique ordered segment names and independent maximum wall limits for every segment, all active work, startup, final shutdown, complete session, and unattributed overhead. It shall require finite nonnegative observations, completed segments, exact segment-name/order agreement, active-time identity, shutdown and total presence, and a closed accounting identity within frozen tolerance. No timing interval may be silently excluded. Policy implementation and saved-evidence assessment must make zero model/provider calls.
- DD-216 implements `FR-019fb`. Seven new policy tests and 14 focused session/timing tests pass property-free. Static assessment of DD-215 passes startup `3.015291 < 5 s`, coarse `15.817244 < 20 s`, refined `6.785416 < 10 s`, active `22.602660 < 25 s`, shutdown `8.309981 < 10 s`, and total `33.928759 < 40 s`. Startup plus segments plus shutdown explains total wall within `0.000827 s`, below the `0.1 s` unattributed limit. One separately frozen already-qualified single-grid production-segment contract may now be designed with both active and complete-session gates; unrestricted duration remains unauthorized.
- `FR-019fc` freezes DD-217 as one 60-second single-grid production qualification. It shall execute exactly `240 x 0.25 s` roots through one reusable eight-worker session with explicit linear prediction. Every accepted science record and response report must agree with DD-213's qualified coarse path within `1e-8`. Root, response, physical, conservation, provider, worker, basis, call, and deadline gates remain mandatory. Startup, active segment, shutdown, complete session, and unattributed wall must remain below `10/180/30/225/1 s`; calls below `1,200,000`. Retry, alternate grid, tuning, fallback, clipping, projection, and equation changes are prohibited.
- DD-217 executes `FR-019fc` once from frozen commit `d089867` and passes every gate. All `240/240` roots and the response close; worst residual is `4.046842e-12`, worst condition is `7.933880e6`, all 688 Jacobians use eight workers, and all root bases pass. All accepted science and response values match DD-213 exactly with difference `0.0`. Startup/active/shutdown/total/unattributed wall are `2.731425/116.236862/15.024129/134.004839/0.012423 s`, and 834,632 logical calls remain below limit. The 60-second `0.25 s` BDF2 production segment is adopted. A property-free continuation payload and no-new-startup handoff are required before another live segment; unrestricted integration remains unauthorized.
- `FR-019fd` freezes DD-218 as one uninterrupted five-minute dynamic execution from the accepted steady root. It shall execute `1200 x 0.25 s` roots through one reusable eight-worker session, preserve DD-217's first 240 accepted science records within `1e-8`, and apply every inherited root, conservation, provider, worker, basis, call, deadline, and timing gate. Startup/active/shutdown/total/unattributed wall must remain below `10/900/45/960/2 s`; calls below `5,500,000`. Full step, worker, and response-history evidence shall be canonically hashed, with the first, every five-second, and final states retained. Retry, alternate grid, tuning, fallback, clipping, projection, and equation changes are prohibited.
- DD-218 executes `FR-019fd` once from frozen commit `8085790`, completes all 1,200 roots, and formally fails only the inherited `total_inventory_strictly_increasing` response gate. Every root, prefix, timing, worker, basis, provider, call, and deadline gate passes. Worst residual is `5.344218e-12`, condition `7.934670e6`, the DD-217 prefix difference is `0.0`, and inventory recurrence closes to `1.432099e-11 lbmol`. Startup/active/shutdown/total wall are `2.851/417.543/38.418/458.846 s`; calls total 3,891,028. Sampled inventory peaks near `280 s` and declines through the final four samples while bottoms withdrawal rises monotonically. DD-218 remains formally failed without rerun. One separately frozen zero-call controlled-response adjudication is authorized.
- `FR-019fe` requires the DD-218 response-only failure to be adjudicated from its immutable saved evidence without a model, provider, solver, timestep, or trajectory call. Acceptance requires all 1,200 roots and every nonresponse campaign and endpoint gate to remain passed; exact inventory recurrence; bounded inventory excursion below `1 lbmol`; a late peak between `240` and `295 s`; at least four final declining samples with a peak-to-final correction above `1e-4 lbmol`; monotonically increasing bottoms withdrawal; monotonically decreasing distillate withdrawal; and physical terminal levels. The DD-218 formal classification shall not be changed. DD-219 aborts before producing a result because NumPy boolean scalars are not JSON serializable. DD-220 is the sole authorized successor and may change only recursive conversion to native JSON scalar types.
- DD-220 executes `FR-019fe` once from frozen commit `ce9f49d` and passes every gate with zero live calls. Inventory starts at `2404.021619110 lbmol`, peaks at `2404.288656299 lbmol` at `280 s`, and declines through four final samples to `2404.288048638 lbmol`. Bottoms withdrawal rises from `4922.021660` to `4929.404745 lbmol/h`, distillate withdrawal falls from `2220.952341` to `2220.948074 lbmol/h`, and levels remain between `0.427453` and `0.459899`. Component inventory identity closes to `1.432099e-11 lbmol` and total inventory relative error is `6.153598e-11`. The DD-218 science is accepted as controlled five-minute dynamics; DD-218's original formal label remains unchanged. This acceptance applies to the seven-volume Core V3 reduced column and does not yet authorize or validate the full production-column model.
- `FR-019ff` requires a property-free structural migration gate before Core V3 may evaluate the full C3/C4 source. The gate shall derive the topology from the tracked 20-stage workbook and staged feed, require exactly one reflux drum, ten generic rectifying volumes, stage-12 feed volume, seven generic stripping volumes, and one combined reboiler/sump, and map every source stage exactly once. For three components, the provider registry, dynamic DAE, terminal-controlled DAE, and controlled BDF2 systems shall be square and full-rank at `160`, `158`, `162`, and `162`; BDF2 shall own `164` complete history values. Structural component/energy conservation, generic interior ownership, valid source geometry/controller metadata, and zero live/numerical calls are mandatory. The workbook profile remains an audit point rather than an accepted root.
- DD-221 implements `FR-019ff` and passes every gate. The 20-stage source has feed stage 12 and maps to `10/1/7` rectifying/feed/stripping interior volumes with complete terminal ownership. Structural ranks are `160/160`, `158/158`, `162/162`, and `162/162`; all conservation, mapping, controller, history, and preparation-only checks pass. A deterministic Hopcroft-Karp matcher resolves SciPy's multi-minute structural-rank scaling stall, reducing the complete audit to about two seconds while matching SciPy on smaller regression patterns. All 502 Core V3 tests pass. No DWSIM, property, residual, Jacobian, solve, timestep, or integration occurs. One separately frozen full-source mapping and live residual/Jacobian readiness audit is authorized; a full root solve and dynamics remain unauthorized.
- `FR-019fg` freezes one DD-222 full-C3/C4 live-readiness audit at the source-derived 20-stage workbook point. The source point shall remain explicitly unaccepted as a root. The audit shall reconstruct the condenser bubble state and duty with DWSIM Peng-Robinson, evaluate one complete 160-row residual, and build two structurally colored central-difference Jacobians at `1e-5` and `5e-6`. Both matrices must retain rank `160`, condition below `1e8`, stable spectra, complete local condenser rank, and no zero registered rows or columns. Seventeen predeclared columns spanning every coordinate family shall be independently differenced and agree with the colored matrix below `1e-6` relative. Physicality, exact component/energy telescoping, condenser bubble closure, liquid TP-flash status, independent-PR agreement, provider ownership, calls below `100,000`, and wall below `900 s` are mandatory. The source residual magnitude is diagnostic and is not a root-acceptance gate. No nonlinear solve, timestep, controller execution, or integration may occur. A complete pass authorizes only one separately frozen full-C3/C4 stationary-root campaign.
- DD-222 executes `FR-019fg` once from frozen commit `f9d24c8` and passes every gate. Both complete live Jacobians retain rank `160/160`, local condenser rank `3/3`, worst condition `3.080727e6`, and spectrum change `2.186122e-9`. All 17 direct-column checks match the colored matrix exactly. Component and energy telescoping close at `8.594582e-17` and `2.766423e-17`; bubble, TP-flash, independent-PR, physicality, and provider gates pass. Structural coloring reduces the frozen audit from 643 prospective residual evaluations to 97; execution uses 9,634 logical property calls in `7.593 s` audited wall (`18.4 s` total process wall including startup/reporting). The source point's `0.547063` scaled residual is diagnostic and is dominated by lower-section Francis hydraulics, confirming that the workbook is a starting point rather than an accepted Core V3 root. One separately frozen full-C3/C4 stationary-root campaign is authorized; timestep and dynamic integration remain unauthorized.
- `FR-019fh` freezes one DD-223 full-C3/C4 stationary-root campaign. Exactly two complete starts are allowed: the DD-222 source audit point and one independently constructed smooth topology-scale state. Both shall use the same transformed physical bounds, residual scales, DWSIM Peng-Robinson governing properties, `least_squares(method="trf")`, 15-color central-difference Jacobian, tolerances, and operating inputs. Each endpoint must close below `1e-8`, remain interior and physical, retain rank `160`, local condenser rank `3`, condition below `1e8`, stable singular spectra, exact conservation and provider ownership, and pass condenser phase diagnostics. Physical root agreement must be below `1e-7`; calls must remain below `1,000,000` and wall below `600 s`. Exact memoization is permitted. Retry, continuation, alternate solver, bound/scale/tolerance changes, clipping, projection, fallback, timestep, or dynamics are prohibited.
- DD-223 executes `FR-019fh` once from frozen commit `420dc84` and fails decisively. The source and independent starts terminate after 51 and 47 function evaluations at scaled residuals `4.309924e-4` and `1.231733e-2`; both exceed `1e-8`, and their physical endpoints differ by `9.127158e-2`. Both endpoints remain positive, temperature/pressure ordered, interior, conservative, and numerically rank `160`, but Jacobian conditions deteriorate to `1.05e10-2.09e10` and `1.20e9-2.45e9`, with spectrum changes near one. Independent condenser checks also fail at the unconverged endpoints. Structural coloring limits the campaign to 236,304 logical calls and `50.383 s`; this is an efficiency success but not a root. DD-223 shall not be retried or tuned, neither endpoint is an initializer, and full-column timestep/dynamic work remains unauthorized. Any next work must be a separately governed static diagnosis or a materially different solver architecture, not another direct least-squares variation.
- DD-224 performs a zero-call completeness audit of the immutable DD-223 evidence. DD-223 saved both final coordinate vectors, complete physical states, block norms, Jacobian ranks, conditions, and singular spectra, but omitted the complete per-row residual vectors and Jacobian matrices needed to identify weak equations and variable directions. Static localization is therefore impossible from the current artifact alone. The saved coordinates and source ledger are sufficient for one exact read-only endpoint replay. DD-223's failed classification and no-retry decision remain unchanged. One separately frozen replay may evaluate both saved endpoints and both original colored finite-difference steps while recording complete residuals and matrices; nonlinear solving, state advance, timestep, and integration remain prohibited.
- `FR-019fi` freezes DD-225 as one read-only replay of DD-223's two exact full-column endpoints. It shall record complete raw and scaled residual vectors, complete colored `160 x 160` Jacobian matrices and singular vectors at `1e-5` and `5e-6`, reproduce the saved residual norms and singular spectra within fixed limits, retain rank and provider ownership, and remain below `50,000` calls and `180 s`. A nonlinear solve, state change, timestep, retry, or dynamic integration is prohibited.
- DD-225 executes `FR-019fi` once from frozen commit `ae2b716` and passes every gate. Both endpoint residual norms and all four singular spectra reproduce DD-223 with zero reported difference; all four matrices retain rank 160. Complete row-level residuals, matrices, and singular vectors are saved. Provider ownership passes with `12,474` logical calls in `6.169 s`. No solve, state change, timestep, or integration occurs. Zero-call conditioning localization is authorized; DD-223 remains failed and its endpoints remain unaccepted.
- DD-226 performs the authorized zero-call conditioning localization. The near-100% finite-difference spectrum change is traced to `francis_hydraulics[stripping_volume_6]` versus `T[stripping_volume_6]`; halving the step nearly doubles that entry and the largest singular value. Separately, the weakest singular direction is dominated by upper-column n-pentane component balances and liquid/vapor composition coordinates. Diagnostic row-and-column equilibration reduces conditions from `1.20e9-2.09e10` to about `631-1,274`, showing a material scaling problem rather than proving physical singularity. One frozen direct-column probe may distinguish colored-pattern contamination from a discontinuous property/hydraulic derivative. Equation changes and root solving remain stopped.
- `FR-019fj` freezes DD-227 as one direct-column probe of the single residual/coordinate pair identified generically by DD-226. At both saved endpoints it shall perturb only that coordinate at `2e-5`, `1e-5`, `5e-6`, and `2.5e-6`, compare direct derivatives with saved colored entries, record density/height/head/Francis/residual values on both sides, prove exact endpoint repeatability, preserve provider ownership, and remain below `20,000` calls and `120 s`. Solving, state adjustment, retry, timestep, and integration are prohibited.
- DD-227 executes `FR-019fj` once from frozen commit `54eae89` and passes every gate. Direct and colored target derivatives agree exactly. The derivative is not step-stable because DWSIM's declared liquid density switches between approximately `0.46-0.47` and `0.57-0.58 lbmol/ft3` across temperature changes of only millionths of a degree. The resulting height, weir-head, and Francis-flow jump creates the false large derivative. Baseline repeats are exact. The probe uses `1,980` calls in `3.379 s` with no solve or state advance. A phase-explicit, parameter-aligned PR liquid-density candidate may be tested before scaling or root solving; Francis hydraulics and coloring shall not be changed.
- DD-228 implements a validation-only phase-explicit liquid-density calculation using the smallest positive root of the same parameter-aligned Peng-Robinson equation used by the independent thermo validator. Across every DD-227 sample, the candidate remains positive and smooth; endpoint densities are `0.503433` and `0.504700 lbmol/ft3`, and derivative relative spread remains below `5e-10` across all four step sizes. The calculation uses zero DWSIM, solver, timestep, or integration calls. One frozen residual/Jacobian parity audit may route only governing liquid density to this candidate while retaining DWSIM fugacity and enthalpy. Root solving and dynamics remain stopped.
- `FR-019fk` freezes DD-229 as one full residual/Jacobian parity audit at both exact DD-223 endpoints. DWSIM shall retain governing imposed-phase fugacity and phase enthalpy, while parameter-aligned PR shall explicitly own liquid density. Two 15-color Jacobians per endpoint at `1e-5` and `5e-6` must retain rank 160, condition below `1e8`, spectrum and matrix stability, conservation, physicality, provider ownership, calls below `20,000`, and wall below `180 s`. Complete matrices shall be saved in compressed evidence. A solve, state change, retry, timestep, or integration is prohibited.
- DD-229 executes `FR-019fk` once from frozen commit `41af14f` and passes every gate. Conditions fall from DD-223's `1e9-2e10` range to `3.36e6` and `4.02e5`; spectrum changes are below `4e-9`, and full-matrix step changes are about `1.5e-10`. Rank, conservation, physicality, and explicit provider routing pass with `12,474` calls in `5.643 s`. Residuals rise to `0.1204/0.1436` because the old endpoints were fitted to discontinuous density branches and are not roots under corrected density. One zero-call fixed coordinate-scaling design is authorized before a materially new frozen root campaign. Dynamics remain stopped.
- DD-230 derives one fixed 160-coordinate scale from the inverse geometric-mean column norm across all four accepted DD-229 matrices. The scale ratio is `8.813`; all four conditions improve by `1.80-1.96x` to `2.05e5-1.86e6`, safely below the existing `1e8` limit. The design uses zero model, provider, solver, timestep, or integration calls. One frozen two-start stationary-root campaign is authorized with explicit aligned-PR density routing and this exact scale. The existing bounds, residual scales, solver, colored Jacobian, and acceptance rules shall remain unchanged; retry, tuning, continuation, and dynamics are prohibited.
- `FR-019fl` freezes DD-231 as one two-start full-C3/C4 stationary-root campaign using the exact DD-223 endpoints, DD-230 coordinate scale, existing residual scales/bounds/solver/15-color Jacobian, DWSIM imposed-phase fugacity and enthalpy, and aligned-PR smallest-root liquid density. Both endpoints must close below `1e-8`, agree physically below `1e-7`, retain rank 160, condition below `1e8`, stable spectra, no active bounds, physicality, phase validity, conservation, and explicit provider ownership. Calls shall remain below `1,000,000` and wall below `600 s`. Retry, tuning, continuation, fallback, timestep, and integration are prohibited.
- DD-231 executes `FR-019fl` once from frozen commit `5f42644` and passes every gate. The two starts close at `8.26e-14` and `4.22e-15`, agree physically to `5.95e-13`, retain rank 160 with worst condition `6.20e5`, and have spectrum changes below `5e-9`. No bound is active. The accepted root has `D=2431.550`, `B=4711.424 lbmol/h`, `Q_C=-50.0522 MMBTU/h`, top/bottom temperatures `119.179/219.908 F`, distillate propane/butane `0.897031/0.102937`, and bottoms propane `0.042411`. The campaign uses `821,564` calls in `179.634 s`. One structural full-column dynamic-DAE mapping contract is authorized; no timestep has occurred.
- `FR-019fm` requires DD-232 to map the accepted DD-231 root into the full-column controlled dynamic and BDF2 ledgers without a live property or numerical model call. All 160 stationary coordinates shall be consumed exactly once as 60 component-inventory states, 98 dynamic algebraic coordinates, and two bumpless product references. The controlled solve shall remain square and full-rank at 162, with zero initial component rates, PI rates, PI memories, and product log ratios. Both BDF2 component-history levels shall repeat the accepted inventory exactly; all 40 energy-history and four PI-history coordinates shall have explicit ownership, for 164 total history values. Energy values and geometry-derived level setpoints shall be reconstructed only in a separately frozen live audit using DWSIM enthalpy and aligned-PR liquid density. No residual, Jacobian, solve, controller advance, timestep, or integration is permitted.
- DD-232 implements `FR-019fm` and passes every gate. The accepted 160-coordinate root reproduces from its saved physical state within `1e-12`; all source coordinates are partitioned once into 60 inventory states, 98 algebraic coordinates, and two product references. The 158-variable open-loop DAE, 162-variable controlled DAE, and 164-value BDF2 history ledger remain complete and full-rank. Both inventory histories repeat the accepted state and all PI seed values are zero. Internal-energy history and level setpoints remain explicitly deferred to the live provider audit. No DWSIM, property, residual, Jacobian, solve, controller advance, timestep, or integration call occurs. One separately frozen live full-column zero-motion residual and leading-Jacobian audit is authorized; a timestep remains unauthorized.
- `FR-019fn` freezes DD-233 as one live zero-motion audit of the complete 20-stage controlled BDF2 handoff. It shall reconstruct internal energies and geometry-derived terminal level setpoints from the accepted DD-231 state using DWSIM imposed-phase fugacity/enthalpy and aligned-PR smallest-root liquid density, then copy identical inventory, energy, and PI-memory values into both history levels at a nominal `0.25 s` history interval. The complete `162 x 162` zero-motion residual must close below `1e-8`; inventory, component, energy, PI, and level motion must remain below their frozen limits. Two colored central-difference Jacobians at `1e-5` and `5e-6`, using the inherited DD-230 scale on algebraic/product coordinates and unit normalized-rate scales, must retain rank 162, condition below `1e8`, stable spectra, and no zero rows or columns. Fifteen predeclared direct sentinel columns must agree with the colored matrix below `1e-6` relative. Physicality, equilibrium, conservation, provider ownership, calls below `150,000`, and wall below `300 s` are mandatory. No nonlinear solve, controller advance, accepted timestep, or integration may occur.
- DD-233 executes `FR-019fn` once from frozen commit `80668bb` and passes every gate. The complete controlled BDF2 residual closes at `8.26e-14`; inventory motion and component, energy, and PI rates are exactly zero. Geometry-derived top/bottom levels are `0.439572/0.528367` with zero controller error. Both 17-color Jacobians retain rank 162, condition `5.80e6`, spectrum change `3.82e-7`, and no zero rows or columns; all 15 direct sentinel columns agree exactly. Physicality, equilibrium, conservation, and explicit DWSIM-enthalpy/aligned-PR-density ownership pass with 9,900 logical calls in 4.474 seconds. No nonlinear solve, controller advance, accepted timestep, or integration occurs. One separately frozen stationary hold-step contract is authorized; moving dynamics remain unauthorized.
- `FR-019fo` freezes DD-234 as the first full-column controlled timestep: one `0.25 s` backward-Euler stationary startup candidate and two successive `0.125 s` backward-Euler refinement steps from the accepted DD-233 handoff. All three roots shall use the unchanged controlled equations, residual scales, DWSIM fugacity/enthalpy, aligned-PR density, topology-colored central differences, DD-233 coordinate scale, solver, levels, PI parameters, and product references. Every root must close below `1e-8`, retain rank 162 and condition below `1e8`, remain physical and conservative, and keep component/energy/PI rates, inventory, algebraic state, controller memory, products, and levels within frozen stationary tolerances. Discrete component, energy, and PI kinematic identities and full-versus-refined agreement are mandatory. Calls shall remain below `100,000` and wall below `300 s`. Disturbance, tuning, retry, fallback, and trajectory execution are prohibited. A pass may authorize only one separately frozen small moving-step contract.
- DD-234 executes `FR-019fo` once from frozen commit `a194cfd` and passes every gate. The full `0.25 s` root and both `0.125 s` roots close at `8.26e-14` in `3/1` function/Jacobian evaluations each. All Jacobians retain rank 162; worst scaled condition is `1.05e7`. Component, energy, and PI rates and all inventory, algebraic, controller-memory, product, and level movements are exactly zero in saved arithmetic. Full-versus-refined differences are also exactly zero. Physicality, equilibrium, conservation, kinematics, provider ownership, call, and wall gates pass with 11,583 calls in 9.430 seconds. The accepted full step supplies a valid stationary backward-Euler startup history. One separately frozen small full-column moving-step contract is authorized; trajectories and controller tuning remain unauthorized.
- `FR-019fp` freezes DD-235 as the first full-column controlled moving-step gate. From DD-234's accepted full-step history, the feed component rates and total feed enthalpy shall increase together by exactly `0.1%`, preserving feed composition and specific enthalpy. One `0.25 s` backward-Euler root shall be compared with two successive `0.125 s` roots using unchanged equations, thermodynamics, density routing, controllers, products, bounds, residual scales, coordinate scales, and solver. Every root must close below `1e-8`, retain rank 162 and condition below `1e8`, remain physical, equilibrated, conservative, and kinematically consistent. Total inventory response must lie between `1e-4` and `1e-2 lbmol`; component-inventory identity must close below `1e-6 lbmol`. Full-versus-refined differences must remain below `1e-4 lbmol` per component, `2e-4 lbmol` in component L1 norm, `1e-9 lbmol` signed total, `1e-5` in rate and algebraic coordinates, `1e-8` in controller memories and levels, and `1e-7` in product-relative coordinates. Calls shall remain below `100,000` and wall below `300 s`. Retry, tuning, alternate disturbance, fallback, and trajectory execution are prohibited. A complete pass may authorize only one separately frozen short full-column trajectory contract.
- DD-235 executes `FR-019fp` once from frozen commit `11b2380` and passes every gate. The full and refined paths accumulate `4.960399e-4 lbmol`, matching their external component balances within `4.43e-13 lbmol`. All three roots close below `1.28e-12`, retain rank 162, and have worst condition `1.052341e7`. The worst full-versus-refined component difference is `2.490470e-6 lbmol`, component L1 difference is `8.890772e-6 lbmol`, signed total difference is `1.300166e-12 lbmol`, and rate/algebraic differences are below `2.14e-7`. Equilibrium, conservation, kinematics, physicality, controller, provider, call, and wall gates pass with 39,204 logical calls in 14.239 seconds. No tuning, retry, or trajectory occurs. One separately frozen short full-column trajectory contract is authorized; long-run dynamics and controller tuning remain unauthorized.
- `FR-019fq` requires the vapor-holdup work to begin as a separately versioned property-free successor rather than changing any accepted Core V3 V1 contract or historical result. Every physical volume shall own positive declared vapor volume and conserved `N_L[j,k]` and `N_V[j,k]` states. Vapor composition shall be derived only from `N_V`; independent algebraic `y` coordinates are prohibited. The ledger shall include liquid and vapor component balances, equal-and-opposite interphase transfer, full fugacity equilibrium, liquid hydraulics, vapor EOS/free-volume closure, interstage pressure drop, one pressure anchor, and total two-phase energy storage. Physical and provider ownership shall be unique and complete. Both the five-volume development topology and 20-volume C3/C4 topology must be square, full rank, conservative, and free of property, residual, solve, timestep, or integration calls. Structural test volumes shall not be accepted as physical geometry.
- DD-236 implements `FR-019fq` and passes every gate. The five-volume/three-component successor has 30 conserved states and a full-rank `63 x 63` implicit ledger. The actual 20-volume C3/C4 topology has 120 conserved states and a full-rank `258 x 258` ledger. Every vapor state has a rate, balance, volume/EOS use, energy use, and provider owner; vapor composition is derived from inventory; phase transfer and both transport families cancel exactly. Tests reject undeclared/nonpositive volume, independent vapor composition, missing vapor balance or transfer unknown, duplicate pressure ownership, and liquid-only energy storage. All 550 focused and Core V3 tests pass when pytest uses a workspace-owned temporary directory. No live or numerical work occurs. Real tray/vessel free-volume geometry remains mandatory before the authorized live vapor-property and EOS residual implementation.
- `FR-019fr` requires the actual C3/C4 workbook geometry to be mapped into the vapor-holdup successor before any live EOS evaluation. Tray control-volume gross capacity shall be stage cross-sectional area times tray spacing. Reflux-drum capacity shall use an explicit total volume or the declared horizontal tangent length, diameter, and two hemispherical heads. The combined bottom capacity shall include the vertical sump and declared reboiler vapor extension. Every source stage and generated volume shall map exactly once. Endpoint free vapor volume shall be `gross capacity - live liquid inventory/live liquid density`; fixed gas-void fractions may seed or validate geometry but shall not prevent live liquid displacement. The mapping must retain the full-rank 20-volume structural ledger and make no property, residual, solve, timestep, or integration call.
- DD-237 implements `FR-019fr` and passes. All 20 workbook stages map exactly once to the generated topology. Gross capacities are `5101.729438 ft3` for the reflux drum, `338.945707-1129.819023 ft3` for tray bays, and `3405.501240 ft3` for the combined bottom volume, including a `291.900106 ft3` reboiler vapor extension. The physical mapping retains the full-rank `258 x 258` vapor-holdup ledger. A reusable evaluator subtracts live liquid volume and rejects overfilled control volumes. Six focused geometry tests and all 556 focused/Core V3 tests pass. Endpoint free volume remains deliberately unevaluated until the separately audited live liquid-density and vapor-property layer.
- `FR-019fs` requires live vapor-holdup reconstruction to use the accepted provider ownership without fallback: aligned parameter-specific PR shall own liquid density, while DWSIM shall own vapor compressibility and liquid/vapor phase enthalpy. For every physical volume, free vapor space shall be gross capacity minus live liquid displacement, resident vapor inventory shall satisfy `N_V=P*V_free/(Z*R*T)`, component vapor inventory shall be `N_V*y`, and stored energy shall include both `N_L*u_L` and `N_V*u_V` with consistent pressure-work conversion. The audit shall reject invalid inventory/composition identity, overfill, nonpositive pressure/temperature/properties, non-governing property use, an incomplete call ledger, or EOS relative residual above `1e-12`. It shall not evaluate the full DAE residual, solve, step, or integrate.
- DD-238 implements `FR-019fs` and passes. At the accepted DD-231 C3/C4 root, all 20 physical volumes have positive free space, density, vapor compressibility, and resident vapor inventory. Exactly 80 governing property calls are recorded with no fallback. The reconstructed column contains `473.563386 lbmol` of vapor versus `2909.337841 lbmol` of liquid, a vapor/liquid mole ratio of `0.162774`; the minimum free volume is `252.365940 ft3`, minimum vapor inventory is `11.121869 lbmol`, and maximum relative EOS residual is `1.122839e-16`. Both liquid and vapor stored energy are finite. The result authorizes implementation of the full two-phase component/energy residual only; it is not a root, step, or dynamic authorization.
- `FR-019ft` requires the successor component equations to publish liquid transport, vapor transport, and interphase transfer separately. Positive `M_VL[j,k]` shall mean vapor-to-liquid transfer and shall appear with equal and opposite signs in the phase balances. Summing the phase equations shall exactly recover the total component balance. Stream enthalpy transport shall telescope to feed plus duties minus live-composition products. Before full residual assembly, the accepted stationary state shall pass separate zero-rate liquid and vapor component residuals below `1e-7 lbmol/h`, energy residual below `1e-4 BTU/h`, component telescoping below `1e-10 lbmol/h`, and energy telescoping below `1e-6 BTU/h` without solve or state movement.
- DD-239 implements `FR-019ft` and passes. Stationary phase transfer is derived from each local vapor transport balance and applied with the opposite sign to the liquid balance. Vapor residuals are exactly zero; maximum liquid/total residual is `1.055384e-9 lbmol/h`; maximum energy residual is `9.220093e-8 BTU/h`; global component and energy telescoping errors are `2.842171e-13 lbmol/h` and exactly zero. Phase transfer cancels exactly. The result authorizes assembly of the complete 258-equation residual, including fugacity, EOS, Francis, pressure-drop, and pressure-anchor rows; it does not authorize a solve or timestep.
- `FR-019fu` requires one complete implicit endpoint residual for the conserved vapor-holdup successor. Its 258 coordinates and rows shall cover liquid/vapor component rates, phase transfer, temperature, pressure, liquid/vapor traffic, condenser duty, separate phase balances, full fugacity equilibrium, vapor EOS, total two-phase energy, Francis hydraulics, vapor pressure drop, and one top pressure anchor. Positive endpoint inventories and flows shall use smooth transformed coordinates. Properties shall be evaluated at the same endpoint state with the DD-238 provider ownership and no fallback. The accepted DD-231 state may be audited only as a reference point; no root, timestep, or integration is authorized.
- DD-240 implements `FR-019fu` and passes. The inherited equilibrium, EOS, separate component balances, energy, and Francis blocks close to numerical precision; maximum relative EOS error is `4.036427e-16`. The only material residual is the new pressure-drop block at `0.620424 psia`, as expected because DD-231 prescribed rather than solved the pressure profile. The complete ledger is `258 x 258`, uses 120 governing calls without fallback, and has a 28-color structural Jacobian pattern. DD-240 authorizes only a two-step Jacobian audit.
- `FR-019fv` requires two 28-color central-difference Jacobians of the DD-240 residual at steps `1e-5` and `5e-6`. Both must retain rank 258, condition below `1e8`, no zero rows or columns, stable matrices and spectra, and exact agreement across direct sentinel columns spanning every coordinate family. Calls shall remain below 25,000 and wall below 180 seconds. No solve or timestep is permitted.
- DD-241 executes `FR-019fv` and passes. Both Jacobians retain rank `258`, condition `1.323785e6`, no zero rows or columns, spectrum change `3.240265e-11`, and matrix change `4.517349e-11`. All eight sentinels pass. Exact memoization limits work to 15,360 logical calls and 2.181 seconds audited wall. This validates the implicit endpoint equations, but does not by itself define a stationary initializer because its inventory-rate coordinates represent a time step.
- `FR-019fw` requires a separate stationary initializer contract that solves resident `N_L` and `N_V` directly with zero accumulation. It shall not reuse endpoint-rate coordinates. Reflux and reboiler duty remain fixed operating inputs; condenser duty, distillate flow, and bottoms flow are algebraic variables. Geometry-based top and bottom liquid-inventory targets shall provide terminal level closure. The five-volume and 20-volume systems must be square, full rank, generic in component count, and property-free before a numerical residual is implemented.
- DD-242 implements `FR-019fw` and passes. The five-volume system is `65 x 65`, rank 65; the full C3/C4 system is `260 x 260`, rank 260. Both have zero structural nullity and no empty or unregistered rows or variables. Distillate and bottoms are solved to satisfy explicit reflux-drum and sump inventory targets. This avoids mislabeling an implicit time step as a steady initializer and authorizes only a live stationary residual.
- `FR-019fx` requires the 260-row stationary residual to use the same phase balances, fugacity, EOS, total energy, Francis, pressure-drop, pressure-anchor, geometry, and provider ownership as the validated implicit residual. Accumulation rates shall be exactly zero. The final two rows shall enforce the top and bottom liquid-inventory targets. Product rates shall be positive transformed variables. At the inherited reference state, all rows must be finite, terminal targets and EOS must close, provider ownership must pass, and no solve or timestep may occur.
- DD-243 implements `FR-019fx` and passes. The full `260 x 260` residual is finite; both terminal inventory residuals are exactly zero and maximum relative EOS error is `4.036427e-16`. All inherited stationary blocks close to numerical precision except the expected `0.620424 psia` pressure-drop mismatch. The residual uses 120 governing calls without fallback and retains a 28-color pattern. One two-step stationary Jacobian audit is authorized.
- `FR-019fy` requires two 28-color central-difference Jacobians of the DD-243 stationary residual at `1e-5` and `5e-6`, rank 260, condition below `1e8`, no zero rows or columns, stable matrices and spectra, and direct checks spanning inventories, transfer, temperature, pressure, both flow families, condenser duty, and both product rates. Calls shall remain below 30,000 and wall below 180 seconds. No nonlinear solve, timestep, or integration is permitted.
- DD-244 executes `FR-019fy` and passes decisively. Both stationary Jacobians retain rank `260`, condition about `6.520281e4`, no zero rows or columns, spectrum change `4.532003e-10`, and matrix change `6.035437e-10`. All ten sentinel columns pass. The audit uses 15,840 logical calls in 2.027 seconds with no fallback. One separately frozen bounded stationary vapor-holdup root campaign is authorized. Dynamic integration remains unauthorized.
- `FR-019fz` freezes DD-245 as one bounded stationary vapor-holdup root campaign from the DD-243 reference point. It shall use one start, `least_squares(method="trf")`, the 28-color central-difference Jacobian at `1e-5`, fixed transformed bounds, and one fixed inverse-column-norm coordinate scale derived only from the two saved DD-244 matrices. The root must close below `1e-8`, remain strictly interior and physical with ordered pressure, satisfy component, energy, fugacity, EOS, pressure, and terminal-inventory limits, and retain rank 260 with condition below `1e8` at `1e-5` and `5e-6`. Calls shall remain below `1,000,000` and wall below `600 s`. Retry, continuation, alternate solver, tolerance or bound changes, clipping, projection, fallback, timestep, and integration are prohibited. Failure stops stationary nonlinear work; success authorizes only a separately frozen dynamic handoff contract.
- DD-245 executes `FR-019fz` once from frozen commit `bccdd5b` and passes every gate. The stationary root closes at `3.050319e-11` in 23 function and 22 Jacobian evaluations. Both endpoint Jacobians retain rank 260, condition about `5.242958e4`, no zero rows or columns, spectrum change `4.863755e-11`, and matrix change `3.334973e-10`. Component, energy, pressure, terminal inventory, fugacity, and relative EOS maxima are `2.421e-8 lbmol/h`, `3.272e-5 BTU/h`, `3.865e-14 psia`, `2.274e-13 lbmol`, `2.494e-13`, and `6.593e-15`. The physical interior endpoint has ordered pressure, D/B=`2519.764/4623.210 lbmol/h`, `Q_C=-50.9973 MMBTU/h`, `2907.909 lbmol` liquid, and `463.640 lbmol` vapor. It uses 164,160 logical calls in 23.437 seconds without fallback, retry, continuation, timestep, or integration. A zero-call reporting compaction reduces duplicated per-state provider evidence from 38.5 MB to 38 KB while preserving all call and route totals. One separately frozen stationary dynamic-handoff contract is authorized.
- `FR-019ga` requires a property-free DD-246 mapping of every accepted DD-245 coordinate into the successor dynamic ownership ledger. Exactly 120 `N_L/N_V` values shall become conserved current states; their 120 initial derivatives shall be zero; 138 phase-transfer, temperature, pressure, liquid/vapor flow, and condenser-duty values shall become algebraic values; and D/B shall become fixed stationary product references. Both BDF2 component-history levels shall repeat all 120 states exactly. Forty total two-phase energy-history values shall have explicit current/previous slots but remain unevaluated until one live audit. No coordinate may be omitted or consumed twice, and no property, residual, solver, controller, timestep, or integration call is permitted.
- DD-246 implements `FR-019ga` and passes. All 260 stationary coordinates are consumed exactly once as 120 conserved states, 138 algebraic values, and two product references. All 120 derivative values are zero. Both component-history levels repeat the accepted root exactly for 240 stored values; 40 current/previous total-energy slots are explicit and deferred. The successor dynamic structure remains full rank at 258. No property, residual, solve, controller, timestep, or integration call occurs. One live zero-motion energy reconstruction and residual/Jacobian audit is authorized.
- `FR-019gb` requires DD-247 to reconstruct current and previous `U_total=U_L+U_V` from the accepted DD-245 inventories using the same aligned-PR density and DWSIM vapor-Z/phase-enthalpy ownership, then evaluate the complete 258-row implicit residual at zero motion for a nominal `0.25 s` step. Both energy histories and both component histories shall be identical. All 120 inventory rates must be exactly zero; the residual must close below `1e-8`; two 28-color Jacobians at `1e-5` and `5e-6` must retain rank 258, condition below `1e8`, stable spectra, and no zero rows or columns. Calls shall remain below 30,000 and wall below 180 seconds. No nonlinear solve, accepted timestep, or integration is permitted.
- DD-247 executes `FR-019gb` and passes. The zero-motion residual is `3.050319e-11`; all 120 liquid/vapor inventory rates are exactly zero; current and previous two-phase energy histories are identical. Liquid and vapor stored-energy totals are `-1.503421e7` and `5.236712e4 BTU`. Both `258 x 258` Jacobians retain rank 258, condition about `1.136251e7`, no zero rows or columns, spectrum change `3.594104e-11`, and matrix change `5.690927e-11`. Provider ownership passes in 13,640 calls and 5.724 seconds. One separately frozen stationary hold-step contract is authorized; no timestep has yet been accepted.
- `FR-019gc` freezes DD-248 as one `0.25 s` backward-Euler stationary hold from the accepted DD-247 history. It shall use `least_squares(method="trf")`, the 28-color central Jacobian at `1e-5`, fixed tight transformed bounds, and at most 20 function evaluations. The endpoint residual must close below `1e-8`, maximum coordinate movement must remain below `1e-8`, maximum inventory rate below `1e-5 lbmol/h`, and two endpoint Jacobians must retain rank 258, condition below `1e8`, and stable spectra. Calls shall remain below 100,000 and wall below 180 seconds. Disturbance, retry, continuation, alternate settings, controller action, fallback, and integration are prohibited.
- DD-248 executes `FR-019gc` once from frozen commit `5add7b7` and passes exactly. The `0.25 s` backward-Euler solve terminates in one function and one Jacobian evaluation at the original coordinate vector. Maximum coordinate movement and every liquid/vapor inventory rate are exactly zero; the scaled residual remains `3.050319e-11`. Both endpoint Jacobians retain rank 258 and condition about `1.136251e7`, with spectrum and matrix changes below `5.7e-11`. Provider ownership passes in 20,400 calls and 5.882 seconds without fallback, retry, disturbance, or integration. One separately frozen small moving-step/refinement contract is authorized.
- `FR-019gd` freezes DD-249 as the first moving response of the vapor-holdup successor. Feed component rates and feed enthalpy shall increase together by exactly `0.1%`, preserving feed composition and specific enthalpy. One `0.25 s` backward-Euler endpoint shall be compared with two successive `0.125 s` endpoints while product rates, reflux, reboiler duty, and the top-pressure anchor remain fixed. Every endpoint must close below `1e-8`, retain rank 258 and condition below `1e8`, remain physical, satisfy fugacity and EOS closure, and reproduce its discrete external component and energy balances. The positive total inventory response must lie between `1e-4` and `1e-2 lbmol`. Full-versus-refined component inventory, temperature, pressure, flow, phase-transfer, and condenser-duty differences must remain within the fixed DD-249 contract limits. The refined endpoint Jacobian must be stable at `1e-5` and `5e-6`. Calls shall remain below 300,000 and wall below 300 seconds. Retry, alternate timestep, controller action, fallback, and trajectory execution are prohibited. Failure stops trajectory work; passing authorizes only one separately frozen short open-loop trajectory contract.
- DD-249 executes `FR-019gd` once from frozen commit `9434ef5` and passes every gate. The full and refined paths each accumulate `4.960399e-4 lbmol`, matching their discrete external component balances within `1.08e-13 lbmol`. All three endpoints close below `1.39e-12`, retain rank 258, and have worst condition `4.526523e7`. The worst full-versus-refined component difference is `2.430837e-6 lbmol`; temperature and pressure differences are `6.484e-7 F` and `1.063e-6 psia`. Fugacity, EOS, physicality, energy, provider ownership, refinement, call, and wall gates pass. The campaign uses 186,840 logical calls and 27.783 seconds without retry, controller action, alternate timestep, or trajectory. One separately frozen short open-loop trajectory contract is authorized.
- `N/A` in tests indicates behavior exists in implementation without a dedicated direct unit/integration assertion at present.
