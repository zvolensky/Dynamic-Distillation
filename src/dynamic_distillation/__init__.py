"""
dynamic_distillation package

PURPOSE
-------
Namespace package for dynamic distillation modeling components, including case
I/O, specification build/validation, thermo providers, RHS dynamics, runner/CLI,
and experiment-ledger tooling.

NOTES
-----
Modules in this package are versioned with `_v1` / `_v2` suffixes to preserve
explicit evolution paths while keeping call sites stable.
"""
